type: sections
max_columns: 4
title: Garage Rooms
path: garage-rooms
sections:
  - type: grid
    cards:
      - type: custom:auto-entities
        card:
          type: vertical-stack
        card_param: cards
        filter:
          template: >-
            {%- from 'macros/area_cards.jinja' import area_cards -%}
            {{- area_cards('exercise_room') | from_json -}}
  - type: grid
    cards:
      - type: custom:auto-entities
        card:
          type: vertical-stack
        card_param: cards
        filter:
          template: >-
            {%- from 'macros/area_cards.jinja' import area_cards -%}
            {{- area_cards('playroom') | from_json -}}
