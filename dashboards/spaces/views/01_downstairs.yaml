title: Downstairs
type: sections
max_columns: 4
sections:
  - type: grid
    cards:
      - type: custom:auto-entities
        card:
          type: vertical-stack
        card_param: cards
        filter:
          template: >-
            {%- from 'macros/area_cards.jinja' import area_cards -%}
            {{- area_cards('parlor') | from_json -}}
  - type: grid
    cards:
      - type: custom:auto-entities
        card:
          type: vertical-stack
        card_param: cards
        filter:
          template: >-
            {%- from 'macros/area_cards.jinja' import area_cards -%}
            {{- area_cards('living_room') | from_json -}}
  - type: grid
    cards:
      - type: custom:auto-entities
        card:
          type: vertical-stack
        card_param: cards
        filter:
          template: >-
            {%- from 'macros/area_cards.jinja' import area_cards -%}
            {{- area_cards('dining_room') | from_json -}}
  - type: grid
    cards:
      - type: custom:auto-entities
        card:
          type: vertical-stack
        card_param: cards
        filter:
          template: >-
            {%- from 'macros/area_cards.jinja' import area_cards -%}
            {{- area_cards('breakfast_room') | from_json -}}
  - type: grid
    cards:
      - type: custom:auto-entities
        card:
          type: vertical-stack
        card_param: cards
        filter:
          template: >-
            {%- from 'macros/area_cards.jinja' import area_cards -%}
            {{- area_cards('kitchen') | from_json -}}
  - type: grid
    cards:
      - type: custom:auto-entities
        card:
          type: vertical-stack
        card_param: cards
        filter:
          template: >-
            {%- from 'macros/area_cards.jinja' import area_cards -%}
            {{- area_cards('pantry') | from_json -}}
  - type: grid
    cards:
      - type: custom:auto-entities
        card:
          type: vertical-stack
        card_param: cards
        filter:
          template: >-
            {%- from 'macros/area_cards.jinja' import area_cards -%}
            {{- area_cards('laundry_room') | from_json -}}
