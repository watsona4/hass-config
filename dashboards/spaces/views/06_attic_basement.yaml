type: sections
max_columns: 4
title: Attic & Basement
path: attic-basement
sections:
  - type: grid
    cards:
      - type: custom:auto-entities
        card:
          type: vertical-stack
        card_param: cards
        filter:
          template: >-
            {%- from 'macros/area_cards.jinja' import area_cards -%}
            {{- area_cards('attic') | from_json -}}
  - type: grid
    cards:
      - type: custom:auto-entities
        card:
          type: vertical-stack
        card_param: cards
        filter:
          template: >-
            {%- from 'macros/area_cards.jinja' import area_cards -%}
            {{- area_cards('basement') | from_json -}}
