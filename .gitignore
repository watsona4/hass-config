############################
# Home Assistant config
############################

# --- Core state, caches, runtime ---
config/.homeassistant_v2.db
config/home-assistant.db
config/home-assistant_v2.db
config/home-assistant_v2.db-*
config/home-assistant.log
config/home-assistant.log.*
config/*.log*
config/*.db
config/*.db-journal
config/.HA_VERSION
config/.storage/
config/.cloud/
config/tts/
config/deps/
config/lib/
config/ssh/
config/.uuid
config/.timeline
config/.vacuum
config/SERVICE_ACCOUNT.JSON

config/dashboards/spaces/views/
config/dashboards/environment/plant_sections
config/packages/plants
config/zwift_access_token.txt
config/zwift_id_token.txt
config/zwift_refresh_token.txt
config/packages/strava.yaml
config/packages/xert_activities.yaml

# Ignore heavy binary assets, allow config assets
config/www/community 
config/www/**/*.jpg
config/www/**/*.jpeg
config/www/**/*.png
config/www/**/*.gif
config/www/**/*.mp4
config/www/**/*.mp3
config/www/**/*.wav
config/www/**/*.zip
config/www/**/*.tar
config/www/**/*.gz
config/www/**/*.pdf
config/www/**/*.avif
!config/www/**/*.js
!config/www/**/*.css
!config/www/**/*.yaml
!config/www/**/*.json
config/www/ride/*.json

# Old OpenZWave / OZW logs and DBs
config/ozw_log.txt
config/pyozw.sqlite

# iOS app & push registrations
config/.ios.conf
config/.ios*
config/html5_push_registrations.conf

# Device/Integration local artifacts
config/google_calendars.yaml
config/google_calendars.yaml.*
config/google_calendars/
config/icloud/
config/ip_bans.yaml
config/known_devices.yaml
config/phue.conf
config/plex.conf
config/tradfri.conf
config/webostv.conf
config/harmony_media_room.conf

# --- Secrets & credentials ---
# Keep live secrets out of Git. If you use encrypted secrets,
# you can add a negation rule (see note below).
config/secrets.yaml
config/secrets/*.yaml
config/*.key
config/*.pem
config/*.p12
config/*.crt
config/*.csr
config/*.gpg
config/*.age

# If using encrypted secrets tracked in Git, uncomment one:
# !config/secrets.enc.yaml
# !config/secrets.sops.yaml

# --- Temporary / build noise ---
config/*.pid
config/**/__pycache__/
config/.custom_components/**/__pycache__/

# --- Backups/exports ---
config/backups/
config/*.tar
config/*.zip

# --- OS / editor junk ---
config/.DS_Store
config/Thumbs.db
config/*.swp
config/*.swo
config/.idea/
config/.vscode/

############################
# Optional: Development
############################
# If you do NOT want to track local custom components, uncomment:
# config/custom_components/

# If you keep diagrams or images that are generated, ignore them:
# config/image/
