# =============================================================================
# Package: Laptop Hardware Monitor
# =============================================================================
#
# Auto-generated by generate_hwmon_assets.py
# DO NOT EDIT - Changes will be overwritten
#
# Monitors Laptop system telemetry via MQTT and provides:
#   - Data sensors for CPU, memory, disk, network, etc.
#   - UI sensors for dashboard card attributes
#   - Binary sensors for health status
#
# Source telemetry sensor: sensor.laptop_oo58t6kn_laptop_oo58t6kn_hardware_telemetry
#
# =============================================================================

template:
  # ---------------------------------------------------------------------------
  # Data Sensors: Laptop
  # ---------------------------------------------------------------------------
  - trigger:
      - platform: homeassistant
        event: start
      - platform: state
        entity_id: sensor.laptop_oo58t6kn_laptop_oo58t6kn_hardware_telemetry
    variables:
      entity_id: sensor.laptop_oo58t6kn_laptop_oo58t6kn_hardware_telemetry
      available: "{{ has_value(entity_id) }}"
      host: "{{ state_attr(entity_id, 'host') or {} }}"
      health: "{{ state_attr(entity_id, 'health') or {} }}"
      cpus: "{{ state_attr(entity_id, 'cpus') or [] }}"
      memory: "{{ state_attr(entity_id, 'memory') or {} }}"
      filesystems: "{{ state_attr(entity_id, 'filesystems') or [] }}"
      ifaces: "{{ state_attr(entity_id, 'ifaces') or [] }}"
      gpus: "{{ state_attr(entity_id, 'gpus') or [] }}"
      tpus: "{{ state_attr(entity_id, 'tpus') or [] }}"
      batteries: "{{ state_attr(entity_id, 'batteries') or [] }}"
      drives: "{{ state_attr(entity_id, 'drives') or [] }}"
    sensor:
      - name: "Laptop Uptime (s)"
        unique_id: 822caf47-116b-5d8b-93c5-8035b271f950
        availability: "{{ available }}"
        state: "{{ (host.uptime_s | default(0)) | int }}"
        icon: "mdi:timer-outline"
        unit_of_measurement: "s"
        state_class: measurement
        device_class: duration
      - name: "Laptop Process Count"
        unique_id: e22dabda-9601-5692-b225-de8ca6b6e318
        availability: "{{ available }}"
        state: "{{ (host.process_count | default(0)) | int }}"
        icon: "mdi:application"
        state_class: measurement
      - name: "Laptop Overall Status Summary"
        unique_id: 7ab7d62f-bc50-513b-85e1-b81bde38f0c8
        availability: "{{ available }}"
        state: >-
          {{ (health.issues[0][:250] ~ '...') if health.issues and health.issues[0]|length > 250 else (health.issues[0] if health.issues else 'OK') }}
        icon: "mdi:card-text-outline"
      - name: "Laptop Updates Pending"
        unique_id: bec269e8-d1e4-5c58-a792-ccaaa826df76
        availability: "{{ available }}"
        state: >-
          {{ ((health.updates | default({})).pending | default(0)) | int }}
        icon: "mdi:update"
        state_class: measurement
      - name: "Laptop Updates List"
        unique_id: 550a27e0-630f-5f42-a32e-5aa2e36dfd51
        availability: "{{ available }}"
        state: >-
          {{ ((health.updates | default({})).get('items', []) | map(attribute='name') | list | length) }} packages
        icon: "mdi:package-variant"
        attributes:
          packages: "{{ (health.updates | default({})).get('items', []) | map(attribute='name') | list }}"
      - name: "Laptop Services Checked"
        unique_id: effb9d04-d45c-5ea1-97d5-defcf54ec1a2
        availability: "{{ available }}"
        state: "{{ (health.services | default([]) | length) }}"
        icon: "mdi:playlist-check"
        state_class: measurement
      - name: "Laptop Containers Checked"
        unique_id: d25626dd-71fb-5804-a863-2bf1b8aa5d9c
        availability: "{{ available }}"
        state: "{{ (health.containers | default([]) | length) }}"
        icon: "mdi:docker"
        state_class: measurement
      - name: "Laptop CPU Utilization"
        unique_id: 60ffdf1b-ff51-56cb-8cda-e17d44e750c1
        availability: "{{ available }}"
        state: "{{ (cpus[0].load_pct | default(0)) | round(1) }}"
        icon: "mdi:chip"
        unit_of_measurement: "%"
        state_class: measurement
      - name: "Laptop CPU Load 1m"
        unique_id: c3fc0d05-8003-524c-b274-c28ba8d26bbe
        availability: "{{ available }}"
        state: "{{ (cpus[0].load_1m | default(0)) | round(2) }}"
        icon: "mdi:chip"
        state_class: measurement
      - name: "Laptop CPU Load 5m"
        unique_id: 7c3f465f-76d0-5854-b09a-aadb9639e543
        availability: "{{ available }}"
        state: "{{ (cpus[0].load_5m | default(0)) | round(2) }}"
        icon: "mdi:chip"
        state_class: measurement
      - name: "Laptop CPU Load 15m"
        unique_id: 6a098452-edfd-5b3c-8b47-d3e255ff85e9
        availability: "{{ available }}"
        state: "{{ (cpus[0].load_15m | default(0)) | round(2) }}"
        icon: "mdi:chip"
        state_class: measurement
      - name: "Laptop CPU Temp"
        unique_id: 205533c2-3e44-579c-a6dd-91d61e7526ec
        availability: "{{ available }}"
        state: "{{ (cpus[0].temp_c | default(0)) | round(1) }}"
        unit_of_measurement: "°C"
        state_class: measurement
        device_class: temperature
      - name: "Laptop CPU Physical Cores"
        unique_id: 239a8282-262c-5325-b0d7-4d1bf52e052c
        availability: "{{ available }}"
        state: "{{ cpus[0].num_physical_cores | default(0) }}"
        icon: "mdi:chip"
        state_class: measurement
      - name: "Laptop CPU Logical Cores"
        unique_id: 49c07730-ba47-56ad-90e8-c109dced1a1f
        availability: "{{ available }}"
        state: "{{ cpus[0].num_logical_cores | default(0) }}"
        icon: "mdi:chip"
        state_class: measurement
      - name: "Laptop CPU Power"
        unique_id: 40357354-bdd3-55fc-833c-c8494598780c
        availability: "{{ available }}"
        state: "{{ (cpus[0].power_w | default(0)) | round(1) }}"
        icon: "mdi:flash"
        unit_of_measurement: "W"
        state_class: measurement
        device_class: power
      - name: "Laptop Memory Percent"
        unique_id: 2bc1aabe-7ac2-5d55-be1f-ea9f68009d44
        availability: "{{ available }}"
        state: "{{ (memory.system.load_pct | default(0)) | round(1) }}"
        icon: "mdi:memory"
        unit_of_measurement: "%"
        state_class: measurement
      - name: "Laptop Memory Used"
        unique_id: c9b3f135-288a-58a8-bcb9-05e4f1f4aa21
        availability: "{{ available }}"
        state: >-
          {{ ((memory.system.used_b | default(0)) / 1073741824) | round(2) }}
        unit_of_measurement: "GiB"
        state_class: measurement
        device_class: data_size
      - name: "Laptop Memory Available"
        unique_id: 2f995ee8-9e39-5478-af53-0d6a73da1390
        availability: "{{ available }}"
        state: >-
          {{ ((memory.system.available_b | default(0)) / 1073741824) | round(2) }}
        unit_of_measurement: "GiB"
        state_class: measurement
        device_class: data_size
      - name: "Laptop Memory Total"
        unique_id: 6887c69e-2a5a-5ca5-92ac-411eddabf381
        availability: "{{ available }}"
        state: >-
          {{ ((memory.system.total_b | default(0)) / 1073741824) | round(2) }}
        unit_of_measurement: "GiB"
        state_class: measurement
        device_class: data_size
      - name: "Laptop Swap Percent"
        unique_id: 825ce063-c35a-550d-8a28-5a6b226dc2d4
        availability: "{{ available }}"
        state: "{{ (memory.virtual.load_pct | default(0)) | round(1) }}"
        icon: "mdi:memory"
        unit_of_measurement: "%"
        state_class: measurement
      - name: "Laptop FS Windows (C:) Percent"
        unique_id: 0c8a2a5d-531a-5823-bd6c-c6d48ae8ebef
        availability: "{{ available }}"
        state: >-
          {% set fs = filesystems | selectattr('label', 'eq', 'Windows') | first | default(none) %}{{ ((fs.used_b / (fs.used_b + fs.available_b)) * 100) | round(1) if fs else 0 }}
        icon: "mdi:harddisk"
        unit_of_measurement: "%"
        state_class: measurement
      - name: "Laptop FS Windows (C:) Used"
        unique_id: 7c9fb26e-a299-528a-85e9-575be2e46b4d
        availability: "{{ available }}"
        state: >-
          {% set fs = filesystems | selectattr('label', 'eq', 'Windows') | first | default(none) %}{{ ((fs.used_b | default(0)) / 1073741824) | round(2) if fs else 0 }}
        icon: "mdi:harddisk"
        unit_of_measurement: "GiB"
        state_class: measurement
        device_class: data_size
      - name: "Laptop FS Windows (C:) Total"
        unique_id: 431e0f8b-bc95-543b-9878-9c911f406f18
        availability: "{{ available }}"
        state: >-
          {% set fs = filesystems | selectattr('label', 'eq', 'Windows') | first | default(none) %}{{ (((fs.used_b | default(0)) + (fs.available_b | default(0))) / 1073741824) | round(2) if fs else 0 }}
        icon: "mdi:harddisk"
        unit_of_measurement: "GiB"
        state_class: measurement
        device_class: data_size
      - name: "Laptop WiFi Upload"
        unique_id: 560da956-1e50-5b09-a491-55893cc3f48e
        availability: "{{ available }}"
        state: >-
          {% set iface = ifaces | selectattr('name', 'eq', 'Wi-Fi') | first | default(none) %}{{ (iface.rate_up_bps | default(0)) | round(0) if iface else 0 }}
        icon: "mdi:upload"
        unit_of_measurement: "B/s"
        device_class: data_rate
      - name: "Laptop WiFi Download"
        unique_id: 53865479-2d2d-5ee1-9274-97f3d970a050
        availability: "{{ available }}"
        state: >-
          {% set iface = ifaces | selectattr('name', 'eq', 'Wi-Fi') | first | default(none) %}{{ (iface.rate_down_bps | default(0)) | round(0) if iface else 0 }}
        icon: "mdi:download"
        unit_of_measurement: "B/s"
        device_class: data_rate
      - name: "Laptop WiFi IPv4"
        unique_id: 72c65b4c-3366-5f9f-adc5-c8ad710c26fb
        availability: "{{ available }}"
        state: >-
          {% set iface = ifaces | selectattr('name', 'eq', 'Wi-Fi') | first | default(none) %}{{ iface.ipv4 | default('unknown') if iface else 'unknown' }}
      - name: "Laptop WiFi Info"
        unique_id: 5fd0c1b8-59b1-578d-b098-87c274a4b368
        availability: "{{ available }}"
        state: >-
          {% set iface = ifaces | selectattr('name', 'eq', 'Wi-Fi') | first | default(none) %}{{ iface.name | default('Unknown') if iface else 'Unknown' }}
        icon: "mdi:ethernet"
        attributes:
          name: "{% set iface = ifaces | selectattr('name', 'eq', 'Wi-Fi') | first | default(none) %}{{ iface.name | default('Unknown') if iface else 'Unknown' }}"
          mac: "{% set iface = ifaces | selectattr('name', 'eq', 'Wi-Fi') | first | default(none) %}{{ iface.mac | default('Unknown') if iface else 'Unknown' }}"
          ipv4: "{% set iface = ifaces | selectattr('name', 'eq', 'Wi-Fi') | first | default(none) %}{{ iface.ipv4 | default('None') if iface else 'None' }}"
          ipv6: "{% set iface = ifaces | selectattr('name', 'eq', 'Wi-Fi') | first | default(none) %}{{ iface.ipv6 | default('None') if iface else 'None' }}"
          link_speed_mbps: "{% set iface = ifaces | selectattr('name', 'eq', 'Wi-Fi') | first | default(none) %}{{ iface.link_speed_mbps | default(0) if iface else 0 }}"
          duplex: "{% set iface = ifaces | selectattr('name', 'eq', 'Wi-Fi') | first | default(none) %}{{ iface.duplex | default('unknown') if iface else 'unknown' }}"
          mtu: "{% set iface = ifaces | selectattr('name', 'eq', 'Wi-Fi') | first | default(none) %}{{ iface.mtu | default(0) if iface else 0 }}"
          carrier: "{% set iface = ifaces | selectattr('name', 'eq', 'Wi-Fi') | first | default(none) %}{{ iface.carrier | default(false) if iface else false }}"
          driver: "{% set iface = ifaces | selectattr('name', 'eq', 'Wi-Fi') | first | default(none) %}{{ iface.driver | default('Unknown') if iface else 'Unknown' }}"
      - name: "Laptop Tailscale Upload"
        unique_id: 31de8354-36d5-5c4f-af71-b4309697dd36
        availability: "{{ available }}"
        state: >-
          {% set iface = ifaces | selectattr('name', 'eq', 'Tailscale') | first | default(none) %}{{ (iface.rate_up_bps | default(0)) | round(0) if iface else 0 }}
        icon: "mdi:upload"
        unit_of_measurement: "B/s"
        device_class: data_rate
      - name: "Laptop Tailscale Download"
        unique_id: 21be7ef6-59e5-5ba6-b493-cb5579436e37
        availability: "{{ available }}"
        state: >-
          {% set iface = ifaces | selectattr('name', 'eq', 'Tailscale') | first | default(none) %}{{ (iface.rate_down_bps | default(0)) | round(0) if iface else 0 }}
        icon: "mdi:download"
        unit_of_measurement: "B/s"
        device_class: data_rate
      - name: "Laptop Tailscale IPv4"
        unique_id: 6c989678-548d-552e-ae05-c9bfb90bf94b
        availability: "{{ available }}"
        state: >-
          {% set iface = ifaces | selectattr('name', 'eq', 'Tailscale') | first | default(none) %}{{ iface.ipv4 | default('unknown') if iface else 'unknown' }}
      - name: "Laptop Tailscale Info"
        unique_id: f7ea155e-3b53-5ebb-8700-ce1c2b411024
        availability: "{{ available }}"
        state: >-
          {% set iface = ifaces | selectattr('name', 'eq', 'Tailscale') | first | default(none) %}{{ iface.name | default('Unknown') if iface else 'Unknown' }}
        icon: "mdi:ethernet"
        attributes:
          name: "{% set iface = ifaces | selectattr('name', 'eq', 'Tailscale') | first | default(none) %}{{ iface.name | default('Unknown') if iface else 'Unknown' }}"
          mac: "{% set iface = ifaces | selectattr('name', 'eq', 'Tailscale') | first | default(none) %}{{ iface.mac | default('Unknown') if iface else 'Unknown' }}"
          ipv4: "{% set iface = ifaces | selectattr('name', 'eq', 'Tailscale') | first | default(none) %}{{ iface.ipv4 | default('None') if iface else 'None' }}"
          ipv6: "{% set iface = ifaces | selectattr('name', 'eq', 'Tailscale') | first | default(none) %}{{ iface.ipv6 | default('None') if iface else 'None' }}"
          link_speed_mbps: "{% set iface = ifaces | selectattr('name', 'eq', 'Tailscale') | first | default(none) %}{{ iface.link_speed_mbps | default(0) if iface else 0 }}"
          duplex: "{% set iface = ifaces | selectattr('name', 'eq', 'Tailscale') | first | default(none) %}{{ iface.duplex | default('unknown') if iface else 'unknown' }}"
          mtu: "{% set iface = ifaces | selectattr('name', 'eq', 'Tailscale') | first | default(none) %}{{ iface.mtu | default(0) if iface else 0 }}"
          carrier: "{% set iface = ifaces | selectattr('name', 'eq', 'Tailscale') | first | default(none) %}{{ iface.carrier | default(false) if iface else false }}"
          driver: "{% set iface = ifaces | selectattr('name', 'eq', 'Tailscale') | first | default(none) %}{{ iface.driver | default('Unknown') if iface else 'Unknown' }}"
      - name: "Laptop GPU Utilization"
        unique_id: 3b77fda4-27fe-5dbd-a0d0-0ee620f22ed5
        availability: "{{ available }}"
        state: >-
          {{ (gpus[0].core.load_pct | default(0)) | round(1) if gpus else 0 }}
        icon: "mdi:gpu"
        unit_of_measurement: "%"
        state_class: measurement
      - name: "Laptop Battery Level"
        unique_id: f2e30c12-d13d-56a4-a2e4-2768dc98c79e
        availability: "{{ available }}"
        state: >-
          {{ (batteries[0].charge_level_pct | default(0)) | round(0) | int if batteries else 0 }}
        icon: "mdi:battery"
        unit_of_measurement: "%"
        state_class: measurement
        device_class: battery
      - name: "Laptop Battery Status"
        unique_id: b2742eef-2dcd-5d03-ba8f-445334624879
        availability: "{{ available }}"
        state: >-
          {{ 'Discharging' if batteries[0].discharging else 'Charging' if batteries else 'Unknown' }}
        icon: "mdi:battery-charging"
      - name: "Laptop Battery Power"
        unique_id: 9d1d0d78-78de-5df2-9f1d-a20f5fbc7ac0
        availability: "{{ available }}"
        state: >-
          {{ (batteries[0].power_w | default(0)) | round(1) if batteries else 0 }}
        icon: "mdi:flash"
        unit_of_measurement: "W"
        state_class: measurement
        device_class: power
      - name: "Laptop Battery Voltage"
        unique_id: b868c559-168c-5aa9-86bc-36817ac19395
        availability: "{{ available }}"
        state: >-
          {{ (batteries[0].voltage_v | default(0)) | round(2) if batteries else 0 }}
        icon: "mdi:flash"
        unit_of_measurement: "V"
        state_class: measurement
        device_class: voltage
      - name: "Laptop Battery Degradation"
        unique_id: 17cc0100-eebe-5f35-a963-d951ad846f6d
        availability: "{{ available }}"
        state: >-
          {{ (batteries[0].degradation_pct | default(0)) | round(1) if batteries else 0 }}
        icon: "mdi:battery-alert"
        unit_of_measurement: "%"
        state_class: measurement
      - name: "Laptop Battery Remaining Capacity"
        unique_id: f964e60e-5c49-590f-903a-e4956bc7d4a2
        availability: "{{ available }}"
        state: >-
          {{ ((batteries[0].remain_cap_mwh | default(0)) / 1000) | round(1) if batteries else 0 }}
        icon: "mdi:battery"
        unit_of_measurement: "Wh"
        device_class: energy
      - name: "Laptop Battery Full Capacity"
        unique_id: f8e4c2ea-8d9c-5856-9202-bb966bf5a1ca
        availability: "{{ available }}"
        state: >-
          {{ ((batteries[0].full_cap_mwh | default(0)) / 1000) | round(1) if batteries else 0 }}
        icon: "mdi:battery"
        unit_of_measurement: "Wh"
        device_class: energy
      - name: "Laptop NVMe SSD Temp"
        unique_id: bae8de25-076f-5d48-9ad0-9824f4fa9821
        availability: "{{ available }}"
        state: >-
          {% set d = drives | selectattr('name', 'eq', 'PhysicalDrive0') | first | default(none) %}{{ (d.temp_c | default(0)) | round(0) | int if d else 0 }}
        unit_of_measurement: "°C"
        state_class: measurement
        device_class: temperature
      - name: "Laptop NVMe SSD Health"
        unique_id: d8d656a4-a7fb-5736-a5e9-91103e9de098
        availability: "{{ available }}"
        state: >-
          {% set d = drives | selectattr('name', 'eq', 'PhysicalDrive0') | first | default(none) %}{{ d.get('smart', {}).get('overall_health', 'Unknown') if d else 'Unknown' }}
        icon: "mdi:harddisk"
      - name: "Laptop NVMe SSD Wear Level"
        unique_id: d4f78570-9ba2-57c1-a2bc-16ddc816f6e2
        availability: "{{ available }}"
        state: >-
          {% set d = drives | selectattr('name', 'eq', 'PhysicalDrive0') | first | default(none) %}{{ (100 - d.get('smart', {}).get('percentage_used_pct', 0)) | int if d else 100 }}
        icon: "mdi:disc"
        unit_of_measurement: "%"
        state_class: measurement
      - name: "Laptop WiFi Network"
        unique_id: 0d658bcb-3ff4-5c4a-86f5-cfe99f40f16a
        availability: "{{ available }}"
        state: >-
          {% set iface = ifaces | selectattr('name', 'eq', 'Wi-Fi') | first | default(none) %}{{ iface.wifi.essid | default('Not Connected') if iface and iface.wifi else 'Not Connected' }}
        icon: "mdi:wifi"
      - name: "Laptop WiFi Signal"
        unique_id: 3533caa9-1a06-5c99-a28c-e7584c78eb10
        availability: "{{ available }}"
        state: >-
          {% set iface = ifaces | selectattr('name', 'eq', 'Wi-Fi') | first | default(none) %}{{ (iface.wifi.signal_pct | default(0)) | int if iface and iface.wifi else 0 }}
        icon: "mdi:wifi"
        unit_of_measurement: "%"
        state_class: measurement
      - name: "Laptop WiFi Signal dBm"
        unique_id: 58dcee34-6e34-59ed-855f-eb16547fa118
        availability: "{{ available }}"
        state: >-
          {% set iface = ifaces | selectattr('name', 'eq', 'Wi-Fi') | first | default(none) %}{{ (iface.wifi.signal_dbm | default(-100)) | int if iface and iface.wifi else -100 }}
        icon: "mdi:wifi"
        unit_of_measurement: "dBm"
        state_class: measurement
        device_class: signal_strength
      - name: "Laptop WiFi Channel"
        unique_id: 9d22e15e-01c4-536a-a661-cc975ba01f9a
        availability: "{{ available }}"
        state: >-
          {% set iface = ifaces | selectattr('name', 'eq', 'Wi-Fi') | first | default(none) %}{{ (iface.wifi.channel | default(0)) | int if iface and iface.wifi else 0 }}
        icon: "mdi:wifi"
        state_class: measurement
      - name: "Laptop WiFi Bitrate"
        unique_id: 81593835-afd2-5159-97ea-ccf889d6de91
        availability: "{{ available }}"
        state: >-
          {% set iface = ifaces | selectattr('name', 'eq', 'Wi-Fi') | first | default(none) %}{{ (iface.wifi.bitrate_mbps | default(0)) | round(0) | int if iface and iface.wifi else 0 }}
        icon: "mdi:speedometer"
        unit_of_measurement: "Mbit/s"
        state_class: measurement
        device_class: data_rate
    binary_sensor:
      - name: "Laptop Overall Status OK"
        unique_id: 06b933b8-7252-5681-a6e3-894eccf1f55b
        availability: "{{ available }}"
        state: "{{ health.overall_ok | default(false) }}"
        icon: "mdi:check-circle"
      - name: "Laptop Status Alarm"
        unique_id: 864f33e8-fa3e-5be3-9432-d60dba0d1475
        availability: "{{ available }}"
        state: "{{ not (health.overall_ok | default(true)) }}"
        device_class: problem
      - name: "Laptop Services OK"
        unique_id: da6c26c1-7dd0-557f-b9b2-406620a0e7fc
        availability: "{{ available }}"
        state: >-
          {% set tracked = ['Dnscache', 'LanmanServer', 'LibreHardwareMonitor', 'Spooler'] %}{% set svc_list = health.services | default([]) %}{{ svc_list | selectattr('name', 'in', tracked) | selectattr('ok', 'eq', false) | list | length == 0 }}
        icon: "mdi:check-circle"
      - name: "Laptop Display Services"
        unique_id: babaa5ce-9ba2-577e-89c6-7c2b1f2f20e7
        availability: "{{ available }}"
        state: >-
          {% set tracked = ['Dnscache', 'LanmanServer', 'LibreHardwareMonitor', 'Spooler'] %}{% set svc_list = health.services | default([]) %}{{ svc_list | selectattr('name', 'in', tracked) | selectattr('ok', 'eq', false) | list | length > 0 }}
        icon: "mdi:cog-off"
      - name: "Laptop Service Dnscache"
        unique_id: b60087e8-0d81-5ba5-b272-e3a88bcbb00f
        availability: "{{ available }}"
        state: >-
          {% set svc = health.services | default([]) | selectattr('name', 'eq', 'Dnscache') | first | default(none) %}{{ svc.ok | default(false) if svc else false }}
        icon: "mdi:cog"
      - name: "Laptop Service LanmanServer"
        unique_id: cc3c1f6b-d4db-57c0-b667-56d6a2f65b78
        availability: "{{ available }}"
        state: >-
          {% set svc = health.services | default([]) | selectattr('name', 'eq', 'LanmanServer') | first | default(none) %}{{ svc.ok | default(false) if svc else false }}
        icon: "mdi:cog"
      - name: "Laptop Service LibreHardwareMonitor"
        unique_id: 64baeae6-1fb2-5807-a4cc-966bce65db36
        availability: "{{ available }}"
        state: >-
          {% set svc = health.services | default([]) | selectattr('name', 'eq', 'LibreHardwareMonitor') | first | default(none) %}{{ svc.ok | default(false) if svc else false }}
        icon: "mdi:cog"
      - name: "Laptop Service Spooler"
        unique_id: 8415f187-adec-51f0-a4f0-8f478096f316
        availability: "{{ available }}"
        state: >-
          {% set svc = health.services | default([]) | selectattr('name', 'eq', 'Spooler') | first | default(none) %}{{ svc.ok | default(false) if svc else false }}
        icon: "mdi:cog"
      - name: "ui_laptop_services"
        unique_id: 10cebeee-ad1f-5473-9bce-c7415ff13d97
        state: >-
          {{ health.services | default([]) | selectattr('ok', 'eq', false) | list | length == 0 }}
        attributes:
          libre_hardware_monitor: "{{ {'icon': 'mdi:cog' if (health.services | default([]) | selectattr('name', 'search', '(?i)libre_hardware_monitor') | selectattr('ok', 'eq', true) | list | length > 0) else 'mdi:cog-off', 'icon_color': 'green' if (health.services | default([]) | selectattr('name', 'search', '(?i)libre_hardware_monitor') | selectattr('ok', 'eq', true) | list | length > 0) else 'red', 'content': 'Libre Hardware Monitor'} }}"
          dnscache: "{{ {'icon': 'mdi:cog' if (health.services | default([]) | selectattr('name', 'search', '(?i)dnscache') | selectattr('ok', 'eq', true) | list | length > 0) else 'mdi:cog-off', 'icon_color': 'green' if (health.services | default([]) | selectattr('name', 'search', '(?i)dnscache') | selectattr('ok', 'eq', true) | list | length > 0) else 'red', 'content': 'Dnscache'} }}"
          lanman_server: "{{ {'icon': 'mdi:cog' if (health.services | default([]) | selectattr('name', 'search', '(?i)lanman_server') | selectattr('ok', 'eq', true) | list | length > 0) else 'mdi:cog-off', 'icon_color': 'green' if (health.services | default([]) | selectattr('name', 'search', '(?i)lanman_server') | selectattr('ok', 'eq', true) | list | length > 0) else 'red', 'content': 'Lanman Server'} }}"
          spooler: "{{ {'icon': 'mdi:cog' if (health.services | default([]) | selectattr('name', 'search', '(?i)spooler') | selectattr('ok', 'eq', true) | list | length > 0) else 'mdi:cog-off', 'icon_color': 'green' if (health.services | default([]) | selectattr('name', 'search', '(?i)spooler') | selectattr('ok', 'eq', true) | list | length > 0) else 'red', 'content': 'Spooler'} }}"

  # ---------------------------------------------------------------------------
  # UI Sensors: Dashboard Display
  # ---------------------------------------------------------------------------
  - trigger:
      - platform: homeassistant
        event: start
      - platform: state
        entity_id: sensor.laptop_cpu_utilization
    variables:
      entity_id: sensor.laptop_cpu_utilization
      app: cpu_utilization
      options: {}
      state_info: >-
        {%- from 'main.jinja' import get_device_state_info -%}
        {{ get_device_state_info(entity_id, app, options=options) | from_json }}
      state_info_vertical: >-
        {%- from 'main.jinja' import get_device_state_info -%}
        {{ get_device_state_info(entity_id, app, layout='vertical', options=options) | from_json }}
    sensor:
      - name: ui_laptop_cpu_utilization
        unique_id: a17e463a-e9e8-509b-a3cc-a13ec6609d9e
        state: "{{ states(entity_id) }}"
        attributes:
          value: "{{ state_info.value }}"
          name: "{{ state_info.name }}"
          default_desc: "{{ state_info.default_desc }}"
          short_desc: "{{ state_info.short_desc }}"
          full_desc: "{{ state_info.full_desc }}"
          icon: "{{ state_info.icon }}"
          color: "{{ state_info.color }}"
          badge: "{{ state_info.badge }}"
          badge_color: "{{ state_info.badge_color }}"
          label: "{{ state_info.label }}"
          short_label: "{{ state_info.short_label }}"
          long_label: "{{ state_info.long_label }}"
          label_vertical: "{{ state_info_vertical.label }}"
          short_label_vertical: "{{ state_info_vertical.short_label }}"
          long_label_vertical: "{{ state_info_vertical.long_label }}"
          app: cpu_utilization
          device_class: none
  - trigger:
      - platform: homeassistant
        event: start
      - platform: state
        entity_id:
          - sensor.laptop_cpu_utilization
          - sensor.laptop_cpu_load_1m
    variables:
      entity_id: sensor.laptop_cpu_utilization
      app: cpu_utilization
      options:
        load: sensor.laptop_cpu_load_1m
      state_info: >-
        {%- from 'main.jinja' import get_device_state_info -%}
        {{ get_device_state_info(entity_id, app, options=options) | from_json }}
      state_info_vertical: >-
        {%- from 'main.jinja' import get_device_state_info -%}
        {{ get_device_state_info(entity_id, app, layout='vertical', options=options) | from_json }}
    sensor:
      - name: ui_laptop_cpu_utilization_load
        unique_id: 286dff64-3d59-5fe7-92eb-c224af7d4295
        state: "{{ states(entity_id) }}"
        attributes:
          value: "{{ state_info.value }}"
          name: "{{ state_info.name }}"
          default_desc: "{{ state_info.default_desc }}"
          short_desc: "{{ state_info.short_desc }}"
          full_desc: "{{ state_info.full_desc }}"
          icon: "{{ state_info.icon }}"
          color: "{{ state_info.color }}"
          badge: "{{ state_info.badge }}"
          badge_color: "{{ state_info.badge_color }}"
          label: "{{ state_info.label }}"
          short_label: "{{ state_info.short_label }}"
          long_label: "{{ state_info.long_label }}"
          label_vertical: "{{ state_info_vertical.label }}"
          short_label_vertical: "{{ state_info_vertical.short_label }}"
          long_label_vertical: "{{ state_info_vertical.long_label }}"
          app: cpu_utilization
          device_class: none
  - trigger:
      - platform: homeassistant
        event: start
      - platform: state
        entity_id:
          - sensor.laptop_cpu_load_1m
          - sensor.laptop_cpu_load_5m
          - sensor.laptop_cpu_load_15m
          - sensor.laptop_cpu_logical_cores
    variables:
      entity_id: sensor.laptop_cpu_load_1m
      app: cpu_load
      options:
        cores: sensor.laptop_cpu_logical_cores
      state_info: >-
        {%- from 'main.jinja' import get_device_state_info -%}
        {{ get_device_state_info(entity_id, app, options=options) | from_json }}
      state_info_vertical: >-
        {%- from 'main.jinja' import get_device_state_info -%}
        {{ get_device_state_info(entity_id, app, layout='vertical', options=options) | from_json }}
    sensor:
      - name: ui_laptop_cpu_load_1m
        unique_id: 79339ca3-b2b6-5fb2-ae7e-2ef406876f96
        state: "{{ states(entity_id) }}"
        attributes:
          value: "{{ state_info.value }}"
          name: "{{ state_info.name }}"
          default_desc: "{{ state_info.default_desc }}"
          short_desc: "{{ state_info.short_desc }}"
          full_desc: "{{ state_info.full_desc }}"
          icon: "{{ state_info.icon }}"
          color: "{{ state_info.color }}"
          badge: "{{ state_info.badge }}"
          badge_color: "{{ state_info.badge_color }}"
          label: "{{ state_info.label }}"
          short_label: "{{ state_info.short_label }}"
          long_label: "{{ state_info.long_label }}"
          label_vertical: "{{ state_info_vertical.label }}"
          short_label_vertical: "{{ state_info_vertical.short_label }}"
          long_label_vertical: "{{ state_info_vertical.long_label }}"
          app: cpu_load
          device_class: none
          long_desc: >-
            {%- set m1 = states('sensor.laptop_cpu_load_1m')|float(0) -%}{%- set m5 = states('sensor.laptop_cpu_load_5m')|float(0) -%}{%- set m15 = states('sensor.laptop_cpu_load_15m')|float(0) -%}{%- set c = states('sensor.laptop_cpu_logical_cores')|int(0) -%}{{- '1m: ' ~ m1 ~ ' • 5m: ' ~ m5 ~ ' • 15m: ' ~ m15 ~ ' • ' ~ c ~ ' cores' -}}
  - trigger:
      - platform: homeassistant
        event: start
      - platform: state
        entity_id:
          - sensor.laptop_memory_percent
          - sensor.laptop_memory_used
          - sensor.laptop_memory_total
          - sensor.laptop_memory_available
    variables:
      entity_id: sensor.laptop_memory_percent
      app: memory_utilization
      options:
        used: sensor.laptop_memory_used
      state_info: >-
        {%- from 'main.jinja' import get_device_state_info -%}
        {{ get_device_state_info(entity_id, app, options=options) | from_json }}
      state_info_vertical: >-
        {%- from 'main.jinja' import get_device_state_info -%}
        {{ get_device_state_info(entity_id, app, layout='vertical', options=options) | from_json }}
    sensor:
      - name: ui_laptop_memory_percent
        unique_id: 0cb082fb-202b-547d-8701-9d6b3c1a86db
        state: "{{ states(entity_id) }}"
        attributes:
          value: "{{ state_info.value }}"
          name: "{{ state_info.name }}"
          default_desc: "{{ state_info.default_desc }}"
          short_desc: "{{ state_info.short_desc }}"
          full_desc: "{{ state_info.full_desc }}"
          icon: "{{ state_info.icon }}"
          color: "{{ state_info.color }}"
          badge: "{{ state_info.badge }}"
          badge_color: "{{ state_info.badge_color }}"
          label: "{{ state_info.label }}"
          short_label: "{{ state_info.short_label }}"
          long_label: "{{ state_info.long_label }}"
          label_vertical: "{{ state_info_vertical.label }}"
          short_label_vertical: "{{ state_info_vertical.short_label }}"
          long_label_vertical: "{{ state_info_vertical.long_label }}"
          app: memory_utilization
          device_class: none
          long_desc: >-
            {%- from 'units/base.jinja' import u_humanize_entity -%}{%- set mem_used = u_humanize_entity('sensor.laptop_memory_used') -%}{%- set mem_total = u_humanize_entity('sensor.laptop_memory_total') -%}{%- set mem_free = u_humanize_entity('sensor.laptop_memory_available') -%}{{- 'Used: ' ~ mem_used ~ ' / ' ~ mem_total ~ '\nFree: ' ~ mem_free -}}
  - trigger:
      - platform: homeassistant
        event: start
      - platform: state
        entity_id: sensor.laptop_uptime_s
    variables:
      entity_id: sensor.laptop_uptime_s
      app: uptime
      options:
        device_class: duration
        uom: s
      state_info: >-
        {%- from 'main.jinja' import get_device_state_info -%}
        {{ get_device_state_info(entity_id, app, options=options) | from_json }}
      state_info_vertical: >-
        {%- from 'main.jinja' import get_device_state_info -%}
        {{ get_device_state_info(entity_id, app, layout='vertical', options=options) | from_json }}
    sensor:
      - name: ui_laptop_uptime_s
        unique_id: 8b848081-7b5f-5ff9-b3c7-39dce112d3f5
        state: "{{ states(entity_id) }}"
        attributes:
          value: "{{ state_info.value }}"
          name: "{{ state_info.name }}"
          default_desc: "{{ state_info.default_desc }}"
          short_desc: "{{ state_info.short_desc }}"
          full_desc: "{{ state_info.full_desc }}"
          icon: "{{ state_info.icon }}"
          color: "{{ state_info.color }}"
          badge: "{{ state_info.badge }}"
          badge_color: "{{ state_info.badge_color }}"
          label: "{{ state_info.label }}"
          short_label: "{{ state_info.short_label }}"
          long_label: "{{ state_info.long_label }}"
          label_vertical: "{{ state_info_vertical.label }}"
          short_label_vertical: "{{ state_info_vertical.short_label }}"
          long_label_vertical: "{{ state_info_vertical.long_label }}"
          app: uptime
          device_class: duration
  - trigger:
      - platform: homeassistant
        event: start
      - platform: state
        entity_id: sensor.laptop_updates_pending
    variables:
      entity_id: sensor.laptop_updates_pending
      app: count_pending
      options: {}
      state_info: >-
        {%- from 'main.jinja' import get_device_state_info -%}
        {{ get_device_state_info(entity_id, app, options=options) | from_json }}
      state_info_vertical: >-
        {%- from 'main.jinja' import get_device_state_info -%}
        {{ get_device_state_info(entity_id, app, layout='vertical', options=options) | from_json }}
    sensor:
      - name: ui_laptop_updates_pending
        unique_id: dfdcc451-ce40-5bec-897f-5f470090b2ba
        state: "{{ states(entity_id) }}"
        attributes:
          value: "{{ state_info.value }}"
          name: "{{ state_info.name }}"
          default_desc: "{{ state_info.default_desc }}"
          short_desc: "{{ state_info.short_desc }}"
          full_desc: "{{ state_info.full_desc }}"
          icon: "{{ state_info.icon }}"
          color: "{{ state_info.color }}"
          badge: "{{ state_info.badge }}"
          badge_color: "{{ state_info.badge_color }}"
          label: "{{ state_info.label }}"
          short_label: "{{ state_info.short_label }}"
          long_label: "{{ state_info.long_label }}"
          label_vertical: "{{ state_info_vertical.label }}"
          short_label_vertical: "{{ state_info_vertical.short_label }}"
          long_label_vertical: "{{ state_info_vertical.long_label }}"
          app: count_pending
          device_class: none
  - trigger:
      - platform: homeassistant
        event: start
      - platform: state
        entity_id:
          - binary_sensor.laptop_status_alarm
          - sensor.laptop_overall_status_summary
    variables:
      entity_id: binary_sensor.laptop_status_alarm
      app: alarm
      options:
        device_class: binary
        summary: sensor.laptop_overall_status_summary
      state_info: >-
        {%- from 'main.jinja' import get_device_state_info -%}
        {{ get_device_state_info(entity_id, app, options=options) | from_json }}
      state_info_vertical: >-
        {%- from 'main.jinja' import get_device_state_info -%}
        {{ get_device_state_info(entity_id, app, layout='vertical', options=options) | from_json }}
    sensor:
      - name: ui_laptop_status_alarm
        unique_id: 97356d2c-7572-5418-9071-04bc6cd74ae7
        state: "{{ states(entity_id) }}"
        attributes:
          value: "{{ state_info.value }}"
          name: "{{ state_info.name }}"
          default_desc: "{{ state_info.default_desc }}"
          short_desc: "{{ state_info.short_desc }}"
          full_desc: "{{ state_info.full_desc }}"
          icon: "{{ state_info.icon }}"
          color: "{{ state_info.color }}"
          badge: "{{ state_info.badge }}"
          badge_color: "{{ state_info.badge_color }}"
          label: "{{ state_info.label }}"
          short_label: "{{ state_info.short_label }}"
          long_label: "{{ state_info.long_label }}"
          label_vertical: "{{ state_info_vertical.label }}"
          short_label_vertical: "{{ state_info_vertical.short_label }}"
          long_label_vertical: "{{ state_info_vertical.long_label }}"
          app: alarm
          device_class: binary
  - trigger:
      - platform: homeassistant
        event: start
      - platform: state
        entity_id: sensor.laptop_cpu_temp
    variables:
      entity_id: sensor.laptop_cpu_temp
      app: cpu
      options:
        device_class: temperature
      state_info: >-
        {%- from 'main.jinja' import get_device_state_info -%}
        {{ get_device_state_info(entity_id, app, options=options) | from_json }}
      state_info_vertical: >-
        {%- from 'main.jinja' import get_device_state_info -%}
        {{ get_device_state_info(entity_id, app, layout='vertical', options=options) | from_json }}
    sensor:
      - name: ui_laptop_cpu_temp
        unique_id: 595cc26c-5739-5ac1-bdb1-aadc22d09886
        state: "{{ states(entity_id) }}"
        attributes:
          value: "{{ state_info.value }}"
          name: "{{ state_info.name }}"
          default_desc: "{{ state_info.default_desc }}"
          short_desc: "{{ state_info.short_desc }}"
          full_desc: "{{ state_info.full_desc }}"
          icon: "{{ state_info.icon }}"
          color: "{{ state_info.color }}"
          badge: "{{ state_info.badge }}"
          badge_color: "{{ state_info.badge_color }}"
          label: "{{ state_info.label }}"
          short_label: "{{ state_info.short_label }}"
          long_label: "{{ state_info.long_label }}"
          label_vertical: "{{ state_info_vertical.label }}"
          short_label_vertical: "{{ state_info_vertical.short_label }}"
          long_label_vertical: "{{ state_info_vertical.long_label }}"
          app: cpu
          device_class: temperature
  - trigger:
      - platform: homeassistant
        event: start
      - platform: state
        entity_id:
          - sensor.laptop_fs_windows_c_percent
          - sensor.laptop_fs_windows_c_used
          - sensor.laptop_fs_windows_c_total
    variables:
      entity_id: sensor.laptop_fs_windows_c_percent
      app: disk_utilization
      options:
        free: sensor.laptop_fs_windows_c_free
      state_info: >-
        {%- from 'main.jinja' import get_device_state_info -%}
        {{ get_device_state_info(entity_id, app, options=options) | from_json }}
      state_info_vertical: >-
        {%- from 'main.jinja' import get_device_state_info -%}
        {{ get_device_state_info(entity_id, app, layout='vertical', options=options) | from_json }}
    sensor:
      - name: ui_laptop_fs_windows_c_percent
        unique_id: 668bf1ed-8866-5b9d-9879-686827fb0862
        state: "{{ states(entity_id) }}"
        attributes:
          value: "{{ state_info.value }}"
          name: "{{ state_info.name }}"
          default_desc: "{{ state_info.default_desc }}"
          short_desc: "{{ state_info.short_desc }}"
          full_desc: "{{ state_info.full_desc }}"
          icon: "{{ state_info.icon }}"
          color: "{{ state_info.color }}"
          badge: "{{ state_info.badge }}"
          badge_color: "{{ state_info.badge_color }}"
          label: "{{ state_info.label }}"
          short_label: "{{ state_info.short_label }}"
          long_label: "{{ state_info.long_label }}"
          label_vertical: "{{ state_info_vertical.label }}"
          short_label_vertical: "{{ state_info_vertical.short_label }}"
          long_label_vertical: "{{ state_info_vertical.long_label }}"
          app: disk_utilization
          device_class: none
          long_desc: >-
            {%- from 'units/base.jinja' import u_humanize_entity -%}{%- set total = u_humanize_entity('sensor.laptop_fs_windows_c_total') -%}{%- set used = u_humanize_entity('sensor.laptop_fs_windows_c_used') -%}{{- 'Total: ' ~ total ~ ' • Used: ' ~ used -}}
  - trigger:
      - platform: homeassistant
        event: start
      - platform: state
        entity_id: sensor.laptop_gpu_utilization
    variables:
      entity_id: sensor.laptop_gpu_utilization
      app: cpu_utilization
      options: {}
      state_info: >-
        {%- from 'main.jinja' import get_device_state_info -%}
        {{ get_device_state_info(entity_id, app, options=options) | from_json }}
      state_info_vertical: >-
        {%- from 'main.jinja' import get_device_state_info -%}
        {{ get_device_state_info(entity_id, app, layout='vertical', options=options) | from_json }}
    sensor:
      - name: ui_laptop_gpu_utilization
        unique_id: 1dc7a285-77fd-5878-8999-57508f610684
        state: "{{ states(entity_id) }}"
        attributes:
          value: "{{ state_info.value }}"
          name: "{{ state_info.name }}"
          default_desc: "{{ state_info.default_desc }}"
          short_desc: "{{ state_info.short_desc }}"
          full_desc: "{{ state_info.full_desc }}"
          icon: "{{ state_info.icon }}"
          color: "{{ state_info.color }}"
          badge: "{{ state_info.badge }}"
          badge_color: "{{ state_info.badge_color }}"
          label: "{{ state_info.label }}"
          short_label: "{{ state_info.short_label }}"
          long_label: "{{ state_info.long_label }}"
          label_vertical: "{{ state_info_vertical.label }}"
          short_label_vertical: "{{ state_info_vertical.short_label }}"
          long_label_vertical: "{{ state_info_vertical.long_label }}"
          app: cpu_utilization
          device_class: none
