# =============================================================================
# Package: RV Hass Hardware Monitor
# =============================================================================
#
# Auto-generated by generate_hwmon_assets.py
# DO NOT EDIT - Changes will be overwritten
#
# Monitors RV Hass system telemetry via MQTT and provides:
#   - Data sensors for CPU, memory, disk, network, etc.
#   - UI sensors for dashboard card attributes
#   - Binary sensors for health status
#
# Source telemetry sensor: sensor.a0d7b954_ssh_a0d7b954_ssh_hardware_telemetry
#
# =============================================================================

template:
  # ---------------------------------------------------------------------------
  # Data Sensors: RV Hass
  # ---------------------------------------------------------------------------
  - trigger:
      - platform: homeassistant
        event: start
      - platform: state
        entity_id: sensor.a0d7b954_ssh_a0d7b954_ssh_hardware_telemetry
    variables:
      entity_id: sensor.a0d7b954_ssh_a0d7b954_ssh_hardware_telemetry
      available: "{{ has_value(entity_id) }}"
      host: "{{ state_attr(entity_id, 'host') or {} }}"
      health: "{{ state_attr(entity_id, 'health') or {} }}"
      cpus: "{{ state_attr(entity_id, 'cpus') or [] }}"
      memory: "{{ state_attr(entity_id, 'memory') or {} }}"
      filesystems: "{{ state_attr(entity_id, 'filesystems') or [] }}"
      ifaces: "{{ state_attr(entity_id, 'ifaces') or [] }}"
      gpus: "{{ state_attr(entity_id, 'gpus') or [] }}"
      tpus: "{{ state_attr(entity_id, 'tpus') or [] }}"
    sensor:
      - name: "RV Hass Uptime (s)"
        unique_id: 5789b7f2-2b5d-57f3-9b62-090d12bfed32
        availability: "{{ available }}"
        state: "{{ (host.uptime_s | default(0)) | int }}"
        icon: "mdi:timer-outline"
        unit_of_measurement: "s"
        state_class: measurement
        device_class: duration
      - name: "RV Hass Process Count"
        unique_id: cb26786b-71ce-5775-b4cd-ef85164588d1
        availability: "{{ available }}"
        state: "{{ (host.process_count | default(0)) | int }}"
        icon: "mdi:application"
        state_class: measurement
      - name: "RV Hass Overall Status Summary"
        unique_id: 0f252e41-34c3-5afe-b62a-a7f93397c845
        availability: "{{ available }}"
        state: >-
          {{ (health.issues[0][:250] ~ '...') if health.issues and health.issues[0]|length > 250 else (health.issues[0] if health.issues else 'OK') }}
        icon: "mdi:card-text-outline"
      - name: "RV Hass Updates Pending"
        unique_id: 73e46d42-1ed9-5935-a647-d930463e175d
        availability: "{{ available }}"
        state: >-
          {{ ((health.updates | default({})).pending | default(0)) | int }}
        icon: "mdi:update"
        state_class: measurement
      - name: "RV Hass Updates List"
        unique_id: b9a1b26c-21bb-585b-b112-11d5b38e5295
        availability: "{{ available }}"
        state: >-
          {{ ((health.updates | default({})).get('items', []) | map(attribute='name') | list | length) }} packages
        icon: "mdi:package-variant"
        attributes:
          packages: "{{ (health.updates | default({})).get('items', []) | map(attribute='name') | list }}"
      - name: "RV Hass Services Checked"
        unique_id: d614b19e-f934-553d-8786-a7ecfd500230
        availability: "{{ available }}"
        state: "{{ (health.services | default([]) | length) }}"
        icon: "mdi:playlist-check"
        state_class: measurement
      - name: "RV Hass Containers Checked"
        unique_id: 9eb693a1-6ab4-501c-aaa1-322aaf0bba6b
        availability: "{{ available }}"
        state: "{{ (health.containers | default([]) | length) }}"
        icon: "mdi:docker"
        state_class: measurement
      - name: "RV Hass CPU Utilization"
        unique_id: f8480c95-ead4-53dd-86db-cc637e744ae9
        availability: "{{ available }}"
        state: "{{ (cpus[0].load_pct | default(0)) | round(1) }}"
        icon: "mdi:chip"
        unit_of_measurement: "%"
        state_class: measurement
      - name: "RV Hass CPU Load 1m"
        unique_id: 860a1af1-13c9-5a1e-a0ef-6a1ed64e6b4b
        availability: "{{ available }}"
        state: "{{ (cpus[0].load_1m | default(0)) | round(2) }}"
        icon: "mdi:chip"
        state_class: measurement
      - name: "RV Hass CPU Load 5m"
        unique_id: 2d02f926-d288-5857-90e0-943009625337
        availability: "{{ available }}"
        state: "{{ (cpus[0].load_5m | default(0)) | round(2) }}"
        icon: "mdi:chip"
        state_class: measurement
      - name: "RV Hass CPU Load 15m"
        unique_id: cdbbd470-e2a8-5d57-8658-2ab64132b282
        availability: "{{ available }}"
        state: "{{ (cpus[0].load_15m | default(0)) | round(2) }}"
        icon: "mdi:chip"
        state_class: measurement
      - name: "RV Hass CPU Temp"
        unique_id: 20a857fa-4e78-5c1f-8424-44fc526a4f00
        availability: "{{ available }}"
        state: "{{ (cpus[0].temp_c | default(0)) | round(1) }}"
        unit_of_measurement: "°C"
        state_class: measurement
        device_class: temperature
      - name: "RV Hass CPU Physical Cores"
        unique_id: 4f6e4b65-99a0-546c-a7fd-d6482ebfaec8
        availability: "{{ available }}"
        state: "{{ cpus[0].num_physical_cores | default(0) }}"
        icon: "mdi:chip"
        state_class: measurement
      - name: "RV Hass CPU Logical Cores"
        unique_id: e02dc82d-9c02-5ef1-b8c7-a5b19e5deec3
        availability: "{{ available }}"
        state: "{{ cpus[0].num_logical_cores | default(0) }}"
        icon: "mdi:chip"
        state_class: measurement
      - name: "RV Hass Memory Percent"
        unique_id: 3c411b76-6a54-5cae-8244-5bf2af82306f
        availability: "{{ available }}"
        state: "{{ (memory.system.load_pct | default(0)) | round(1) }}"
        icon: "mdi:memory"
        unit_of_measurement: "%"
        state_class: measurement
      - name: "RV Hass Memory Used"
        unique_id: 3c3439df-1304-55f3-b81a-914f2386266d
        availability: "{{ available }}"
        state: >-
          {{ ((memory.system.used_b | default(0)) / 1073741824) | round(2) }}
        unit_of_measurement: "GiB"
        state_class: measurement
        device_class: data_size
      - name: "RV Hass Memory Available"
        unique_id: f4f1490e-0a2a-5a76-ab09-50cc4d6f3e5c
        availability: "{{ available }}"
        state: >-
          {{ ((memory.system.available_b | default(0)) / 1073741824) | round(2) }}
        unit_of_measurement: "GiB"
        state_class: measurement
        device_class: data_size
      - name: "RV Hass Memory Total"
        unique_id: b9034723-9d16-5d88-9d46-055f0aa59b3d
        availability: "{{ available }}"
        state: >-
          {{ ((memory.system.total_b | default(0)) / 1073741824) | round(2) }}
        unit_of_measurement: "GiB"
        state_class: measurement
        device_class: data_size
      - name: "RV Hass Swap Percent"
        unique_id: 18c41f6f-132f-5155-90a7-e79b5e5e218e
        availability: "{{ available }}"
        state: "{{ (memory.virtual.load_pct | default(0)) | round(1) }}"
        icon: "mdi:memory"
        unit_of_measurement: "%"
        state_class: measurement
      - name: "RV Hass FS Data Percent"
        unique_id: bdf8d96e-7ab2-52b9-b03f-8c33e27b94d1
        availability: "{{ available }}"
        state: >-
          {% set fs = filesystems | selectattr('mountpoint', 'eq', '/data') | first | default(none) %}{{ ((fs.used_b / (fs.used_b + fs.available_b)) * 100) | round(1) if fs else 0 }}
        icon: "mdi:harddisk"
        unit_of_measurement: "%"
        state_class: measurement
      - name: "RV Hass FS Data Used"
        unique_id: 9ffa4273-95d0-5302-8d0e-1d8e9a82b36d
        availability: "{{ available }}"
        state: >-
          {% set fs = filesystems | selectattr('mountpoint', 'eq', '/data') | first | default(none) %}{{ ((fs.used_b | default(0)) / 1073741824) | round(2) if fs else 0 }}
        icon: "mdi:harddisk"
        unit_of_measurement: "GiB"
        state_class: measurement
        device_class: data_size
      - name: "RV Hass FS Data Total"
        unique_id: a98a98b1-0d03-58fe-9102-f4e9de5d909d
        availability: "{{ available }}"
        state: >-
          {% set fs = filesystems | selectattr('mountpoint', 'eq', '/data') | first | default(none) %}{{ (((fs.used_b | default(0)) + (fs.available_b | default(0))) / 1073741824) | round(2) if fs else 0 }}
        icon: "mdi:harddisk"
        unit_of_measurement: "GiB"
        state_class: measurement
        device_class: data_size
      - name: "RV Hass Ethernet Upload"
        unique_id: 75b67cc6-618b-5882-81a8-af2abbfba1ea
        availability: "{{ available }}"
        state: >-
          {% set iface = ifaces | selectattr('name', 'eq', 'end0') | first | default(none) %}{{ (iface.rate_up_bps | default(0)) | round(0) if iface else 0 }}
        icon: "mdi:upload"
        unit_of_measurement: "B/s"
        device_class: data_rate
      - name: "RV Hass Ethernet Download"
        unique_id: 3c33b0f2-d74d-515f-bc0c-8b2528713c38
        availability: "{{ available }}"
        state: >-
          {% set iface = ifaces | selectattr('name', 'eq', 'end0') | first | default(none) %}{{ (iface.rate_down_bps | default(0)) | round(0) if iface else 0 }}
        icon: "mdi:download"
        unit_of_measurement: "B/s"
        device_class: data_rate
      - name: "RV Hass Ethernet IPv4"
        unique_id: 1f42bd5e-01f7-5e38-a627-46978d2b0c77
        availability: "{{ available }}"
        state: >-
          {% set iface = ifaces | selectattr('name', 'eq', 'end0') | first | default(none) %}{{ iface.ipv4 | default('unknown') if iface else 'unknown' }}
      - name: "RV Hass Ethernet Info"
        unique_id: 9e741863-bdfa-5d6e-8427-203327c82557
        availability: "{{ available }}"
        state: >-
          {% set iface = ifaces | selectattr('name', 'eq', 'end0') | first | default(none) %}{{ iface.name | default('Unknown') if iface else 'Unknown' }}
        icon: "mdi:ethernet"
        attributes:
          name: "{% set iface = ifaces | selectattr('name', 'eq', 'end0') | first | default(none) %}{{ iface.name | default('Unknown') if iface else 'Unknown' }}"
          mac: "{% set iface = ifaces | selectattr('name', 'eq', 'end0') | first | default(none) %}{{ iface.mac | default('Unknown') if iface else 'Unknown' }}"
          ipv4: "{% set iface = ifaces | selectattr('name', 'eq', 'end0') | first | default(none) %}{{ iface.ipv4 | default('None') if iface else 'None' }}"
          ipv6: "{% set iface = ifaces | selectattr('name', 'eq', 'end0') | first | default(none) %}{{ iface.ipv6 | default('None') if iface else 'None' }}"
          link_speed_mbps: "{% set iface = ifaces | selectattr('name', 'eq', 'end0') | first | default(none) %}{{ iface.link_speed_mbps | default(0) if iface else 0 }}"
          duplex: "{% set iface = ifaces | selectattr('name', 'eq', 'end0') | first | default(none) %}{{ iface.duplex | default('unknown') if iface else 'unknown' }}"
          mtu: "{% set iface = ifaces | selectattr('name', 'eq', 'end0') | first | default(none) %}{{ iface.mtu | default(0) if iface else 0 }}"
          carrier: "{% set iface = ifaces | selectattr('name', 'eq', 'end0') | first | default(none) %}{{ iface.carrier | default(false) if iface else false }}"
          driver: "{% set iface = ifaces | selectattr('name', 'eq', 'end0') | first | default(none) %}{{ iface.driver | default('Unknown') if iface else 'Unknown' }}"
      - name: "RV Hass Tailscale Upload"
        unique_id: 5d2ffb6f-7eb5-5b2f-93f9-15f77c82e820
        availability: "{{ available }}"
        state: >-
          {% set iface = ifaces | selectattr('name', 'eq', 'tailscale0') | first | default(none) %}{{ (iface.rate_up_bps | default(0)) | round(0) if iface else 0 }}
        icon: "mdi:upload"
        unit_of_measurement: "B/s"
        device_class: data_rate
      - name: "RV Hass Tailscale Download"
        unique_id: ac7d047f-419f-5b06-ac08-bc6e1016bbed
        availability: "{{ available }}"
        state: >-
          {% set iface = ifaces | selectattr('name', 'eq', 'tailscale0') | first | default(none) %}{{ (iface.rate_down_bps | default(0)) | round(0) if iface else 0 }}
        icon: "mdi:download"
        unit_of_measurement: "B/s"
        device_class: data_rate
      - name: "RV Hass Tailscale IPv4"
        unique_id: 19b11707-9ea4-53d5-9cd1-646f147e10a4
        availability: "{{ available }}"
        state: >-
          {% set iface = ifaces | selectattr('name', 'eq', 'tailscale0') | first | default(none) %}{{ iface.ipv4 | default('unknown') if iface else 'unknown' }}
      - name: "RV Hass Tailscale Info"
        unique_id: 737a312e-3354-5f76-8086-812847e83c89
        availability: "{{ available }}"
        state: >-
          {% set iface = ifaces | selectattr('name', 'eq', 'tailscale0') | first | default(none) %}{{ iface.name | default('Unknown') if iface else 'Unknown' }}
        icon: "mdi:ethernet"
        attributes:
          name: "{% set iface = ifaces | selectattr('name', 'eq', 'tailscale0') | first | default(none) %}{{ iface.name | default('Unknown') if iface else 'Unknown' }}"
          mac: "{% set iface = ifaces | selectattr('name', 'eq', 'tailscale0') | first | default(none) %}{{ iface.mac | default('Unknown') if iface else 'Unknown' }}"
          ipv4: "{% set iface = ifaces | selectattr('name', 'eq', 'tailscale0') | first | default(none) %}{{ iface.ipv4 | default('None') if iface else 'None' }}"
          ipv6: "{% set iface = ifaces | selectattr('name', 'eq', 'tailscale0') | first | default(none) %}{{ iface.ipv6 | default('None') if iface else 'None' }}"
          link_speed_mbps: "{% set iface = ifaces | selectattr('name', 'eq', 'tailscale0') | first | default(none) %}{{ iface.link_speed_mbps | default(0) if iface else 0 }}"
          duplex: "{% set iface = ifaces | selectattr('name', 'eq', 'tailscale0') | first | default(none) %}{{ iface.duplex | default('unknown') if iface else 'unknown' }}"
          mtu: "{% set iface = ifaces | selectattr('name', 'eq', 'tailscale0') | first | default(none) %}{{ iface.mtu | default(0) if iface else 0 }}"
          carrier: "{% set iface = ifaces | selectattr('name', 'eq', 'tailscale0') | first | default(none) %}{{ iface.carrier | default(false) if iface else false }}"
          driver: "{% set iface = ifaces | selectattr('name', 'eq', 'tailscale0') | first | default(none) %}{{ iface.driver | default('Unknown') if iface else 'Unknown' }}"
    binary_sensor:
      - name: "RV Hass Overall Status OK"
        unique_id: 95c99ade-56ed-5d79-ba94-532977120a5d
        availability: "{{ available }}"
        state: "{{ health.overall_ok | default(false) }}"
        icon: "mdi:check-circle"
      - name: "RV Hass Status Alarm"
        unique_id: 851b30d4-4c7d-5e6e-af84-9ea07943ee44
        availability: "{{ available }}"
        state: "{{ not (health.overall_ok | default(true)) }}"
        device_class: problem

  # ---------------------------------------------------------------------------
  # UI Sensors: Dashboard Display
  # ---------------------------------------------------------------------------
  - trigger:
      - platform: homeassistant
        event: start
      - platform: state
        entity_id: sensor.rv_hass_cpu_utilization
    variables:
      entity_id: sensor.rv_hass_cpu_utilization
      app: cpu_utilization
      options: {}
      state_info: >-
        {%- from 'main.jinja' import get_device_state_info -%}
        {{ get_device_state_info(entity_id, app, options=options) | from_json }}
      state_info_vertical: >-
        {%- from 'main.jinja' import get_device_state_info -%}
        {{ get_device_state_info(entity_id, app, layout='vertical', options=options) | from_json }}
    sensor:
      - name: ui_rvhass_cpu_utilization
        unique_id: 763a6074-9afc-5ab6-896e-e8dea30d46ad
        state: "{{ states(entity_id) }}"
        attributes:
          value: "{{ state_info.value }}"
          name: "{{ state_info.name }}"
          default_desc: "{{ state_info.default_desc }}"
          short_desc: "{{ state_info.short_desc }}"
          full_desc: "{{ state_info.full_desc }}"
          icon: "{{ state_info.icon }}"
          color: "{{ state_info.color }}"
          badge: "{{ state_info.badge }}"
          badge_color: "{{ state_info.badge_color }}"
          label: "{{ state_info.label }}"
          short_label: "{{ state_info.short_label }}"
          long_label: "{{ state_info.long_label }}"
          label_vertical: "{{ state_info_vertical.label }}"
          short_label_vertical: "{{ state_info_vertical.short_label }}"
          long_label_vertical: "{{ state_info_vertical.long_label }}"
          app: cpu_utilization
          device_class: none
  - trigger:
      - platform: homeassistant
        event: start
      - platform: state
        entity_id:
          - sensor.rv_hass_cpu_utilization
          - sensor.rv_hass_cpu_load_1m
    variables:
      entity_id: sensor.rv_hass_cpu_utilization
      app: cpu_utilization
      options:
        load: sensor.rv_hass_cpu_load_1m
      state_info: >-
        {%- from 'main.jinja' import get_device_state_info -%}
        {{ get_device_state_info(entity_id, app, options=options) | from_json }}
      state_info_vertical: >-
        {%- from 'main.jinja' import get_device_state_info -%}
        {{ get_device_state_info(entity_id, app, layout='vertical', options=options) | from_json }}
    sensor:
      - name: ui_rvhass_cpu_utilization_load
        unique_id: 8261198a-fbe5-56d2-b09d-3464825225ab
        state: "{{ states(entity_id) }}"
        attributes:
          value: "{{ state_info.value }}"
          name: "{{ state_info.name }}"
          default_desc: "{{ state_info.default_desc }}"
          short_desc: "{{ state_info.short_desc }}"
          full_desc: "{{ state_info.full_desc }}"
          icon: "{{ state_info.icon }}"
          color: "{{ state_info.color }}"
          badge: "{{ state_info.badge }}"
          badge_color: "{{ state_info.badge_color }}"
          label: "{{ state_info.label }}"
          short_label: "{{ state_info.short_label }}"
          long_label: "{{ state_info.long_label }}"
          label_vertical: "{{ state_info_vertical.label }}"
          short_label_vertical: "{{ state_info_vertical.short_label }}"
          long_label_vertical: "{{ state_info_vertical.long_label }}"
          app: cpu_utilization
          device_class: none
  - trigger:
      - platform: homeassistant
        event: start
      - platform: state
        entity_id:
          - sensor.rv_hass_cpu_load_1m
          - sensor.rv_hass_cpu_load_5m
          - sensor.rv_hass_cpu_load_15m
          - sensor.rv_hass_cpu_logical_cores
    variables:
      entity_id: sensor.rv_hass_cpu_load_1m
      app: cpu_load
      options:
        cores: sensor.rv_hass_cpu_logical_cores
      state_info: >-
        {%- from 'main.jinja' import get_device_state_info -%}
        {{ get_device_state_info(entity_id, app, options=options) | from_json }}
      state_info_vertical: >-
        {%- from 'main.jinja' import get_device_state_info -%}
        {{ get_device_state_info(entity_id, app, layout='vertical', options=options) | from_json }}
    sensor:
      - name: ui_rvhass_cpu_load_1m
        unique_id: b4611f32-8844-563c-84ed-6659fd1d219c
        state: "{{ states(entity_id) }}"
        attributes:
          value: "{{ state_info.value }}"
          name: "{{ state_info.name }}"
          default_desc: "{{ state_info.default_desc }}"
          short_desc: "{{ state_info.short_desc }}"
          full_desc: "{{ state_info.full_desc }}"
          icon: "{{ state_info.icon }}"
          color: "{{ state_info.color }}"
          badge: "{{ state_info.badge }}"
          badge_color: "{{ state_info.badge_color }}"
          label: "{{ state_info.label }}"
          short_label: "{{ state_info.short_label }}"
          long_label: "{{ state_info.long_label }}"
          label_vertical: "{{ state_info_vertical.label }}"
          short_label_vertical: "{{ state_info_vertical.short_label }}"
          long_label_vertical: "{{ state_info_vertical.long_label }}"
          app: cpu_load
          device_class: none
          long_desc: >-
            {%- set m1 = states('sensor.rv_hass_cpu_load_1m')|float(0) -%}{%- set m5 = states('sensor.rv_hass_cpu_load_5m')|float(0) -%}{%- set m15 = states('sensor.rv_hass_cpu_load_15m')|float(0) -%}{%- set c = states('sensor.rv_hass_cpu_logical_cores')|int(0) -%}{{- '1m: ' ~ m1 ~ ' • 5m: ' ~ m5 ~ ' • 15m: ' ~ m15 ~ ' • ' ~ c ~ ' cores' -}}
  - trigger:
      - platform: homeassistant
        event: start
      - platform: state
        entity_id:
          - sensor.rv_hass_memory_percent
          - sensor.rv_hass_memory_used
          - sensor.rv_hass_memory_total
          - sensor.rv_hass_memory_available
    variables:
      entity_id: sensor.rv_hass_memory_percent
      app: memory_utilization
      options:
        used: sensor.rv_hass_memory_used
      state_info: >-
        {%- from 'main.jinja' import get_device_state_info -%}
        {{ get_device_state_info(entity_id, app, options=options) | from_json }}
      state_info_vertical: >-
        {%- from 'main.jinja' import get_device_state_info -%}
        {{ get_device_state_info(entity_id, app, layout='vertical', options=options) | from_json }}
    sensor:
      - name: ui_rvhass_memory_percent
        unique_id: 25df4550-1ad1-5a29-b3fa-75f91300b881
        state: "{{ states(entity_id) }}"
        attributes:
          value: "{{ state_info.value }}"
          name: "{{ state_info.name }}"
          default_desc: "{{ state_info.default_desc }}"
          short_desc: "{{ state_info.short_desc }}"
          full_desc: "{{ state_info.full_desc }}"
          icon: "{{ state_info.icon }}"
          color: "{{ state_info.color }}"
          badge: "{{ state_info.badge }}"
          badge_color: "{{ state_info.badge_color }}"
          label: "{{ state_info.label }}"
          short_label: "{{ state_info.short_label }}"
          long_label: "{{ state_info.long_label }}"
          label_vertical: "{{ state_info_vertical.label }}"
          short_label_vertical: "{{ state_info_vertical.short_label }}"
          long_label_vertical: "{{ state_info_vertical.long_label }}"
          app: memory_utilization
          device_class: none
          long_desc: >-
            {%- from 'units/base.jinja' import u_humanize_entity -%}{%- set mem_used = u_humanize_entity('sensor.rv_hass_memory_used') -%}{%- set mem_total = u_humanize_entity('sensor.rv_hass_memory_total') -%}{%- set mem_free = u_humanize_entity('sensor.rv_hass_memory_available') -%}{{- 'Used: ' ~ mem_used ~ ' / ' ~ mem_total ~ '\nFree: ' ~ mem_free -}}
  - trigger:
      - platform: homeassistant
        event: start
      - platform: state
        entity_id: sensor.rv_hass_uptime_s
    variables:
      entity_id: sensor.rv_hass_uptime_s
      app: uptime
      options:
        device_class: duration
        uom: s
      state_info: >-
        {%- from 'main.jinja' import get_device_state_info -%}
        {{ get_device_state_info(entity_id, app, options=options) | from_json }}
      state_info_vertical: >-
        {%- from 'main.jinja' import get_device_state_info -%}
        {{ get_device_state_info(entity_id, app, layout='vertical', options=options) | from_json }}
    sensor:
      - name: ui_rvhass_uptime_s
        unique_id: 86cc2bf5-7424-5b64-9efb-09919d17585d
        state: "{{ states(entity_id) }}"
        attributes:
          value: "{{ state_info.value }}"
          name: "{{ state_info.name }}"
          default_desc: "{{ state_info.default_desc }}"
          short_desc: "{{ state_info.short_desc }}"
          full_desc: "{{ state_info.full_desc }}"
          icon: "{{ state_info.icon }}"
          color: "{{ state_info.color }}"
          badge: "{{ state_info.badge }}"
          badge_color: "{{ state_info.badge_color }}"
          label: "{{ state_info.label }}"
          short_label: "{{ state_info.short_label }}"
          long_label: "{{ state_info.long_label }}"
          label_vertical: "{{ state_info_vertical.label }}"
          short_label_vertical: "{{ state_info_vertical.short_label }}"
          long_label_vertical: "{{ state_info_vertical.long_label }}"
          app: uptime
          device_class: duration
  - trigger:
      - platform: homeassistant
        event: start
      - platform: state
        entity_id: sensor.rv_hass_updates_pending
    variables:
      entity_id: sensor.rv_hass_updates_pending
      app: count_pending
      options: {}
      state_info: >-
        {%- from 'main.jinja' import get_device_state_info -%}
        {{ get_device_state_info(entity_id, app, options=options) | from_json }}
      state_info_vertical: >-
        {%- from 'main.jinja' import get_device_state_info -%}
        {{ get_device_state_info(entity_id, app, layout='vertical', options=options) | from_json }}
    sensor:
      - name: ui_rvhass_updates_pending
        unique_id: 4eaf1f7d-4ac2-5029-8a12-9bb64ba72ee7
        state: "{{ states(entity_id) }}"
        attributes:
          value: "{{ state_info.value }}"
          name: "{{ state_info.name }}"
          default_desc: "{{ state_info.default_desc }}"
          short_desc: "{{ state_info.short_desc }}"
          full_desc: "{{ state_info.full_desc }}"
          icon: "{{ state_info.icon }}"
          color: "{{ state_info.color }}"
          badge: "{{ state_info.badge }}"
          badge_color: "{{ state_info.badge_color }}"
          label: "{{ state_info.label }}"
          short_label: "{{ state_info.short_label }}"
          long_label: "{{ state_info.long_label }}"
          label_vertical: "{{ state_info_vertical.label }}"
          short_label_vertical: "{{ state_info_vertical.short_label }}"
          long_label_vertical: "{{ state_info_vertical.long_label }}"
          app: count_pending
          device_class: none
  - trigger:
      - platform: homeassistant
        event: start
      - platform: state
        entity_id:
          - binary_sensor.rv_hass_status_alarm
          - sensor.rv_hass_overall_status_summary
    variables:
      entity_id: binary_sensor.rv_hass_status_alarm
      app: alarm
      options:
        device_class: binary
        summary: sensor.rv_hass_overall_status_summary
      state_info: >-
        {%- from 'main.jinja' import get_device_state_info -%}
        {{ get_device_state_info(entity_id, app, options=options) | from_json }}
      state_info_vertical: >-
        {%- from 'main.jinja' import get_device_state_info -%}
        {{ get_device_state_info(entity_id, app, layout='vertical', options=options) | from_json }}
    sensor:
      - name: ui_rvhass_status_alarm
        unique_id: 3319a23b-4e2e-58e3-8a17-2819efc6effe
        state: "{{ states(entity_id) }}"
        attributes:
          value: "{{ state_info.value }}"
          name: "{{ state_info.name }}"
          default_desc: "{{ state_info.default_desc }}"
          short_desc: "{{ state_info.short_desc }}"
          full_desc: "{{ state_info.full_desc }}"
          icon: "{{ state_info.icon }}"
          color: "{{ state_info.color }}"
          badge: "{{ state_info.badge }}"
          badge_color: "{{ state_info.badge_color }}"
          label: "{{ state_info.label }}"
          short_label: "{{ state_info.short_label }}"
          long_label: "{{ state_info.long_label }}"
          label_vertical: "{{ state_info_vertical.label }}"
          short_label_vertical: "{{ state_info_vertical.short_label }}"
          long_label_vertical: "{{ state_info_vertical.long_label }}"
          app: alarm
          device_class: binary
  - trigger:
      - platform: homeassistant
        event: start
      - platform: state
        entity_id: sensor.rv_hass_cpu_temp
    variables:
      entity_id: sensor.rv_hass_cpu_temp
      app: cpu
      options:
        device_class: temperature
      state_info: >-
        {%- from 'main.jinja' import get_device_state_info -%}
        {{ get_device_state_info(entity_id, app, options=options) | from_json }}
      state_info_vertical: >-
        {%- from 'main.jinja' import get_device_state_info -%}
        {{ get_device_state_info(entity_id, app, layout='vertical', options=options) | from_json }}
    sensor:
      - name: ui_rvhass_cpu_temp
        unique_id: bdd622c6-e3a1-5c3d-97ea-db66a871ab01
        state: "{{ states(entity_id) }}"
        attributes:
          value: "{{ state_info.value }}"
          name: "{{ state_info.name }}"
          default_desc: "{{ state_info.default_desc }}"
          short_desc: "{{ state_info.short_desc }}"
          full_desc: "{{ state_info.full_desc }}"
          icon: "{{ state_info.icon }}"
          color: "{{ state_info.color }}"
          badge: "{{ state_info.badge }}"
          badge_color: "{{ state_info.badge_color }}"
          label: "{{ state_info.label }}"
          short_label: "{{ state_info.short_label }}"
          long_label: "{{ state_info.long_label }}"
          label_vertical: "{{ state_info_vertical.label }}"
          short_label_vertical: "{{ state_info_vertical.short_label }}"
          long_label_vertical: "{{ state_info_vertical.long_label }}"
          app: cpu
          device_class: temperature
  - trigger:
      - platform: homeassistant
        event: start
      - platform: state
        entity_id:
          - sensor.rv_hass_fs_data_percent
          - sensor.rv_hass_fs_data_used
          - sensor.rv_hass_fs_data_total
    variables:
      entity_id: sensor.rv_hass_fs_data_percent
      app: disk_utilization
      options:
        free: sensor.rv_hass_fs_data_free
      state_info: >-
        {%- from 'main.jinja' import get_device_state_info -%}
        {{ get_device_state_info(entity_id, app, options=options) | from_json }}
      state_info_vertical: >-
        {%- from 'main.jinja' import get_device_state_info -%}
        {{ get_device_state_info(entity_id, app, layout='vertical', options=options) | from_json }}
    sensor:
      - name: ui_rvhass_fs_data_percent
        unique_id: c39ce5b2-c317-56b0-8f92-9a5724170784
        state: "{{ states(entity_id) }}"
        attributes:
          value: "{{ state_info.value }}"
          name: "{{ state_info.name }}"
          default_desc: "{{ state_info.default_desc }}"
          short_desc: "{{ state_info.short_desc }}"
          full_desc: "{{ state_info.full_desc }}"
          icon: "{{ state_info.icon }}"
          color: "{{ state_info.color }}"
          badge: "{{ state_info.badge }}"
          badge_color: "{{ state_info.badge_color }}"
          label: "{{ state_info.label }}"
          short_label: "{{ state_info.short_label }}"
          long_label: "{{ state_info.long_label }}"
          label_vertical: "{{ state_info_vertical.label }}"
          short_label_vertical: "{{ state_info_vertical.short_label }}"
          long_label_vertical: "{{ state_info_vertical.long_label }}"
          app: disk_utilization
          device_class: none
          long_desc: >-
            {%- from 'units/base.jinja' import u_humanize_entity -%}{%- set total = u_humanize_entity('sensor.rv_hass_fs_data_total') -%}{%- set used = u_humanize_entity('sensor.rv_hass_fs_data_used') -%}{{- 'Total: ' ~ total ~ ' • Used: ' ~ used -}}
