# =============================================================================
# Package: OPNsense Hardware Monitor
# =============================================================================
#
# Auto-generated by generate_hwmon_assets.py
# DO NOT EDIT - Changes will be overwritten
#
# Monitors OPNsense system telemetry via MQTT and provides:
#   - Data sensors for CPU, memory, disk, network, etc.
#   - UI sensors for dashboard card attributes
#   - Binary sensors for health status
#
# Source telemetry sensor: sensor.opnsense_home_arpa_opnsense_home_arpa_hardware_telemetry
#
# =============================================================================

template:
  # ---------------------------------------------------------------------------
  # Data Sensors: OPNsense
  # ---------------------------------------------------------------------------
  - trigger:
      - platform: homeassistant
        event: start
      - platform: state
        entity_id: sensor.opnsense_home_arpa_opnsense_home_arpa_hardware_telemetry
    variables:
      entity_id: sensor.opnsense_home_arpa_opnsense_home_arpa_hardware_telemetry
      available: "{{ has_value(entity_id) }}"
      host: "{{ state_attr(entity_id, 'host') or {} }}"
      health: "{{ state_attr(entity_id, 'health') or {} }}"
      cpus: "{{ state_attr(entity_id, 'cpus') or [] }}"
      memory: "{{ state_attr(entity_id, 'memory') or {} }}"
      filesystems: "{{ state_attr(entity_id, 'filesystems') or [] }}"
      ifaces: "{{ state_attr(entity_id, 'ifaces') or [] }}"
      gpus: "{{ state_attr(entity_id, 'gpus') or [] }}"
      tpus: "{{ state_attr(entity_id, 'tpus') or [] }}"
      motherboard_temps: "{{ state_attr(entity_id, 'motherboard').get('temps', []) if state_attr(entity_id, 'motherboard') else [] }}"
      opnsense_plugins: "{{ state_attr(entity_id, 'opnsense_plugins') or [] }}"
      zenarmor: "{{ state_attr(entity_id, 'zenarmor') or {} }}"
    sensor:
      - name: "OPNsense Uptime (s)"
        unique_id: 1b34d41c-a646-5afd-ab9d-cd97cd6c2805
        availability: "{{ available }}"
        state: "{{ (host.uptime_s | default(0)) | int }}"
        icon: "mdi:timer-outline"
        unit_of_measurement: "s"
        state_class: measurement
        device_class: duration
      - name: "OPNsense Process Count"
        unique_id: ee11cebd-3e67-5906-a226-c2bc00e507ea
        availability: "{{ available }}"
        state: "{{ (host.process_count | default(0)) | int }}"
        icon: "mdi:application"
        state_class: measurement
      - name: "OPNsense Overall Status Summary"
        unique_id: 53cf4e72-8f01-5f7a-83b0-931659d97b98
        availability: "{{ available }}"
        state: >-
          {{ (health.issues[0][:250] ~ '...') if health.issues and health.issues[0]|length > 250 else (health.issues[0] if health.issues else 'OK') }}
        icon: "mdi:card-text-outline"
      - name: "OPNsense Updates Pending"
        unique_id: aa502627-9975-52a1-b37e-17d42b19fda9
        availability: "{{ available }}"
        state: "{{ (health.updates.pending | default(0)) | int }}"
        icon: "mdi:update"
        state_class: measurement
      - name: "OPNsense Services Checked"
        unique_id: 3500aad7-b587-5f92-bb4c-f1bc0cb70c98
        availability: "{{ available }}"
        state: "{{ (health.services | default([]) | length) }}"
        icon: "mdi:playlist-check"
        state_class: measurement
      - name: "OPNsense Containers Checked"
        unique_id: 914d8a24-0081-555e-8a1a-57b08e7ce1e7
        availability: "{{ available }}"
        state: "{{ (health.containers | default([]) | length) }}"
        icon: "mdi:docker"
        state_class: measurement
      - name: "OPNsense CPU Utilization"
        unique_id: 67a8e93e-b9b9-58c2-af02-3aad24d05143
        availability: "{{ available }}"
        state: "{{ (cpus[0].load_pct | default(0)) | round(1) }}"
        icon: "mdi:chip"
        unit_of_measurement: "%"
        state_class: measurement
      - name: "OPNsense CPU Load 1m"
        unique_id: c4716b41-ca0d-5636-ab20-39eb5ce2a6aa
        availability: "{{ available }}"
        state: "{{ (cpus[0].load_1m | default(0)) | round(2) }}"
        icon: "mdi:chip"
        state_class: measurement
      - name: "OPNsense CPU Load 5m"
        unique_id: 0ec3c6ec-c269-5009-b51c-8ff20dd569c3
        availability: "{{ available }}"
        state: "{{ (cpus[0].load_5m | default(0)) | round(2) }}"
        icon: "mdi:chip"
        state_class: measurement
      - name: "OPNsense CPU Load 15m"
        unique_id: 1b2460c2-b7a8-5f48-b966-39c85ab9bbf7
        availability: "{{ available }}"
        state: "{{ (cpus[0].load_15m | default(0)) | round(2) }}"
        icon: "mdi:chip"
        state_class: measurement
      - name: "OPNsense CPU Physical Cores"
        unique_id: 78a20a83-2451-5f17-89fc-87573b91c9af
        availability: "{{ available }}"
        state: "{{ cpus[0].num_physical_cores | default(0) }}"
        icon: "mdi:chip"
        state_class: measurement
      - name: "OPNsense CPU Logical Cores"
        unique_id: ce1d3853-cec8-5df5-9ebb-36829ac70b87
        availability: "{{ available }}"
        state: "{{ cpus[0].num_logical_cores | default(0) }}"
        icon: "mdi:chip"
        state_class: measurement
      - name: "OPNsense Memory Percent"
        unique_id: d8094427-598b-5156-b5a1-aed34b3c6a82
        availability: "{{ available }}"
        state: "{{ (memory.system.load_pct | default(0)) | round(1) }}"
        icon: "mdi:memory"
        unit_of_measurement: "%"
        state_class: measurement
      - name: "OPNsense Memory Used"
        unique_id: cdbfb266-97db-5b4f-8053-8e7f7f6f2afc
        availability: "{{ available }}"
        state: >-
          {{ ((memory.system.used_b | default(0)) / 1073741824) | round(2) }}
        unit_of_measurement: "GiB"
        state_class: measurement
        device_class: data_size
      - name: "OPNsense Memory Available"
        unique_id: 897d5164-4202-55e4-9db9-956fcab4475a
        availability: "{{ available }}"
        state: >-
          {{ ((memory.system.available_b | default(0)) / 1073741824) | round(2) }}
        unit_of_measurement: "GiB"
        state_class: measurement
        device_class: data_size
      - name: "OPNsense Memory Total"
        unique_id: 3e5102e5-8f89-5fec-92b1-3502666fef73
        availability: "{{ available }}"
        state: >-
          {{ ((memory.system.total_b | default(0)) / 1073741824) | round(2) }}
        unit_of_measurement: "GiB"
        state_class: measurement
        device_class: data_size
      - name: "OPNsense Swap Percent"
        unique_id: 6239bc58-e7bc-5d5f-8ca6-e5fc34fa2167
        availability: "{{ available }}"
        state: "{{ (memory.virtual.load_pct | default(0)) | round(1) }}"
        icon: "mdi:memory"
        unit_of_measurement: "%"
        state_class: measurement
      - name: "OPNsense FS Root Percent"
        unique_id: c9b7f247-3454-50a0-98d2-9c66621c9946
        availability: "{{ available }}"
        state: >-
          {% set fs = filesystems | selectattr('mountpoint', 'eq', '/') | first | default(none) %}{{ ((fs.used_b / (fs.used_b + fs.available_b)) * 100) | round(1) if fs else 0 }}
        icon: "mdi:harddisk"
        unit_of_measurement: "%"
        state_class: measurement
      - name: "OPNsense FS Root Used"
        unique_id: 5feb2eeb-6c53-5a86-87b1-79bd96d7880e
        availability: "{{ available }}"
        state: >-
          {% set fs = filesystems | selectattr('mountpoint', 'eq', '/') | first | default(none) %}{{ ((fs.used_b | default(0)) / 1073741824) | round(2) if fs else 0 }}
        icon: "mdi:harddisk"
        unit_of_measurement: "GiB"
        state_class: measurement
        device_class: data_size
      - name: "OPNsense FS Root Total"
        unique_id: 6a47df36-c3d5-5ef2-8974-ce3dfdb237f7
        availability: "{{ available }}"
        state: >-
          {% set fs = filesystems | selectattr('mountpoint', 'eq', '/') | first | default(none) %}{{ (((fs.used_b | default(0)) + (fs.available_b | default(0))) / 1073741824) | round(2) if fs else 0 }}
        icon: "mdi:harddisk"
        unit_of_measurement: "GiB"
        state_class: measurement
        device_class: data_size
      - name: "OPNsense FS Logs Percent"
        unique_id: 02957e22-7805-5990-865c-ce7712dea2e9
        availability: "{{ available }}"
        state: >-
          {% set fs = filesystems | selectattr('mountpoint', 'eq', '/var/log') | first | default(none) %}{{ ((fs.used_b / (fs.used_b + fs.available_b)) * 100) | round(1) if fs else 0 }}
        icon: "mdi:harddisk"
        unit_of_measurement: "%"
        state_class: measurement
      - name: "OPNsense FS Logs Used"
        unique_id: b6dee06d-1f71-5556-af20-ae06e257f5c5
        availability: "{{ available }}"
        state: >-
          {% set fs = filesystems | selectattr('mountpoint', 'eq', '/var/log') | first | default(none) %}{{ ((fs.used_b | default(0)) / 1073741824) | round(2) if fs else 0 }}
        icon: "mdi:harddisk"
        unit_of_measurement: "GiB"
        state_class: measurement
        device_class: data_size
      - name: "OPNsense FS Logs Total"
        unique_id: d7ded361-20a2-5efb-9d47-5c29a860a7cb
        availability: "{{ available }}"
        state: >-
          {% set fs = filesystems | selectattr('mountpoint', 'eq', '/var/log') | first | default(none) %}{{ (((fs.used_b | default(0)) + (fs.available_b | default(0))) / 1073741824) | round(2) if fs else 0 }}
        icon: "mdi:harddisk"
        unit_of_measurement: "GiB"
        state_class: measurement
        device_class: data_size
      - name: "OPNsense LAN Upload"
        unique_id: a476686a-63be-508b-bb04-831097fcdd0b
        availability: "{{ available }}"
        state: >-
          {% set iface = ifaces | selectattr('name', 'eq', 're0') | first | default(none) %}{{ (iface.rate_up_bps | default(0)) | round(0) if iface else 0 }}
        icon: "mdi:upload"
        unit_of_measurement: "B/s"
        device_class: data_rate
      - name: "OPNsense LAN Download"
        unique_id: 5f07791d-2aa8-5bb5-8acf-2fa089062dd4
        availability: "{{ available }}"
        state: >-
          {% set iface = ifaces | selectattr('name', 'eq', 're0') | first | default(none) %}{{ (iface.rate_down_bps | default(0)) | round(0) if iface else 0 }}
        icon: "mdi:download"
        unit_of_measurement: "B/s"
        device_class: data_rate
      - name: "OPNsense LAN IPv4"
        unique_id: 6af50e70-e874-57e0-bd1b-2e1d82f8bd3f
        availability: "{{ available }}"
        state: >-
          {% set iface = ifaces | selectattr('name', 'eq', 're0') | first | default(none) %}{{ iface.ipv4 | default('unknown') if iface else 'unknown' }}
      - name: "OPNsense WAN Upload"
        unique_id: 49baaaa6-0b23-5ab9-bb1f-ccb8569f6d3a
        availability: "{{ available }}"
        state: >-
          {% set iface = ifaces | selectattr('name', 'eq', 're1') | first | default(none) %}{{ (iface.rate_up_bps | default(0)) | round(0) if iface else 0 }}
        icon: "mdi:upload"
        unit_of_measurement: "B/s"
        device_class: data_rate
      - name: "OPNsense WAN Download"
        unique_id: 3f43d508-60a0-5283-a8d8-cc32307bd378
        availability: "{{ available }}"
        state: >-
          {% set iface = ifaces | selectattr('name', 'eq', 're1') | first | default(none) %}{{ (iface.rate_down_bps | default(0)) | round(0) if iface else 0 }}
        icon: "mdi:download"
        unit_of_measurement: "B/s"
        device_class: data_rate
      - name: "OPNsense WAN IPv4"
        unique_id: 0129af34-a5ec-5c33-a87d-d2f3671caff5
        availability: "{{ available }}"
        state: >-
          {% set iface = ifaces | selectattr('name', 'eq', 're1') | first | default(none) %}{{ iface.ipv4 | default('unknown') if iface else 'unknown' }}
      - name: "OPNsense VLAN10 Upload"
        unique_id: 7144e7d1-13e6-508b-b820-f65745a891b3
        availability: "{{ available }}"
        state: >-
          {% set iface = ifaces | selectattr('name', 'eq', 're0_vlan10') | first | default(none) %}{{ (iface.rate_up_bps | default(0)) | round(0) if iface else 0 }}
        icon: "mdi:upload"
        unit_of_measurement: "B/s"
        device_class: data_rate
      - name: "OPNsense VLAN10 Download"
        unique_id: a7842514-1165-58a4-b63a-4bd67cb1ec36
        availability: "{{ available }}"
        state: >-
          {% set iface = ifaces | selectattr('name', 'eq', 're0_vlan10') | first | default(none) %}{{ (iface.rate_down_bps | default(0)) | round(0) if iface else 0 }}
        icon: "mdi:download"
        unit_of_measurement: "B/s"
        device_class: data_rate
      - name: "OPNsense VLAN10 IPv4"
        unique_id: 0503b48e-3859-5117-8694-9cc38e9583c0
        availability: "{{ available }}"
        state: >-
          {% set iface = ifaces | selectattr('name', 'eq', 're0_vlan10') | first | default(none) %}{{ iface.ipv4 | default('unknown') if iface else 'unknown' }}
      - name: "OPNsense VLAN20 Upload"
        unique_id: aa88fc22-c3f6-509c-8f97-73baa038eeb3
        availability: "{{ available }}"
        state: >-
          {% set iface = ifaces | selectattr('name', 'eq', 're0_vlan20') | first | default(none) %}{{ (iface.rate_up_bps | default(0)) | round(0) if iface else 0 }}
        icon: "mdi:upload"
        unit_of_measurement: "B/s"
        device_class: data_rate
      - name: "OPNsense VLAN20 Download"
        unique_id: 52c5a707-d396-5e72-8fc9-a25f6b125131
        availability: "{{ available }}"
        state: >-
          {% set iface = ifaces | selectattr('name', 'eq', 're0_vlan20') | first | default(none) %}{{ (iface.rate_down_bps | default(0)) | round(0) if iface else 0 }}
        icon: "mdi:download"
        unit_of_measurement: "B/s"
        device_class: data_rate
      - name: "OPNsense VLAN20 IPv4"
        unique_id: 470d0389-1364-53d8-8270-062698e2aaa0
        availability: "{{ available }}"
        state: >-
          {% set iface = ifaces | selectattr('name', 'eq', 're0_vlan20') | first | default(none) %}{{ iface.ipv4 | default('unknown') if iface else 'unknown' }}
      - name: "OPNsense WireGuard Upload"
        unique_id: 0e31fb69-c784-53f0-9eb5-488a274161df
        availability: "{{ available }}"
        state: >-
          {% set iface = ifaces | selectattr('name', 'eq', 'wg0') | first | default(none) %}{{ (iface.rate_up_bps | default(0)) | round(0) if iface else 0 }}
        icon: "mdi:upload"
        unit_of_measurement: "B/s"
        device_class: data_rate
      - name: "OPNsense WireGuard Download"
        unique_id: f2043b11-e56e-5fb4-994b-d6692c7513e8
        availability: "{{ available }}"
        state: >-
          {% set iface = ifaces | selectattr('name', 'eq', 'wg0') | first | default(none) %}{{ (iface.rate_down_bps | default(0)) | round(0) if iface else 0 }}
        icon: "mdi:download"
        unit_of_measurement: "B/s"
        device_class: data_rate
      - name: "OPNsense WireGuard IPv4"
        unique_id: 7d190304-426f-56c5-a946-6bd1e0853cfe
        availability: "{{ available }}"
        state: >-
          {% set iface = ifaces | selectattr('name', 'eq', 'wg0') | first | default(none) %}{{ iface.ipv4 | default('unknown') if iface else 'unknown' }}
      - name: "OPNsense Tailscale Upload"
        unique_id: ec0b1ebf-96ce-5db4-a9f0-f658aeaa6631
        availability: "{{ available }}"
        state: >-
          {% set iface = ifaces | selectattr('name', 'eq', 'tailscale0') | first | default(none) %}{{ (iface.rate_up_bps | default(0)) | round(0) if iface else 0 }}
        icon: "mdi:upload"
        unit_of_measurement: "B/s"
        device_class: data_rate
      - name: "OPNsense Tailscale Download"
        unique_id: 984b5c8e-fc95-5303-aa66-52fd31f1e3ab
        availability: "{{ available }}"
        state: >-
          {% set iface = ifaces | selectattr('name', 'eq', 'tailscale0') | first | default(none) %}{{ (iface.rate_down_bps | default(0)) | round(0) if iface else 0 }}
        icon: "mdi:download"
        unit_of_measurement: "B/s"
        device_class: data_rate
      - name: "OPNsense Tailscale IPv4"
        unique_id: b3068ece-e957-501d-b98a-c254884c83c5
        availability: "{{ available }}"
        state: >-
          {% set iface = ifaces | selectattr('name', 'eq', 'tailscale0') | first | default(none) %}{{ iface.ipv4 | default('unknown') if iface else 'unknown' }}
      - name: "OPNsense System Temp"
        unique_id: dbf68476-d4a8-5d1e-bfc1-5c2eaba6533e
        availability: "{{ available }}"
        state: >-
          {% set t = motherboard_temps | selectattr('name', 'eq', 'tz0') | first | default(none) %}{{ (t.temp_c | default(0)) | round(1) if t else 0 }}
        unit_of_measurement: "°C"
        state_class: measurement
        device_class: temperature
      - name: "OPNsense Plugins Count"
        unique_id: 0a34d233-6bba-51e0-9e0f-bdaed35c03e0
        availability: "{{ available }}"
        state: "{{ opnsense_plugins | length }}"
        icon: "mdi:puzzle"
        state_class: measurement
      - name: "OPNsense Zenarmor Cloud"
        unique_id: 8e3da521-cf7e-5f0a-ae9c-82a8cf50a103
        availability: "{{ available }}"
        state: "{{ 'Running' if zenarmor.cloud_running else 'Stopped' }}"
        icon: "mdi:cloud-check"
      - name: "OPNsense Zenarmor Engine"
        unique_id: c80a30f5-dab7-575f-98ce-1a533648efde
        availability: "{{ available }}"
        state: "{{ 'Running' if zenarmor.engine_running else 'Stopped' }}"
        icon: "mdi:engine"
    binary_sensor:
      - name: "OPNsense Overall Status OK"
        unique_id: f3bc7662-3f8a-595f-8f8e-c72e3ad76364
        availability: "{{ available }}"
        state: "{{ health.overall_ok | default(false) }}"
        icon: "mdi:check-circle"
      - name: "OPNsense Status Alarm"
        unique_id: 92d74fe2-fd30-535f-920a-372afbc881fd
        availability: "{{ available }}"
        state: "{{ not (health.overall_ok | default(true)) }}"
        device_class: problem
      - name: "OPNsense Services OK"
        unique_id: 6cc3a292-e6ba-5336-bc0f-f8d03da0c405
        availability: "{{ available }}"
        state: >-
          {% set tracked = ['adguardhome', 'kea', 'crowdsec', 'cron'] %}{% set svc_list = health.services | default([]) %}{{ svc_list | selectattr('name', 'in', tracked) | selectattr('ok', 'eq', false) | list | length == 0 }}
        icon: "mdi:check-circle"
      - name: "OPNsense Display Services"
        unique_id: 1693efad-586e-59ef-bc7a-5aa25aeebf91
        availability: "{{ available }}"
        state: >-
          {% set tracked = ['adguardhome', 'kea', 'crowdsec', 'cron'] %}{% set svc_list = health.services | default([]) %}{{ svc_list | selectattr('name', 'in', tracked) | selectattr('ok', 'eq', false) | list | length > 0 }}
        icon: "mdi:cog-off"
      - name: "OPNsense Service adguardhome"
        unique_id: 3236acd0-77e5-59eb-9e17-1fdd14c572ea
        availability: "{{ available }}"
        state: >-
          {% set svc = health.services | default([]) | selectattr('name', 'eq', 'adguardhome') | first | default(none) %}{{ svc.ok | default(false) if svc else false }}
        icon: "mdi:cog"
      - name: "OPNsense Service kea"
        unique_id: f548ccf0-ed15-5233-9918-3ab75a7b09d0
        availability: "{{ available }}"
        state: >-
          {% set svc = health.services | default([]) | selectattr('name', 'eq', 'kea') | first | default(none) %}{{ svc.ok | default(false) if svc else false }}
        icon: "mdi:cog"
      - name: "OPNsense Service crowdsec"
        unique_id: 82411390-6f42-53f1-a1bb-f48db609fcd8
        availability: "{{ available }}"
        state: >-
          {% set svc = health.services | default([]) | selectattr('name', 'eq', 'crowdsec') | first | default(none) %}{{ svc.ok | default(false) if svc else false }}
        icon: "mdi:cog"
      - name: "OPNsense Service cron"
        unique_id: 8f02af0e-1b0d-58a4-8df3-7b5fcee25de7
        availability: "{{ available }}"
        state: >-
          {% set svc = health.services | default([]) | selectattr('name', 'eq', 'cron') | first | default(none) %}{{ svc.ok | default(false) if svc else false }}
        icon: "mdi:cog"

  # ---------------------------------------------------------------------------
  # UI Sensors: Dashboard Display
  # ---------------------------------------------------------------------------
  - trigger:
      - platform: homeassistant
        event: start
      - platform: state
        entity_id: sensor.opnsense_cpu_utilization
    variables:
      entity_id: sensor.opnsense_cpu_utilization
      app: cpu_utilization
      options: {}
      state_info: >-
        {%- from 'main.jinja' import get_device_state_info -%}
        {{ get_device_state_info(entity_id, app, options=options) | from_json }}
      state_info_vertical: >-
        {%- from 'main.jinja' import get_device_state_info -%}
        {{ get_device_state_info(entity_id, app, layout='vertical', options=options) | from_json }}
    sensor:
      - name: ui_opnsense_cpu_utilization
        unique_id: 0767fe54-e2f3-5a2c-8375-c1b2577bba0a
        state: "{{ states(entity_id) }}"
        attributes:
          value: "{{ state_info.value }}"
          name: "{{ state_info.name }}"
          default_desc: "{{ state_info.default_desc }}"
          short_desc: "{{ state_info.short_desc }}"
          full_desc: "{{ state_info.full_desc }}"
          icon: "{{ state_info.icon }}"
          color: "{{ state_info.color }}"
          badge: "{{ state_info.badge }}"
          badge_color: "{{ state_info.badge_color }}"
          label: "{{ state_info.label }}"
          short_label: "{{ state_info.short_label }}"
          long_label: "{{ state_info.long_label }}"
          label_vertical: "{{ state_info_vertical.label }}"
          short_label_vertical: "{{ state_info_vertical.short_label }}"
          long_label_vertical: "{{ state_info_vertical.long_label }}"
          app: cpu_utilization
          device_class: none
  - trigger:
      - platform: homeassistant
        event: start
      - platform: state
        entity_id:
          - sensor.opnsense_cpu_utilization
          - sensor.opnsense_cpu_load_1m
    variables:
      entity_id: sensor.opnsense_cpu_utilization
      app: cpu_utilization
      options:
        load: sensor.opnsense_cpu_load_1m
      state_info: >-
        {%- from 'main.jinja' import get_device_state_info -%}
        {{ get_device_state_info(entity_id, app, options=options) | from_json }}
      state_info_vertical: >-
        {%- from 'main.jinja' import get_device_state_info -%}
        {{ get_device_state_info(entity_id, app, layout='vertical', options=options) | from_json }}
    sensor:
      - name: ui_opnsense_cpu_utilization_load
        unique_id: 5fb9e027-f5f4-5649-9865-10d2f84c53a1
        state: "{{ states(entity_id) }}"
        attributes:
          value: "{{ state_info.value }}"
          name: "{{ state_info.name }}"
          default_desc: "{{ state_info.default_desc }}"
          short_desc: "{{ state_info.short_desc }}"
          full_desc: "{{ state_info.full_desc }}"
          icon: "{{ state_info.icon }}"
          color: "{{ state_info.color }}"
          badge: "{{ state_info.badge }}"
          badge_color: "{{ state_info.badge_color }}"
          label: "{{ state_info.label }}"
          short_label: "{{ state_info.short_label }}"
          long_label: "{{ state_info.long_label }}"
          label_vertical: "{{ state_info_vertical.label }}"
          short_label_vertical: "{{ state_info_vertical.short_label }}"
          long_label_vertical: "{{ state_info_vertical.long_label }}"
          app: cpu_utilization
          device_class: none
  - trigger:
      - platform: homeassistant
        event: start
      - platform: state
        entity_id:
          - sensor.opnsense_cpu_load_1m
          - sensor.opnsense_cpu_load_5m
          - sensor.opnsense_cpu_load_15m
          - sensor.opnsense_cpu_logical_cores
    variables:
      entity_id: sensor.opnsense_cpu_load_1m
      app: cpu_load
      options:
        cores: sensor.opnsense_cpu_logical_cores
      state_info: >-
        {%- from 'main.jinja' import get_device_state_info -%}
        {{ get_device_state_info(entity_id, app, options=options) | from_json }}
      state_info_vertical: >-
        {%- from 'main.jinja' import get_device_state_info -%}
        {{ get_device_state_info(entity_id, app, layout='vertical', options=options) | from_json }}
    sensor:
      - name: ui_opnsense_cpu_load_1m
        unique_id: fdd85a34-7067-56e7-9d08-a1ebbe0ba248
        state: "{{ states(entity_id) }}"
        attributes:
          value: "{{ state_info.value }}"
          name: "{{ state_info.name }}"
          default_desc: "{{ state_info.default_desc }}"
          short_desc: "{{ state_info.short_desc }}"
          full_desc: "{{ state_info.full_desc }}"
          icon: "{{ state_info.icon }}"
          color: "{{ state_info.color }}"
          badge: "{{ state_info.badge }}"
          badge_color: "{{ state_info.badge_color }}"
          label: "{{ state_info.label }}"
          short_label: "{{ state_info.short_label }}"
          long_label: "{{ state_info.long_label }}"
          label_vertical: "{{ state_info_vertical.label }}"
          short_label_vertical: "{{ state_info_vertical.short_label }}"
          long_label_vertical: "{{ state_info_vertical.long_label }}"
          app: cpu_load
          device_class: none
          long_desc: >-
            {%- set m1 = states('sensor.opnsense_cpu_load_1m')|float(0) -%}{%- set m5 = states('sensor.opnsense_cpu_load_5m')|float(0) -%}{%- set m15 = states('sensor.opnsense_cpu_load_15m')|float(0) -%}{%- set c = states('sensor.opnsense_cpu_logical_cores')|int(0) -%}{{- '1m: ' ~ m1 ~ ' • 5m: ' ~ m5 ~ ' • 15m: ' ~ m15 ~ ' • ' ~ c ~ ' cores' -}}
  - trigger:
      - platform: homeassistant
        event: start
      - platform: state
        entity_id:
          - sensor.opnsense_memory_percent
          - sensor.opnsense_memory_used
          - sensor.opnsense_memory_total
          - sensor.opnsense_memory_available
    variables:
      entity_id: sensor.opnsense_memory_percent
      app: memory_utilization
      options:
        used: sensor.opnsense_memory_used
      state_info: >-
        {%- from 'main.jinja' import get_device_state_info -%}
        {{ get_device_state_info(entity_id, app, options=options) | from_json }}
      state_info_vertical: >-
        {%- from 'main.jinja' import get_device_state_info -%}
        {{ get_device_state_info(entity_id, app, layout='vertical', options=options) | from_json }}
    sensor:
      - name: ui_opnsense_memory_percent
        unique_id: 91cf8871-7052-52d8-9979-dc693648f95e
        state: "{{ states(entity_id) }}"
        attributes:
          value: "{{ state_info.value }}"
          name: "{{ state_info.name }}"
          default_desc: "{{ state_info.default_desc }}"
          short_desc: "{{ state_info.short_desc }}"
          full_desc: "{{ state_info.full_desc }}"
          icon: "{{ state_info.icon }}"
          color: "{{ state_info.color }}"
          badge: "{{ state_info.badge }}"
          badge_color: "{{ state_info.badge_color }}"
          label: "{{ state_info.label }}"
          short_label: "{{ state_info.short_label }}"
          long_label: "{{ state_info.long_label }}"
          label_vertical: "{{ state_info_vertical.label }}"
          short_label_vertical: "{{ state_info_vertical.short_label }}"
          long_label_vertical: "{{ state_info_vertical.long_label }}"
          app: memory_utilization
          device_class: none
          long_desc: >-
            {%- from 'units/base.jinja' import u_humanize_entity -%}{%- set mem_used = u_humanize_entity('sensor.opnsense_memory_used') -%}{%- set mem_total = u_humanize_entity('sensor.opnsense_memory_total') -%}{%- set mem_free = u_humanize_entity('sensor.opnsense_memory_available') -%}{{- 'Used: ' ~ mem_used ~ ' / ' ~ mem_total ~ '\nFree: ' ~ mem_free -}}
  - trigger:
      - platform: homeassistant
        event: start
      - platform: state
        entity_id: sensor.opnsense_uptime_s
    variables:
      entity_id: sensor.opnsense_uptime_s
      app: uptime
      options:
        device_class: duration
        uom: s
      state_info: >-
        {%- from 'main.jinja' import get_device_state_info -%}
        {{ get_device_state_info(entity_id, app, options=options) | from_json }}
      state_info_vertical: >-
        {%- from 'main.jinja' import get_device_state_info -%}
        {{ get_device_state_info(entity_id, app, layout='vertical', options=options) | from_json }}
    sensor:
      - name: ui_opnsense_uptime_s
        unique_id: 50553733-09f8-57a6-a0ef-873a7c451575
        state: "{{ states(entity_id) }}"
        attributes:
          value: "{{ state_info.value }}"
          name: "{{ state_info.name }}"
          default_desc: "{{ state_info.default_desc }}"
          short_desc: "{{ state_info.short_desc }}"
          full_desc: "{{ state_info.full_desc }}"
          icon: "{{ state_info.icon }}"
          color: "{{ state_info.color }}"
          badge: "{{ state_info.badge }}"
          badge_color: "{{ state_info.badge_color }}"
          label: "{{ state_info.label }}"
          short_label: "{{ state_info.short_label }}"
          long_label: "{{ state_info.long_label }}"
          label_vertical: "{{ state_info_vertical.label }}"
          short_label_vertical: "{{ state_info_vertical.short_label }}"
          long_label_vertical: "{{ state_info_vertical.long_label }}"
          app: uptime
          device_class: duration
  - trigger:
      - platform: homeassistant
        event: start
      - platform: state
        entity_id: sensor.opnsense_updates_pending
    variables:
      entity_id: sensor.opnsense_updates_pending
      app: count_pending
      options: {}
      state_info: >-
        {%- from 'main.jinja' import get_device_state_info -%}
        {{ get_device_state_info(entity_id, app, options=options) | from_json }}
      state_info_vertical: >-
        {%- from 'main.jinja' import get_device_state_info -%}
        {{ get_device_state_info(entity_id, app, layout='vertical', options=options) | from_json }}
    sensor:
      - name: ui_opnsense_updates_pending
        unique_id: 07032c70-d001-5b71-bc68-0a30f207d33c
        state: "{{ states(entity_id) }}"
        attributes:
          value: "{{ state_info.value }}"
          name: "{{ state_info.name }}"
          default_desc: "{{ state_info.default_desc }}"
          short_desc: "{{ state_info.short_desc }}"
          full_desc: "{{ state_info.full_desc }}"
          icon: "{{ state_info.icon }}"
          color: "{{ state_info.color }}"
          badge: "{{ state_info.badge }}"
          badge_color: "{{ state_info.badge_color }}"
          label: "{{ state_info.label }}"
          short_label: "{{ state_info.short_label }}"
          long_label: "{{ state_info.long_label }}"
          label_vertical: "{{ state_info_vertical.label }}"
          short_label_vertical: "{{ state_info_vertical.short_label }}"
          long_label_vertical: "{{ state_info_vertical.long_label }}"
          app: count_pending
          device_class: none
  - trigger:
      - platform: homeassistant
        event: start
      - platform: state
        entity_id:
          - binary_sensor.opnsense_status_alarm
          - sensor.opnsense_overall_status_summary
    variables:
      entity_id: binary_sensor.opnsense_status_alarm
      app: alarm
      options:
        device_class: binary
        summary: sensor.opnsense_overall_status_summary
      state_info: >-
        {%- from 'main.jinja' import get_device_state_info -%}
        {{ get_device_state_info(entity_id, app, options=options) | from_json }}
      state_info_vertical: >-
        {%- from 'main.jinja' import get_device_state_info -%}
        {{ get_device_state_info(entity_id, app, layout='vertical', options=options) | from_json }}
    sensor:
      - name: ui_opnsense_status_alarm
        unique_id: 32ab4621-6cfc-53db-b6a3-df992f905a71
        state: "{{ states(entity_id) }}"
        attributes:
          value: "{{ state_info.value }}"
          name: "{{ state_info.name }}"
          default_desc: "{{ state_info.default_desc }}"
          short_desc: "{{ state_info.short_desc }}"
          full_desc: "{{ state_info.full_desc }}"
          icon: "{{ state_info.icon }}"
          color: "{{ state_info.color }}"
          badge: "{{ state_info.badge }}"
          badge_color: "{{ state_info.badge_color }}"
          label: "{{ state_info.label }}"
          short_label: "{{ state_info.short_label }}"
          long_label: "{{ state_info.long_label }}"
          label_vertical: "{{ state_info_vertical.label }}"
          short_label_vertical: "{{ state_info_vertical.short_label }}"
          long_label_vertical: "{{ state_info_vertical.long_label }}"
          app: alarm
          device_class: binary
  - trigger:
      - platform: homeassistant
        event: start
      - platform: state
        entity_id:
          - sensor.opnsense_fs_root_percent
          - sensor.opnsense_fs_root_used
          - sensor.opnsense_fs_root_total
    variables:
      entity_id: sensor.opnsense_fs_root_percent
      app: disk_utilization
      options:
        free: sensor.opnsense_fs_root_free
      state_info: >-
        {%- from 'main.jinja' import get_device_state_info -%}
        {{ get_device_state_info(entity_id, app, options=options) | from_json }}
      state_info_vertical: >-
        {%- from 'main.jinja' import get_device_state_info -%}
        {{ get_device_state_info(entity_id, app, layout='vertical', options=options) | from_json }}
    sensor:
      - name: ui_opnsense_fs_root_percent
        unique_id: 89bec172-6a86-583d-b003-18085969b965
        state: "{{ states(entity_id) }}"
        attributes:
          value: "{{ state_info.value }}"
          name: "{{ state_info.name }}"
          default_desc: "{{ state_info.default_desc }}"
          short_desc: "{{ state_info.short_desc }}"
          full_desc: "{{ state_info.full_desc }}"
          icon: "{{ state_info.icon }}"
          color: "{{ state_info.color }}"
          badge: "{{ state_info.badge }}"
          badge_color: "{{ state_info.badge_color }}"
          label: "{{ state_info.label }}"
          short_label: "{{ state_info.short_label }}"
          long_label: "{{ state_info.long_label }}"
          label_vertical: "{{ state_info_vertical.label }}"
          short_label_vertical: "{{ state_info_vertical.short_label }}"
          long_label_vertical: "{{ state_info_vertical.long_label }}"
          app: disk_utilization
          device_class: none
          long_desc: >-
            {%- from 'units/base.jinja' import u_humanize_entity -%}{%- set total = u_humanize_entity('sensor.opnsense_fs_root_total') -%}{%- set used = u_humanize_entity('sensor.opnsense_fs_root_used') -%}{{- 'Total: ' ~ total ~ ' • Used: ' ~ used -}}
