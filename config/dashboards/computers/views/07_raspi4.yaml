# -----------------------------------------------------------------------------
# Computers Dashboard: RasPi4 View
# -----------------------------------------------------------------------------
#
# Auto-generated by generate_hwmon_assets.py
# DO NOT EDIT - Changes will be overwritten
#
# -----------------------------------------------------------------------------

title: RasPi4
path: raspi4
type: sections
max_columns: 3
cards: []

badges:
  - type: custom:mushroom-template-badge
    entity: binary_sensor.raspi4_status_alarm
    icon: "{{ state_attr('sensor.ui_raspi4_status_alarm', 'icon') }}"
    color: "{{ state_attr('sensor.ui_raspi4_status_alarm', 'color') }}"
    label: Status
    content: "{{ state_attr('sensor.ui_raspi4_status_alarm', 'short_label') }}"
    tap_action:
      action: more-info
  - type: custom:mushroom-template-badge
    entity: sensor.raspi4_uptime_s
    icon: "{{ state_attr('sensor.ui_raspi4_uptime_s', 'icon') }}"
    color: "{{ state_attr('sensor.ui_raspi4_uptime_s', 'color') }}"
    label: Uptime
    content: "{{ state_attr('sensor.ui_raspi4_uptime_s', 'short_label') }}"
    tap_action:
      action: more-info
  - type: custom:mushroom-template-badge
    entity: sensor.raspi4_cpu_load_1m
    icon: "{{ state_attr('sensor.ui_raspi4_cpu_load_1m', 'icon') }}"
    color: "{{ state_attr('sensor.ui_raspi4_cpu_load_1m', 'color') }}"
    label: CPU Load
    content: "{{ state_attr('sensor.ui_raspi4_cpu_load_1m', 'short_label') }}"
    tap_action:
      action: more-info
  - type: custom:mushroom-template-badge
    entity: sensor.raspi4_cpu_utilization
    icon: "{{ state_attr('sensor.ui_raspi4_cpu_utilization', 'icon') }}"
    color: "{{ state_attr('sensor.ui_raspi4_cpu_utilization', 'color') }}"
    label: CPU
    content: "{{ state_attr('sensor.ui_raspi4_cpu_utilization', 'short_label') }}"
    tap_action:
      action: more-info
  - type: custom:mushroom-template-badge
    entity: sensor.raspi4_cpu_temp
    icon: "{{ state_attr('sensor.ui_raspi4_cpu_temp', 'icon') }}"
    color: "{{ state_attr('sensor.ui_raspi4_cpu_temp', 'color') }}"
    label: CPU Temp
    content: "{{ state_attr('sensor.ui_raspi4_cpu_temp', 'short_label') }}"
    tap_action:
      action: more-info
  - type: custom:mushroom-template-badge
    entity: sensor.raspi4_memory_percent
    icon: "{{ state_attr('sensor.ui_raspi4_memory_percent', 'icon') }}"
    color: "{{ state_attr('sensor.ui_raspi4_memory_percent', 'color') }}"
    label: Memory
    content: "{{ state_attr('sensor.ui_raspi4_memory_percent', 'short_label') }}"
    tap_action:
      action: more-info

sections:

  - type: grid
    column_span: 1
    cards:
      - type: vertical-stack
        cards:
          - type: custom:mushroom-title-card
            title: System Status
            subtitle: Health overview and pending updates
          - type: markdown
            content: |
              {% set packages = state_attr('sensor.raspi4_updates_list', 'packages') | default([]) %}
              **Available Updates:**
              {%- for pkg in packages %}
                - {{ pkg }}
              {%- endfor %}
            visibility:
              - condition: numeric_state
                entity: sensor.raspi4_updates_pending
                above: 0
            card_mod:
              style: |
                ha-card {
                  background: rgba(var(--rgb-primary-color), 0.1);
                  font-size: 0.9em;
                }
          - type: custom:mushroom-template-card
            entity: binary_sensor.raspi4_status_alarm
            icon: "{{ state_attr('sensor.ui_raspi4_status_alarm', 'icon') }}"
            icon_color: "{{ state_attr('sensor.ui_raspi4_status_alarm', 'color') }}"
            primary: Health Status
            secondary: "{{ state_attr('sensor.ui_raspi4_status_alarm', 'long_desc') }}"
            tap_action:
              action: more-info

  - type: grid
    column_span: 1
    cards:
      - type: vertical-stack
        cards:
          - type: custom:mushroom-title-card
            title: CPU
          - type: custom:mushroom-template-card
            entity: sensor.raspi4_cpu_load_1m
            icon: "{{ state_attr('sensor.ui_raspi4_cpu_load_1m', 'icon') }}"
            icon_color: "{{ state_attr('sensor.ui_raspi4_cpu_load_1m', 'color') }}"
            primary: CPU Load (1m/5m/15m)
            secondary: "{{ state_attr('sensor.ui_raspi4_cpu_load_1m', 'long_desc') }}"
            tap_action:
              action: more-info
          - type: custom:mushroom-template-card
            entity: sensor.raspi4_cpu_temp
            icon: "{{ state_attr('sensor.ui_raspi4_cpu_temp', 'icon') }}"
            icon_color: "{{ state_attr('sensor.ui_raspi4_cpu_temp', 'color') }}"
            primary: CPU Temperature
            secondary: "{{ state_attr('sensor.ui_raspi4_cpu_temp', 'label') }}"
            tap_action:
              action: more-info

  - type: grid
    column_span: 1
    cards:
      - type: vertical-stack
        cards:
          - type: custom:mushroom-title-card
            title: Memory
          - type: custom:mushroom-template-card
            entity: sensor.raspi4_swap_percent
            icon: "{{ state_attr('sensor.ui_raspi4_swap_percent', 'icon') }}"
            icon_color: "{{ state_attr('sensor.ui_raspi4_swap_percent', 'color') }}"
            primary: "Swap ({{ state_attr('sensor.ui_raspi4_swap_percent', 'value') }} used)"
            secondary: "{{ state_attr('sensor.ui_raspi4_swap_percent', 'long_desc') }}"
            tap_action:
              action: more-info

  - type: grid
    column_span: 1
    cards:
      - type: vertical-stack
        cards:
          - type: custom:mushroom-title-card
            title: Storage
            subtitle: Disk capacity and performance
          - type: custom:mushroom-template-card
            entity: sensor.raspi4_fs_root_percent
            icon: "{{ state_attr('sensor.ui_raspi4_fs_root', 'fs_icon') | default('mdi:folder') }}"
            icon_color: "{{ state_attr('sensor.ui_raspi4_fs_root', 'fs_color') | default('green') }}"
            primary: Root
            secondary: "{{ state_attr('sensor.ui_raspi4_fs_root', 'fs_secondary') | default('') }}"
            multiline_secondary: true
            tap_action:
              action: more-info
          - type: custom:mushroom-template-card
            entity: sensor.raspi4_fs_boot_percent
            icon: "{{ state_attr('sensor.ui_raspi4_fs_boot', 'fs_icon') | default('mdi:folder') }}"
            icon_color: "{{ state_attr('sensor.ui_raspi4_fs_boot', 'fs_color') | default('green') }}"
            primary: Boot
            secondary: "{{ state_attr('sensor.ui_raspi4_fs_boot', 'fs_secondary') | default('') }}"
            multiline_secondary: true
            tap_action:
              action: more-info

  - type: grid
    column_span: 1
    cards:
      - type: vertical-stack
        cards:
          - type: custom:mushroom-title-card
            title: Network
            subtitle: Interface status and connectivity
          - type: custom:mushroom-template-card
            entity: sensor.raspi4_ethernet_info
            icon: "{{ state_attr('sensor.ui_raspi4_network_ethernet','iface_icon') }}"
            icon_color: "{{ state_attr('sensor.ui_raspi4_network_ethernet','iface_color') }}"
            primary: "{{ state_attr('sensor.raspi4_ethernet_info', 'name') }}"
            secondary: "{{ state_attr('sensor.ui_raspi4_network_ethernet','long_desc') }}"
            multiline_secondary: true
            tap_action:
              action: more-info

  - type: grid
    column_span: 1
    cards:
      - type: vertical-stack
        cards:
          - type: custom:mushroom-title-card
            title: Services
            subtitle: Monitored system services
          - type: custom:mushroom-template-card
            entity: binary_sensor.raspi4_services_ok
            icon: "{{ state_attr('sensor.ui_raspi4_services_status', 'services_icon') }}"
            icon_color: "{{ state_attr('sensor.ui_raspi4_services_status', 'services_color') }}"
            primary: "{{ state_attr('sensor.ui_raspi4_services_status', 'services_primary') }}"
            secondary: "{{ state_attr('sensor.ui_raspi4_services_status', 'services_secondary') }}"
            tap_action:
              action: more-info
