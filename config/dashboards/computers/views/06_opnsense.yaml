# -----------------------------------------------------------------------------
# Computers Dashboard: OPNsense View
# -----------------------------------------------------------------------------
#
# Auto-generated by generate_hwmon_assets.py
# DO NOT EDIT - Changes will be overwritten
#
# -----------------------------------------------------------------------------

title: OPNsense
path: opnsense
type: sections
max_columns: 3
cards: []

badges:
  - type: custom:mushroom-template-badge
    entity: binary_sensor.opnsense_status_alarm
    icon: "{{ state_attr('sensor.ui_opnsense_status_alarm', 'icon') }}"
    color: "{{ state_attr('sensor.ui_opnsense_status_alarm', 'color') }}"
    label: Status
    content: "{{ state_attr('sensor.ui_opnsense_status_alarm', 'short_label') }}"
    tap_action:
      action: more-info
  - type: custom:mushroom-template-badge
    entity: sensor.opnsense_uptime_s
    icon: "{{ state_attr('sensor.ui_opnsense_uptime_s', 'icon') }}"
    color: "{{ state_attr('sensor.ui_opnsense_uptime_s', 'color') }}"
    label: Uptime
    content: "{{ state_attr('sensor.ui_opnsense_uptime_s', 'short_label') }}"
    tap_action:
      action: more-info
  - type: custom:mushroom-template-badge
    entity: sensor.opnsense_cpu_load_1m
    icon: "{{ state_attr('sensor.ui_opnsense_cpu_load_1m', 'icon') }}"
    color: "{{ state_attr('sensor.ui_opnsense_cpu_load_1m', 'color') }}"
    label: CPU Load
    content: "{{ state_attr('sensor.ui_opnsense_cpu_load_1m', 'short_label') }}"
    tap_action:
      action: more-info
  - type: custom:mushroom-template-badge
    entity: sensor.opnsense_cpu_utilization
    icon: "{{ state_attr('sensor.ui_opnsense_cpu_utilization', 'icon') }}"
    color: "{{ state_attr('sensor.ui_opnsense_cpu_utilization', 'color') }}"
    label: CPU
    content: "{{ state_attr('sensor.ui_opnsense_cpu_utilization', 'short_label') }}"
    tap_action:
      action: more-info
  - type: custom:mushroom-template-badge
    entity: sensor.opnsense_cpu_temp
    icon: "{{ state_attr('sensor.ui_opnsense_cpu_temp', 'icon') }}"
    color: "{{ state_attr('sensor.ui_opnsense_cpu_temp', 'color') }}"
    label: CPU Temp
    content: "{{ state_attr('sensor.ui_opnsense_cpu_temp', 'short_label') }}"
    tap_action:
      action: more-info
  - type: custom:mushroom-template-badge
    entity: sensor.opnsense_memory_percent
    icon: "{{ state_attr('sensor.ui_opnsense_memory_percent', 'icon') }}"
    color: "{{ state_attr('sensor.ui_opnsense_memory_percent', 'color') }}"
    label: Memory
    content: "{{ state_attr('sensor.ui_opnsense_memory_percent', 'short_label') }}"
    tap_action:
      action: more-info

sections:

  - type: grid
    column_span: 1
    cards:
      - type: vertical-stack
        cards:
          - type: custom:mushroom-title-card
            title: System Status
            subtitle: Health overview and pending updates
          - type: markdown
            content: |
              {% set packages = state_attr('sensor.opnsense_updates_list', 'packages') | default([]) %}
              **Available Updates:**
              {%- for pkg in packages %}
                - {{ pkg }}
              {%- endfor %}
            visibility:
              - condition: numeric_state
                entity: sensor.opnsense_updates_pending
                above: 0
            card_mod:
              style: |
                ha-card {
                  background: rgba(var(--rgb-primary-color), 0.1);
                  font-size: 0.9em;
                }
          - type: custom:mushroom-template-card
            entity: binary_sensor.opnsense_status_alarm
            icon: "{{ state_attr('sensor.ui_opnsense_status_alarm', 'icon') }}"
            icon_color: "{{ state_attr('sensor.ui_opnsense_status_alarm', 'color') }}"
            primary: Health Status
            secondary: "{{ state_attr('sensor.ui_opnsense_status_alarm', 'long_desc') }}"
            tap_action:
              action: more-info

  - type: grid
    column_span: 1
    cards:
      - type: vertical-stack
        cards:
          - type: custom:mushroom-title-card
            title: CPU
          - type: custom:mushroom-template-card
            entity: sensor.opnsense_cpu_load_1m
            icon: "{{ state_attr('sensor.ui_opnsense_cpu_load_1m', 'icon') }}"
            icon_color: "{{ state_attr('sensor.ui_opnsense_cpu_load_1m', 'color') }}"
            primary: CPU Load (1m/5m/15m)
            secondary: "{{ state_attr('sensor.ui_opnsense_cpu_load_1m', 'long_desc') }}"
            tap_action:
              action: more-info

  - type: grid
    column_span: 1
    cards:
      - type: vertical-stack
        cards:
          - type: custom:mushroom-title-card
            title: Memory
          - type: custom:mushroom-template-card
            entity: sensor.opnsense_swap_percent
            icon: "{{ state_attr('sensor.ui_opnsense_swap_percent', 'icon') }}"
            icon_color: "{{ state_attr('sensor.ui_opnsense_swap_percent', 'color') }}"
            primary: "Swap ({{ state_attr('sensor.ui_opnsense_swap_percent', 'value') }} used)"
            secondary: "{{ state_attr('sensor.ui_opnsense_swap_percent', 'long_desc') }}"
            tap_action:
              action: more-info

  - type: grid
    column_span: 1
    cards:
      - type: vertical-stack
        cards:
          - type: custom:mushroom-title-card
            title: Storage
            subtitle: Disk capacity and performance
          - type: custom:mushroom-template-card
            entity: sensor.opnsense_fs_root_percent
            icon: "{{ state_attr('sensor.ui_opnsense_fs_root', 'fs_icon') | default('mdi:folder') }}"
            icon_color: "{{ state_attr('sensor.ui_opnsense_fs_root', 'fs_color') | default('green') }}"
            primary: Root
            secondary: "{{ state_attr('sensor.ui_opnsense_fs_root', 'fs_secondary') | default('') }}"
            multiline_secondary: true
            tap_action:
              action: more-info
          - type: custom:mushroom-template-card
            entity: sensor.opnsense_fs_logs_percent
            icon: "{{ state_attr('sensor.ui_opnsense_fs_logs', 'fs_icon') | default('mdi:folder') }}"
            icon_color: "{{ state_attr('sensor.ui_opnsense_fs_logs', 'fs_color') | default('green') }}"
            primary: Logs
            secondary: "{{ state_attr('sensor.ui_opnsense_fs_logs', 'fs_secondary') | default('') }}"
            multiline_secondary: true
            tap_action:
              action: more-info

  - type: grid
    column_span: 1
    cards:
      - type: vertical-stack
        cards:
          - type: custom:mushroom-title-card
            title: Network
            subtitle: Interface status and connectivity
          - type: custom:mushroom-template-card
            entity: sensor.opnsense_lan_info
            icon: "{{ state_attr('sensor.ui_opnsense_network_lan','iface_icon') }}"
            icon_color: "{{ state_attr('sensor.ui_opnsense_network_lan','iface_color') }}"
            primary: "{{ state_attr('sensor.opnsense_lan_info', 'name') }}"
            secondary: "{{ state_attr('sensor.ui_opnsense_network_lan','long_desc') }}"
            multiline_secondary: true
            tap_action:
              action: more-info
          - type: custom:mushroom-template-card
            entity: sensor.opnsense_wan_info
            icon: "{{ state_attr('sensor.ui_opnsense_network_wan','iface_icon') }}"
            icon_color: "{{ state_attr('sensor.ui_opnsense_network_wan','iface_color') }}"
            primary: "{{ state_attr('sensor.opnsense_wan_info', 'name') }}"
            secondary: "{{ state_attr('sensor.ui_opnsense_network_wan','long_desc') }}"
            multiline_secondary: true
            tap_action:
              action: more-info
          - type: custom:mushroom-template-card
            entity: sensor.opnsense_vlan10_info
            icon: "{{ state_attr('sensor.ui_opnsense_network_vlan10','iface_icon') }}"
            icon_color: "{{ state_attr('sensor.ui_opnsense_network_vlan10','iface_color') }}"
            primary: "{{ state_attr('sensor.opnsense_vlan10_info', 'name') }}"
            secondary: "{{ state_attr('sensor.ui_opnsense_network_vlan10','long_desc') }}"
            multiline_secondary: true
            tap_action:
              action: more-info
          - type: custom:mushroom-template-card
            entity: sensor.opnsense_vlan20_info
            icon: "{{ state_attr('sensor.ui_opnsense_network_vlan20','iface_icon') }}"
            icon_color: "{{ state_attr('sensor.ui_opnsense_network_vlan20','iface_color') }}"
            primary: "{{ state_attr('sensor.opnsense_vlan20_info', 'name') }}"
            secondary: "{{ state_attr('sensor.ui_opnsense_network_vlan20','long_desc') }}"
            multiline_secondary: true
            tap_action:
              action: more-info
          - type: custom:mushroom-template-card
            entity: sensor.opnsense_wireguard_info
            icon: "{{ state_attr('sensor.ui_opnsense_network_wireguard','iface_icon') }}"
            icon_color: "{{ state_attr('sensor.ui_opnsense_network_wireguard','iface_color') }}"
            primary: "{{ state_attr('sensor.opnsense_wireguard_info', 'name') }}"
            secondary: "{{ state_attr('sensor.ui_opnsense_network_wireguard','long_desc') }}"
            multiline_secondary: true
            tap_action:
              action: more-info
          - type: custom:mushroom-template-card
            entity: sensor.opnsense_tailscale_info
            icon: "{{ state_attr('sensor.ui_opnsense_network_tailscale','iface_icon') }}"
            icon_color: "{{ state_attr('sensor.ui_opnsense_network_tailscale','iface_color') }}"
            primary: "{{ state_attr('sensor.opnsense_tailscale_info', 'name') }}"
            secondary: "{{ state_attr('sensor.ui_opnsense_network_tailscale','long_desc') }}"
            multiline_secondary: true
            tap_action:
              action: more-info

  - type: grid
    column_span: 1
    cards:
      - type: vertical-stack
        cards:
          - type: custom:mushroom-title-card
            title: Services
            subtitle: Monitored system services
          - type: custom:mushroom-template-card
            entity: binary_sensor.opnsense_services_ok
            icon: "{{ state_attr('sensor.ui_opnsense_services_status', 'services_icon') }}"
            icon_color: "{{ state_attr('sensor.ui_opnsense_services_status', 'services_color') }}"
            primary: "{{ state_attr('sensor.ui_opnsense_services_status', 'services_primary') }}"
            secondary: "{{ state_attr('sensor.ui_opnsense_services_status', 'services_secondary') }}"
            tap_action:
              action: more-info
