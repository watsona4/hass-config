# -----------------------------------------------------------------------------
# Computers Dashboard: Frigate View
# -----------------------------------------------------------------------------
#
# Auto-generated by generate_hwmon_assets.py
# DO NOT EDIT - Changes will be overwritten
#
# -----------------------------------------------------------------------------

title: Frigate
path: frigate
type: sections
max_columns: 3
cards: []

badges:
  - type: custom:mushroom-template-badge
    entity: binary_sensor.frigate_status_alarm
    icon: "{{ state_attr('sensor.ui_frigate_status_alarm', 'icon') }}"
    color: "{{ state_attr('sensor.ui_frigate_status_alarm', 'color') }}"
    label: Status
    content: "{{ state_attr('sensor.ui_frigate_status_alarm', 'short_label') }}"
    tap_action:
      action: more-info
  - type: custom:mushroom-template-badge
    entity: sensor.frigate_uptime_s
    icon: "{{ state_attr('sensor.ui_frigate_uptime_s', 'icon') }}"
    color: "{{ state_attr('sensor.ui_frigate_uptime_s', 'color') }}"
    label: Uptime
    content: "{{ state_attr('sensor.ui_frigate_uptime_s', 'short_label') }}"
    tap_action:
      action: more-info
  - type: custom:mushroom-template-badge
    entity: sensor.frigate_cpu_load_1m
    icon: "{{ state_attr('sensor.ui_frigate_cpu_load_1m', 'icon') }}"
    color: "{{ state_attr('sensor.ui_frigate_cpu_load_1m', 'color') }}"
    label: CPU Load
    content: "{{ state_attr('sensor.ui_frigate_cpu_load_1m', 'short_label') }}"
    tap_action:
      action: more-info
  - type: custom:mushroom-template-badge
    entity: sensor.frigate_cpu_utilization
    icon: "{{ state_attr('sensor.ui_frigate_cpu_utilization', 'icon') }}"
    color: "{{ state_attr('sensor.ui_frigate_cpu_utilization', 'color') }}"
    label: CPU
    content: "{{ state_attr('sensor.ui_frigate_cpu_utilization', 'short_label') }}"
    tap_action:
      action: more-info
  - type: custom:mushroom-template-badge
    entity: sensor.frigate_cpu_temp
    icon: "{{ state_attr('sensor.ui_frigate_cpu_temp', 'icon') }}"
    color: "{{ state_attr('sensor.ui_frigate_cpu_temp', 'color') }}"
    label: CPU Temp
    content: "{{ state_attr('sensor.ui_frigate_cpu_temp', 'short_label') }}"
    tap_action:
      action: more-info
  - type: custom:mushroom-template-badge
    entity: sensor.frigate_memory_percent
    icon: "{{ state_attr('sensor.ui_frigate_memory_percent', 'icon') }}"
    color: "{{ state_attr('sensor.ui_frigate_memory_percent', 'color') }}"
    label: Memory
    content: "{{ state_attr('sensor.ui_frigate_memory_percent', 'short_label') }}"
    tap_action:
      action: more-info

sections:

  - type: grid
    column_span: 1
    cards:
      - type: vertical-stack
        cards:
          - type: custom:mushroom-title-card
            title: System Status
            subtitle: Health overview and pending updates
          - type: markdown
            content: |
              {% set packages = state_attr('sensor.frigate_updates_list', 'packages') | default([]) %}
              **Available Updates:**
              {%- for pkg in packages %}
                - {{ pkg }}
              {%- endfor %}
            visibility:
              - condition: numeric_state
                entity: sensor.frigate_updates_pending
                above: 0
            card_mod:
              style: |
                ha-card {
                  background: rgba(var(--rgb-primary-color), 0.1);
                  font-size: 0.9em;
                }
          - type: custom:mushroom-template-card
            entity: binary_sensor.frigate_status_alarm
            icon: "{{ state_attr('sensor.ui_frigate_status_alarm', 'icon') }}"
            icon_color: "{{ state_attr('sensor.ui_frigate_status_alarm', 'color') }}"
            primary: Health Status
            secondary: "{{ state_attr('sensor.ui_frigate_status_alarm', 'long_desc') }}"
            tap_action:
              action: more-info

  - type: grid
    column_span: 1
    cards:
      - type: vertical-stack
        cards:
          - type: custom:mushroom-title-card
            title: CPU
          - type: custom:mushroom-template-card
            entity: sensor.frigate_cpu_load_1m
            icon: "{{ state_attr('sensor.ui_frigate_cpu_load_1m', 'icon') }}"
            icon_color: "{{ state_attr('sensor.ui_frigate_cpu_load_1m', 'color') }}"
            primary: CPU Load (1m/5m/15m)
            secondary: "{{ state_attr('sensor.ui_frigate_cpu_load_1m', 'long_desc') }}"
            tap_action:
              action: more-info
          - type: custom:mushroom-template-card
            entity: sensor.frigate_cpu_temp
            icon: "{{ state_attr('sensor.ui_frigate_cpu_temp', 'icon') }}"
            icon_color: "{{ state_attr('sensor.ui_frigate_cpu_temp', 'color') }}"
            primary: CPU Temperature
            secondary: "{{ state_attr('sensor.ui_frigate_cpu_temp', 'label') }}"
            tap_action:
              action: more-info

  - type: grid
    column_span: 1
    cards:
      - type: vertical-stack
        cards:
          - type: custom:mushroom-title-card
            title: Memory
          - type: custom:mushroom-template-card
            entity: sensor.frigate_swap_percent
            icon: "{{ state_attr('sensor.ui_frigate_swap_percent', 'icon') }}"
            icon_color: "{{ state_attr('sensor.ui_frigate_swap_percent', 'color') }}"
            primary: "Swap ({{ state_attr('sensor.ui_frigate_swap_percent', 'value') }} used)"
            secondary: "{{ state_attr('sensor.ui_frigate_swap_percent', 'long_desc') }}"
            tap_action:
              action: more-info

  - type: grid
    column_span: 1
    cards:
      - type: vertical-stack
        cards:
          - type: custom:mushroom-title-card
            title: Storage
            subtitle: Disk capacity and performance
          - type: custom:mushroom-template-card
            entity: sensor.frigate_fs_root_percent
            icon: "{{ state_attr('sensor.ui_frigate_fs_root', 'fs_icon') | default('mdi:folder') }}"
            icon_color: "{{ state_attr('sensor.ui_frigate_fs_root', 'fs_color') | default('green') }}"
            primary: Root
            secondary: "{{ state_attr('sensor.ui_frigate_fs_root', 'fs_secondary') | default('') }}"
            multiline_secondary: true
            tap_action:
              action: more-info
          - type: custom:mushroom-template-card
            entity: sensor.frigate_fs_home_percent
            icon: "{{ state_attr('sensor.ui_frigate_fs_home', 'fs_icon') | default('mdi:folder') }}"
            icon_color: "{{ state_attr('sensor.ui_frigate_fs_home', 'fs_color') | default('green') }}"
            primary: Home
            secondary: "{{ state_attr('sensor.ui_frigate_fs_home', 'fs_secondary') | default('') }}"
            multiline_secondary: true
            tap_action:
              action: more-info
          - type: custom:mushroom-template-card
            entity: sensor.frigate_fs_var_percent
            icon: "{{ state_attr('sensor.ui_frigate_fs_var', 'fs_icon') | default('mdi:folder') }}"
            icon_color: "{{ state_attr('sensor.ui_frigate_fs_var', 'fs_color') | default('green') }}"
            primary: Var
            secondary: "{{ state_attr('sensor.ui_frigate_fs_var', 'fs_secondary') | default('') }}"
            multiline_secondary: true
            tap_action:
              action: more-info
          - type: custom:mushroom-template-card
            entity: sensor.frigate_fs_docker_percent
            icon: "{{ state_attr('sensor.ui_frigate_fs_docker', 'fs_icon') | default('mdi:folder') }}"
            icon_color: "{{ state_attr('sensor.ui_frigate_fs_docker', 'fs_color') | default('green') }}"
            primary: Docker
            secondary: "{{ state_attr('sensor.ui_frigate_fs_docker', 'fs_secondary') | default('') }}"
            multiline_secondary: true
            tap_action:
              action: more-info
          - type: custom:mushroom-template-card
            entity: sensor.frigate_fs_postgresql_percent
            icon: "{{ state_attr('sensor.ui_frigate_fs_postgresql', 'fs_icon') | default('mdi:folder') }}"
            icon_color: "{{ state_attr('sensor.ui_frigate_fs_postgresql', 'fs_color') | default('green') }}"
            primary: PostgreSQL
            secondary: "{{ state_attr('sensor.ui_frigate_fs_postgresql', 'fs_secondary') | default('') }}"
            multiline_secondary: true
            tap_action:
              action: more-info
          - type: custom:mushroom-template-card
            entity: sensor.frigate_fs_frigate_hot_percent
            icon: "{{ state_attr('sensor.ui_frigate_fs_frigate_hot', 'fs_icon') | default('mdi:folder') }}"
            icon_color: "{{ state_attr('sensor.ui_frigate_fs_frigate_hot', 'fs_color') | default('green') }}"
            primary: Frigate Hot
            secondary: "{{ state_attr('sensor.ui_frigate_fs_frigate_hot', 'fs_secondary') | default('') }}"
            multiline_secondary: true
            tap_action:
              action: more-info
          - type: custom:mushroom-template-card
            entity: sensor.frigate_fs_data_percent
            icon: "{{ state_attr('sensor.ui_frigate_fs_data', 'fs_icon') | default('mdi:folder') }}"
            icon_color: "{{ state_attr('sensor.ui_frigate_fs_data', 'fs_color') | default('green') }}"
            primary: Data
            secondary: "{{ state_attr('sensor.ui_frigate_fs_data', 'fs_secondary') | default('') }}"
            multiline_secondary: true
            tap_action:
              action: more-info

  - type: grid
    column_span: 1
    cards:
      - type: vertical-stack
        cards:
          - type: custom:mushroom-title-card
            title: Network
            subtitle: Interface status and connectivity
          - type: custom:mushroom-template-card
            entity: sensor.frigate_ethernet_info
            icon: "{{ state_attr('sensor.ui_frigate_network_ethernet','iface_icon') }}"
            icon_color: "{{ state_attr('sensor.ui_frigate_network_ethernet','iface_color') }}"
            primary: "{{ state_attr('sensor.frigate_ethernet_info', 'name') }}"
            secondary: "{{ state_attr('sensor.ui_frigate_network_ethernet','long_desc') }}"
            multiline_secondary: true
            tap_action:
              action: more-info
          - type: custom:mushroom-template-card
            entity: sensor.frigate_tailscale_info
            icon: "{{ state_attr('sensor.ui_frigate_network_tailscale','iface_icon') }}"
            icon_color: "{{ state_attr('sensor.ui_frigate_network_tailscale','iface_color') }}"
            primary: "{{ state_attr('sensor.frigate_tailscale_info', 'name') }}"
            secondary: "{{ state_attr('sensor.ui_frigate_network_tailscale','long_desc') }}"
            multiline_secondary: true
            tap_action:
              action: more-info

  - type: grid
    column_span: 1
    cards:
      - type: vertical-stack
        cards:
          - type: custom:mushroom-title-card
            title: Services
            subtitle: Monitored system services
          - type: custom:mushroom-template-card
            entity: binary_sensor.frigate_services_ok
            icon: mdi:cog
            icon_color: "{{ 'green' if is_state('binary_sensor.frigate_services_ok', 'on') else 'red' }}"
            primary: Services Status
            secondary: "{{ states('sensor.frigate_services_checked') }} monitored"
            tap_action:
              action: more-info

  - type: grid
    column_span: 1
    cards:
      - type: vertical-stack
        cards:
          - type: custom:mushroom-title-card
            title: Containers
            subtitle: Docker container health
          - type: custom:mushroom-template-card
            entity: binary_sensor.frigate_containers_ok
            icon: mdi:docker
            icon_color: "{{ 'green' if is_state('binary_sensor.frigate_containers_ok', 'on') else 'red' }}"
            primary: Containers Status
            secondary: "{{ states('sensor.frigate_containers_checked') }} monitored"
            tap_action:
              action: more-info

  - type: grid
    column_span: 1
    cards:
      - type: vertical-stack
        cards:
          - type: custom:mushroom-title-card
            title: Hardware
          - type: custom:mushroom-template-card
            entity: sensor.frigate_coral_tpu_temp
            icon: "{{ state_attr('sensor.ui_frigate_coral_tpu_temp', 'icon') }}"
            icon_color: "{{ state_attr('sensor.ui_frigate_coral_tpu_temp', 'color') }}"
            primary: TPU Temperature
            secondary: "{{ state_attr('sensor.ui_frigate_coral_tpu_temp', 'label') }}"
            tap_action:
              action: more-info
