# -----------------------------------------------------------------------------
# Computers Dashboard: Frigate View
# -----------------------------------------------------------------------------
#
# Auto-generated by generate_hwmon_assets.py
# DO NOT EDIT - Changes will be overwritten
#
# -----------------------------------------------------------------------------

title: Frigate
path: frigate
type: sections
max_columns: 2

badges:
  - type: custom:mushroom-template-badge
    entity: sensor.frigate_cpu_utilization
    icon: "{{ state_attr('sensor.ui_frigate_cpu_utilization', 'icon') }}"
    color: "{{ state_attr('sensor.ui_frigate_cpu_utilization', 'color') }}"
    content: "{{ state_attr('sensor.ui_frigate_cpu_utilization', 'short_label') }}"
    tap_action:
      action: more-info
    icon_tap_action:
      action: more-info
  - type: custom:mushroom-template-badge
    entity: sensor.frigate_memory_percent
    icon: "{{ state_attr('sensor.ui_frigate_memory_percent', 'icon') }}"
    color: "{{ state_attr('sensor.ui_frigate_memory_percent', 'color') }}"
    content: "{{ state_attr('sensor.ui_frigate_memory_percent', 'short_label') }}"
    tap_action:
      action: more-info

    icon_tap_action:
      action: more-info
sections:
  # System Status
  - type: grid
    column_span: 2
    cards:
      - type: custom:mushroom-title-card
        title: Frigate
        subtitle: "{{ states('sensor.frigate_overall_status_summary') }}"
      - type: horizontal-stack
        cards:
          - type: custom:mushroom-template-card
            entity: sensor.frigate_uptime_s
            icon: "{{ state_attr('sensor.ui_frigate_uptime_s', 'icon') }}"
            icon_color: "{{ state_attr('sensor.ui_frigate_uptime_s', 'color') }}"
            primary: Uptime
            secondary: "{{ state_attr('sensor.ui_frigate_uptime_s', 'label_vertical') }}"
            layout: vertical
            multiline_secondary: true
            tap_action:
              action: more-info
            icon_tap_action:
              action: more-info
          - type: custom:mushroom-template-card
            entity: sensor.frigate_updates_pending
            icon: "{{ state_attr('sensor.ui_frigate_updates_pending', 'icon') }}"
            icon_color: "{{ state_attr('sensor.ui_frigate_updates_pending', 'color') }}"
            primary: Updates
            secondary: "{{ state_attr('sensor.ui_frigate_updates_pending', 'label_vertical') }}"
            layout: vertical
            multiline_secondary: true
            tap_action:
              action: more-info
            icon_tap_action:
              action: more-info
          - type: custom:mushroom-template-card
            entity: binary_sensor.frigate_status_alarm
            icon: "{{ state_attr('sensor.ui_frigate_status_alarm', 'icon') }}"
            icon_color: "{{ state_attr('sensor.ui_frigate_status_alarm', 'color') }}"
            primary: Status
            layout: vertical
            multiline_secondary: true
            tap_action:
              action: more-info
            icon_tap_action:
              action: more-info

  # CPU
  - type: grid
    column_span: 1
    cards:
      - type: heading
        heading: CPU
        heading_style: subtitle
      - type: custom:mushroom-template-card
        entity: sensor.frigate_cpu_utilization
        icon: "{{ state_attr('sensor.ui_frigate_cpu_utilization', 'icon') }}"
        icon_color: "{{ state_attr('sensor.ui_frigate_cpu_utilization', 'color') }}"
        primary: Utilization
        secondary: "{{ state_attr('sensor.ui_frigate_cpu_utilization', 'label') }}"
        tap_action:
          action: more-info
        icon_tap_action:
          action: more-info
      - type: custom:mushroom-template-card
        entity: sensor.frigate_cpu_load_1m
        icon: "{{ state_attr('sensor.ui_frigate_cpu_load_1m', 'icon') }}"
        icon_color: "{{ state_attr('sensor.ui_frigate_cpu_load_1m', 'color') }}"
        primary: Load Average
        secondary: "{{ state_attr('sensor.ui_frigate_cpu_load_1m', 'long_desc') }}"
        tap_action:
          action: more-info
        icon_tap_action:
          action: more-info
      - type: custom:mushroom-template-card
        entity: sensor.frigate_cpu_temp
        icon: "{{ state_attr('sensor.ui_frigate_cpu_temp', 'icon') }}"
        icon_color: "{{ state_attr('sensor.ui_frigate_cpu_temp', 'color') }}"
        primary: Temperature
        secondary: "{{ state_attr('sensor.ui_frigate_cpu_temp', 'label') }}"
        tap_action:
          action: more-info
        icon_tap_action:
          action: more-info

  # Memory
  - type: grid
    column_span: 1
    cards:
      - type: heading
        heading: Memory
        heading_style: subtitle
      - type: custom:mushroom-template-card
        entity: sensor.frigate_memory_percent
        icon: "{{ state_attr('sensor.ui_frigate_memory_percent', 'icon') }}"
        icon_color: "{{ state_attr('sensor.ui_frigate_memory_percent', 'color') }}"
        primary: Usage
        secondary: "{{ state_attr('sensor.ui_frigate_memory_percent', 'long_desc') }}"
        tap_action:
          action: more-info
        icon_tap_action:
          action: more-info
      - type: custom:mushroom-template-card
        entity: sensor.frigate_swap_percent
        icon: mdi:swap-horizontal
        icon_color: purple
        primary: Swap
        secondary: "{{ states('sensor.frigate_swap_percent') }}%"
        tap_action:
          action: more-info
        icon_tap_action:
          action: more-info

  # Filesystems
  - type: grid
    column_span: 2
    cards:
      - type: heading
        heading: Filesystems
        heading_style: subtitle
      - type: grid
        columns: 2
        square: false
        cards:
          - type: custom:mushroom-template-card
            entity: sensor.frigate_fs_root_percent
            icon: "{{ state_attr('sensor.ui_frigate_fs_root_percent', 'icon') }}"
            icon_color: "{{ state_attr('sensor.ui_frigate_fs_root_percent', 'color') }}"
            primary: Root
            secondary: "{{ state_attr('sensor.ui_frigate_fs_root_percent', 'long_desc') }}"
            tap_action:
              action: more-info
            icon_tap_action:
              action: more-info
          - type: custom:mushroom-template-card
            entity: sensor.frigate_fs_home_percent
            icon: "{{ state_attr('sensor.ui_frigate_fs_home_percent', 'icon') }}"
            icon_color: "{{ state_attr('sensor.ui_frigate_fs_home_percent', 'color') }}"
            primary: Home
            secondary: "{{ state_attr('sensor.ui_frigate_fs_home_percent', 'long_desc') }}"
            tap_action:
              action: more-info
            icon_tap_action:
              action: more-info
          - type: custom:mushroom-template-card
            entity: sensor.frigate_fs_var_percent
            icon: "{{ state_attr('sensor.ui_frigate_fs_var_percent', 'icon') }}"
            icon_color: "{{ state_attr('sensor.ui_frigate_fs_var_percent', 'color') }}"
            primary: Var
            secondary: "{{ state_attr('sensor.ui_frigate_fs_var_percent', 'long_desc') }}"
            tap_action:
              action: more-info
            icon_tap_action:
              action: more-info
          - type: custom:mushroom-template-card
            entity: sensor.frigate_fs_docker_percent
            icon: "{{ state_attr('sensor.ui_frigate_fs_docker_percent', 'icon') }}"
            icon_color: "{{ state_attr('sensor.ui_frigate_fs_docker_percent', 'color') }}"
            primary: Docker
            secondary: "{{ state_attr('sensor.ui_frigate_fs_docker_percent', 'long_desc') }}"
            tap_action:
              action: more-info
            icon_tap_action:
              action: more-info
          - type: custom:mushroom-template-card
            entity: sensor.frigate_fs_postgresql_percent
            icon: "{{ state_attr('sensor.ui_frigate_fs_postgresql_percent', 'icon') }}"
            icon_color: "{{ state_attr('sensor.ui_frigate_fs_postgresql_percent', 'color') }}"
            primary: PostgreSQL
            secondary: "{{ state_attr('sensor.ui_frigate_fs_postgresql_percent', 'long_desc') }}"
            tap_action:
              action: more-info
            icon_tap_action:
              action: more-info
          - type: custom:mushroom-template-card
            entity: sensor.frigate_fs_frigate_hot_percent
            icon: "{{ state_attr('sensor.ui_frigate_fs_frigate_hot_percent', 'icon') }}"
            icon_color: "{{ state_attr('sensor.ui_frigate_fs_frigate_hot_percent', 'color') }}"
            primary: Frigate Hot
            secondary: "{{ state_attr('sensor.ui_frigate_fs_frigate_hot_percent', 'long_desc') }}"
            tap_action:
              action: more-info
            icon_tap_action:
              action: more-info
          - type: custom:mushroom-template-card
            entity: sensor.frigate_fs_data_percent
            icon: "{{ state_attr('sensor.ui_frigate_fs_data_percent', 'icon') }}"
            icon_color: "{{ state_attr('sensor.ui_frigate_fs_data_percent', 'color') }}"
            primary: Data
            secondary: "{{ state_attr('sensor.ui_frigate_fs_data_percent', 'long_desc') }}"
            tap_action:
              action: more-info
            icon_tap_action:
              action: more-info

  # Network
  - type: grid
    column_span: 2
    cards:
      - type: heading
        heading: Network Interfaces
        heading_style: subtitle
      - type: grid
        columns: 2
        square: false
        cards:
          - type: custom:mushroom-template-card
            entity: sensor.frigate_ethernet_ipv4
            icon: mdi:ethernet
            icon_color: blue
            primary: Ethernet
            secondary: "{{ states('sensor.frigate_ethernet_ipv4') }}"
            tap_action:
              action: more-info
            icon_tap_action:
              action: more-info
          - type: custom:mushroom-template-card
            entity: sensor.frigate_tailscale_ipv4
            icon: mdi:ethernet
            icon_color: blue
            primary: Tailscale
            secondary: "{{ states('sensor.frigate_tailscale_ipv4') }}"
            tap_action:
              action: more-info
            icon_tap_action:
              action: more-info

  # Services
  - type: grid
    column_span: 1
    cards:
      - type: heading
        heading: Services
        heading_style: subtitle
      - type: custom:mushroom-template-card
        entity: binary_sensor.frigate_services_ok
        icon: mdi:cog
        icon_color: "{{ 'green' if is_state('binary_sensor.frigate_services_ok', 'on') else 'red' }}"
        primary: Services Status
        secondary: "{{ states('sensor.frigate_services_checked') }} monitored"
        tap_action:
          action: more-info
        icon_tap_action:
          action: more-info

  # Containers
  - type: grid
    column_span: 1
    cards:
      - type: heading
        heading: Containers
        heading_style: subtitle
      - type: custom:mushroom-template-card
        entity: binary_sensor.frigate_containers_ok
        icon: mdi:docker
        icon_color: "{{ 'green' if is_state('binary_sensor.frigate_containers_ok', 'on') else 'red' }}"
        primary: Containers Status
        secondary: "{{ states('sensor.frigate_containers_checked') }} monitored"
        tap_action:
          action: more-info
        icon_tap_action:
          action: more-info

  # GPU
  - type: grid
    column_span: 1
    cards:
      - type: heading
        heading: GPU
        heading_style: subtitle
      - type: custom:mushroom-template-card
        entity: sensor.frigate_gpu_utilization
        icon: "{{ state_attr('sensor.ui_frigate_gpu_utilization', 'icon') }}"
        icon_color: "{{ state_attr('sensor.ui_frigate_gpu_utilization', 'color') }}"
        primary: GPU Utilization
        secondary: "{{ state_attr('sensor.ui_frigate_gpu_utilization', 'label') }}"
        tap_action:
          action: more-info
        icon_tap_action:
          action: more-info

  # TPU (Coral)
  - type: grid
    column_span: 1
    cards:
      - type: heading
        heading: Coral TPU
        heading_style: subtitle
      - type: custom:mushroom-template-card
        entity: sensor.frigate_coral_tpu_temp
        icon: "{{ state_attr('sensor.ui_frigate_coral_tpu_temp', 'icon') }}"
        icon_color: "{{ state_attr('sensor.ui_frigate_coral_tpu_temp', 'color') }}"
        primary: TPU Temperature
        secondary: "{{ state_attr('sensor.ui_frigate_coral_tpu_temp', 'label') }}"
        tap_action:
          action: more-info
        icon_tap_action:
          action: more-info

  # Motherboard Temps
  - type: grid
    column_span: 1
    cards:
      - type: heading
        heading: Temperatures
        heading_style: subtitle
      - type: custom:mushroom-template-card
        entity: sensor.frigate_cpu_package
        icon: mdi:thermometer
        icon_color: "{{ 'red' if states('sensor.frigate_cpu_package') | float(0) > 80 else 'orange' if states('sensor.frigate_cpu_package') | float(0) > 60 else 'green' }}"
        primary: CPU Package
        secondary: "{{ states('sensor.frigate_cpu_package') }}°C"
        tap_action:
          action: more-info
        icon_tap_action:
          action: more-info
      - type: custom:mushroom-template-card
        entity: sensor.frigate_nvme
        icon: mdi:thermometer
        icon_color: "{{ 'red' if states('sensor.frigate_nvme') | float(0) > 80 else 'orange' if states('sensor.frigate_nvme') | float(0) > 60 else 'green' }}"
        primary: NVMe
        secondary: "{{ states('sensor.frigate_nvme') }}°C"
        tap_action:
          action: more-info
        icon_tap_action:
          action: more-info

  # Drives
  - type: grid
    column_span: 2
    cards:
      - type: heading
        heading: Storage
        heading_style: subtitle
      - type: grid
        columns: 2
        square: false
        cards:
          - type: custom:mushroom-template-card
            entity: sensor.frigate_nvme_ssd_transfer_rate
            icon: "{{ 'mdi:harddisk' if state_attr('sensor.frigate_nvme_ssd_info', 'type') == 'HDD' else 'mdi:flash' }}"
            icon_color: "{{ 'cyan' if states('sensor.frigate_nvme_ssd_transfer_rate') | int > 10485760 else 'blue' if states('sensor.frigate_nvme_ssd_transfer_rate') | int > 1048576 else 'grey' }}"
            primary: "{{ state_attr('sensor.frigate_nvme_ssd_info', 'model') | default('NVMe SSD') | truncate(25, True) }}"
            secondary: >-
              {%- set used = states('sensor.frigate_nvme_ssd_used') | float(0) -%}
              {%- set avail = states('sensor.frigate_nvme_ssd_available') | float(0) -%}}
              {%- set pct = states('sensor.frigate_nvme_ssd_pct_used') | float(0) -%}}
              {%- set temp = states('sensor.frigate_nvme_ssd_temp') -%}
              {{ used | round(0) }}G used · {{ avail | round(0) }}G free · {{ pct | round(0) }}%{% if temp not in ['unknown', 'unavailable'] %} · {{ temp }}°C{% endif %}
            multiline_secondary: true
            tap_action:
              action: more-info
          - type: custom:mushroom-template-card
            entity: sensor.frigate_nvme_ssd_efi_info
            icon: "{%- set content = state_attr('sensor.frigate_nvme_ssd_efi_info', 'content') -%}{{ 'mdi:lock' if content == 'encrypted' else 'mdi:swap-horizontal' if content == 'swap' else 'mdi:database' if content == 'lvm_pv' else 'mdi:folder' }}"
            icon_color: "{%- set encrypted = state_attr('sensor.frigate_nvme_ssd_efi_info', 'encrypted') | default(false) -%}}{{ 'green' if encrypted else 'blue' }}"
            primary: "{%- set label = state_attr('sensor.frigate_nvme_ssd_efi_info', 'label') -%}{%- set fstype = state_attr('sensor.frigate_nvme_ssd_efi_info', 'fstype') -%}{{ label if label else 'EFI' }} ({{ fstype }})"
            secondary: >-
              {%- set size = state_attr('sensor.frigate_nvme_ssd_efi_info', 'size_gb') | float(0) -%}}
              {%- set encrypted = state_attr('sensor.frigate_nvme_ssd_efi_info', 'encrypted') | default(false) -%}}
              {%- set scheme = state_attr('sensor.frigate_nvme_ssd_efi_info', 'encryption_scheme') | default('none') -%}}
              {%- set unlocked = state_attr('sensor.frigate_nvme_ssd_efi_info', 'encryption_unlocked') | default(false) -%}}
              {{ size | round(1) }} GiB{% if encrypted %} · {{ scheme | upper }}{% if unlocked %} (unlocked){% else %} (locked){% endif %}{% endif %}
            badge_icon: "{%- set encrypted = state_attr('sensor.frigate_nvme_ssd_efi_info', 'encrypted') | default(false) -%}}{{ 'mdi:shield-lock' if encrypted else '' }}"
            badge_color: "{%- set unlocked = state_attr('sensor.frigate_nvme_ssd_efi_info', 'encryption_unlocked') | default(false) -%}}{{ 'green' if unlocked else 'orange' }}"
            tap_action:
              action: more-info
          - type: custom:mushroom-template-card
            entity: sensor.frigate_nvme_ssd_boot_info
            icon: "{%- set content = state_attr('sensor.frigate_nvme_ssd_boot_info', 'content') -%}{{ 'mdi:lock' if content == 'encrypted' else 'mdi:swap-horizontal' if content == 'swap' else 'mdi:database' if content == 'lvm_pv' else 'mdi:folder' }}"
            icon_color: "{%- set encrypted = state_attr('sensor.frigate_nvme_ssd_boot_info', 'encrypted') | default(false) -%}}{{ 'green' if encrypted else 'blue' }}"
            primary: "{%- set label = state_attr('sensor.frigate_nvme_ssd_boot_info', 'label') -%}{%- set fstype = state_attr('sensor.frigate_nvme_ssd_boot_info', 'fstype') -%}{{ label if label else 'Boot' }} ({{ fstype }})"
            secondary: >-
              {%- set size = state_attr('sensor.frigate_nvme_ssd_boot_info', 'size_gb') | float(0) -%}}
              {%- set encrypted = state_attr('sensor.frigate_nvme_ssd_boot_info', 'encrypted') | default(false) -%}}
              {%- set scheme = state_attr('sensor.frigate_nvme_ssd_boot_info', 'encryption_scheme') | default('none') -%}}
              {%- set unlocked = state_attr('sensor.frigate_nvme_ssd_boot_info', 'encryption_unlocked') | default(false) -%}}
              {{ size | round(1) }} GiB{% if encrypted %} · {{ scheme | upper }}{% if unlocked %} (unlocked){% else %} (locked){% endif %}{% endif %}
            badge_icon: "{%- set encrypted = state_attr('sensor.frigate_nvme_ssd_boot_info', 'encrypted') | default(false) -%}}{{ 'mdi:shield-lock' if encrypted else '' }}"
            badge_color: "{%- set unlocked = state_attr('sensor.frigate_nvme_ssd_boot_info', 'encryption_unlocked') | default(false) -%}}{{ 'green' if unlocked else 'orange' }}"
            tap_action:
              action: more-info
          - type: custom:mushroom-template-card
            entity: sensor.frigate_nvme_ssd_lvm_info
            icon: "{%- set content = state_attr('sensor.frigate_nvme_ssd_lvm_info', 'content') -%}{{ 'mdi:lock' if content == 'encrypted' else 'mdi:swap-horizontal' if content == 'swap' else 'mdi:database' if content == 'lvm_pv' else 'mdi:folder' }}"
            icon_color: "{%- set encrypted = state_attr('sensor.frigate_nvme_ssd_lvm_info', 'encrypted') | default(false) -%}}{{ 'green' if encrypted else 'blue' }}"
            primary: "{%- set label = state_attr('sensor.frigate_nvme_ssd_lvm_info', 'label') -%}{%- set fstype = state_attr('sensor.frigate_nvme_ssd_lvm_info', 'fstype') -%}{{ label if label else 'LVM' }} ({{ fstype }})"
            secondary: >-
              {%- set size = state_attr('sensor.frigate_nvme_ssd_lvm_info', 'size_gb') | float(0) -%}}
              {%- set encrypted = state_attr('sensor.frigate_nvme_ssd_lvm_info', 'encrypted') | default(false) -%}}
              {%- set scheme = state_attr('sensor.frigate_nvme_ssd_lvm_info', 'encryption_scheme') | default('none') -%}}
              {%- set unlocked = state_attr('sensor.frigate_nvme_ssd_lvm_info', 'encryption_unlocked') | default(false) -%}}
              {{ size | round(1) }} GiB{% if encrypted %} · {{ scheme | upper }}{% if unlocked %} (unlocked){% else %} (locked){% endif %}{% endif %}
            badge_icon: "{%- set encrypted = state_attr('sensor.frigate_nvme_ssd_lvm_info', 'encrypted') | default(false) -%}}{{ 'mdi:shield-lock' if encrypted else '' }}"
            badge_color: "{%- set unlocked = state_attr('sensor.frigate_nvme_ssd_lvm_info', 'encryption_unlocked') | default(false) -%}}{{ 'green' if unlocked else 'orange' }}"
            tap_action:
              action: more-info
          - type: custom:mushroom-template-card
            entity: sensor.frigate_hdd_transfer_rate
            icon: "{{ 'mdi:harddisk' if state_attr('sensor.frigate_hdd_info', 'type') == 'HDD' else 'mdi:flash' }}"
            icon_color: "{{ 'cyan' if states('sensor.frigate_hdd_transfer_rate') | int > 10485760 else 'blue' if states('sensor.frigate_hdd_transfer_rate') | int > 1048576 else 'grey' }}"
            primary: "{{ state_attr('sensor.frigate_hdd_info', 'model') | default('HDD') | truncate(25, True) }}"
            secondary: >-
              {%- set used = states('sensor.frigate_hdd_used') | float(0) -%}
              {%- set avail = states('sensor.frigate_hdd_available') | float(0) -%}}
              {%- set pct = states('sensor.frigate_hdd_pct_used') | float(0) -%}}
              {%- set temp = states('sensor.frigate_hdd_temp') -%}
              {{ used | round(0) }}G used · {{ avail | round(0) }}G free · {{ pct | round(0) }}%{% if temp not in ['unknown', 'unavailable'] %} · {{ temp }}°C{% endif %}
            multiline_secondary: true
            tap_action:
              action: more-info
          - type: custom:mushroom-template-card
            entity: sensor.frigate_hdd_data_info
            icon: "{%- set content = state_attr('sensor.frigate_hdd_data_info', 'content') -%}{{ 'mdi:lock' if content == 'encrypted' else 'mdi:swap-horizontal' if content == 'swap' else 'mdi:database' if content == 'lvm_pv' else 'mdi:folder' }}"
            icon_color: "{%- set encrypted = state_attr('sensor.frigate_hdd_data_info', 'encrypted') | default(false) -%}}{{ 'green' if encrypted else 'blue' }}"
            primary: "{%- set label = state_attr('sensor.frigate_hdd_data_info', 'label') -%}{%- set fstype = state_attr('sensor.frigate_hdd_data_info', 'fstype') -%}{{ label if label else 'Data' }} ({{ fstype }})"
            secondary: >-
              {%- set size = state_attr('sensor.frigate_hdd_data_info', 'size_gb') | float(0) -%}}
              {%- set encrypted = state_attr('sensor.frigate_hdd_data_info', 'encrypted') | default(false) -%}}
              {%- set scheme = state_attr('sensor.frigate_hdd_data_info', 'encryption_scheme') | default('none') -%}}
              {%- set unlocked = state_attr('sensor.frigate_hdd_data_info', 'encryption_unlocked') | default(false) -%}}
              {{ size | round(1) }} GiB{% if encrypted %} · {{ scheme | upper }}{% if unlocked %} (unlocked){% else %} (locked){% endif %}{% endif %}
            badge_icon: "{%- set encrypted = state_attr('sensor.frigate_hdd_data_info', 'encrypted') | default(false) -%}}{{ 'mdi:shield-lock' if encrypted else '' }}"
            badge_color: "{%- set unlocked = state_attr('sensor.frigate_hdd_data_info', 'encryption_unlocked') | default(false) -%}}{{ 'green' if unlocked else 'orange' }}"
            tap_action:
              action: more-info

  # SMART Info
  - type: grid
    column_span: 2
    cards:
      - type: heading
        heading: Drive Health (SMART)
        heading_style: subtitle
      - type: grid
        columns: 2
        square: false
        cards:
          - type: custom:mushroom-template-card
            entity: sensor.frigate_nvme_ssd_smart
            icon: "{%- set health = states('sensor.frigate_nvme_ssd_health') -%}}{{ 'mdi:heart-pulse' if health == 'PASSED' else 'mdi:heart-broken' }}"
            icon_color: "{{ 'green' if states('sensor.frigate_nvme_ssd_health') == 'PASSED' else 'red' }}"
            primary: "NVMe SSD"
            secondary: >-
              {%- set health = states('sensor.frigate_nvme_ssd_health') -%}
              {%- set wear = states('sensor.frigate_nvme_ssd_wear_level') -%}
              {%- set temp = states('sensor.frigate_nvme_ssd_temp') -%}
              {%- set poh = state_attr('sensor.frigate_nvme_ssd_smart', 'power_on_hours') | default(0) -%}}
              {%- set cycles = state_attr('sensor.frigate_nvme_ssd_smart', 'power_cycles') | default(0) -%}}
              {%- set errors = state_attr('sensor.frigate_nvme_ssd_smart', 'media_errors') | default(0) -%}}
              {{ health }}{% if wear not in ['unknown', 'unavailable'] %} · {{ wear }}% life{% endif %}{% if temp not in ['unknown', 'unavailable'] %} · {{ temp }}°C{% endif %}
              POH: {{ poh | int }}h · Cycles: {{ cycles }} · Errors: {{ errors }}
            multiline_secondary: true
            tap_action:
              action: more-info
          - type: custom:mushroom-template-card
            entity: sensor.frigate_hdd_smart
            icon: "{%- set health = states('sensor.frigate_hdd_health') -%}}{{ 'mdi:heart-pulse' if health == 'PASSED' else 'mdi:heart-broken' }}"
            icon_color: "{{ 'green' if states('sensor.frigate_hdd_health') == 'PASSED' else 'red' }}"
            primary: "HDD"
            secondary: >-
              {%- set health = states('sensor.frigate_hdd_health') -%}
              {%- set wear = states('sensor.frigate_hdd_wear_level') -%}
              {%- set temp = states('sensor.frigate_hdd_temp') -%}
              {%- set poh = state_attr('sensor.frigate_hdd_smart', 'power_on_hours') | default(0) -%}}
              {%- set cycles = state_attr('sensor.frigate_hdd_smart', 'power_cycles') | default(0) -%}}
              {%- set errors = state_attr('sensor.frigate_hdd_smart', 'media_errors') | default(0) -%}}
              {{ health }}{% if wear not in ['unknown', 'unavailable'] %} · {{ wear }}% life{% endif %}{% if temp not in ['unknown', 'unavailable'] %} · {{ temp }}°C{% endif %}
              POH: {{ poh | int }}h · Cycles: {{ cycles }} · Errors: {{ errors }}
            multiline_secondary: true
            tap_action:
              action: more-info
