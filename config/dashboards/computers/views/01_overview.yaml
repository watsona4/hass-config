# -----------------------------------------------------------------------------
# Computers Dashboard: Systems Overview
# -----------------------------------------------------------------------------
#
# Auto-generated by generate_hwmon_assets.py
# DO NOT EDIT - Changes will be overwritten
#
# -----------------------------------------------------------------------------

title: Systems Overview
type: sections
max_columns: 2
cards: []

badges:
  - type: custom:mushroom-template-badge
    entity: sensor.laptop_cpu_utilization
    icon: "{{ state_attr('sensor.ui_laptop_cpu_utilization', 'icon') }}"
    color: "{{ state_attr('sensor.ui_laptop_cpu_utilization', 'color') }}"
    label: Laptop
    content: "{{ state_attr('sensor.ui_laptop_cpu_utilization', 'short_label') }}"
    tap_action:
      action: more-info
    icon_tap_action:
      action: toggle
  - type: custom:mushroom-template-badge
    entity: sensor.frigate_cpu_utilization
    icon: "{{ state_attr('sensor.ui_frigate_cpu_utilization', 'icon') }}"
    color: "{{ state_attr('sensor.ui_frigate_cpu_utilization', 'color') }}"
    label: Frigate
    content: "{{ state_attr('sensor.ui_frigate_cpu_utilization', 'short_label') }}"
    tap_action:
      action: more-info
    icon_tap_action:
      action: toggle
  - type: custom:mushroom-template-badge
    entity: sensor.desktop_cpu_utilization
    icon: "{{ state_attr('sensor.ui_desktop_cpu_utilization', 'icon') }}"
    color: "{{ state_attr('sensor.ui_desktop_cpu_utilization', 'color') }}"
    label: Desktop
    content: "{{ state_attr('sensor.ui_desktop_cpu_utilization', 'short_label') }}"
    tap_action:
      action: more-info
    icon_tap_action:
      action: toggle
  - type: custom:mushroom-template-badge
    entity: sensor.opnsense_cpu_utilization
    icon: "{{ state_attr('sensor.ui_opnsense_cpu_utilization', 'icon') }}"
    color: "{{ state_attr('sensor.ui_opnsense_cpu_utilization', 'color') }}"
    label: OPNSense
    content: "{{ state_attr('sensor.ui_opnsense_cpu_utilization', 'short_label') }}"
    tap_action:
      action: more-info
    icon_tap_action:
      action: toggle
  - type: custom:mushroom-template-badge
    entity: sensor.raspi4_cpu_utilization
    icon: "{{ state_attr('sensor.ui_raspi4_cpu_utilization', 'icon') }}"
    color: "{{ state_attr('sensor.ui_raspi4_cpu_utilization', 'color') }}"
    label: Raspberry Pi 4
    content: "{{ state_attr('sensor.ui_raspi4_cpu_utilization', 'short_label') }}"
    tap_action:
      action: more-info
    icon_tap_action:
      action: toggle
  - type: custom:mushroom-template-badge
    entity: sensor.rvhass_cpu_utilization
    icon: "{{ state_attr('sensor.ui_rvhass_cpu_utilization', 'icon') }}"
    color: "{{ state_attr('sensor.ui_rvhass_cpu_utilization', 'color') }}"
    label: RV HA Yellow
    content: "{{ state_attr('sensor.ui_rvhass_cpu_utilization', 'short_label') }}"
    tap_action:
      action: more-info
    icon_tap_action:
      action: toggle
  - type: custom:mushroom-template-badge
    entity: sensor.vultr_cpu_utilization
    icon: "{{ state_attr('sensor.ui_vultr_cpu_utilization', 'icon') }}"
    color: "{{ state_attr('sensor.ui_vultr_cpu_utilization', 'color') }}"
    label: Vultr VPS
    content: "{{ state_attr('sensor.ui_vultr_cpu_utilization', 'short_label') }}"
    tap_action:
      action: more-info
    icon_tap_action:
      action: toggle
sections:
  - type: grid
    column_span: 2
    cards:
      - type: vertical-stack
        cards:

          # Header
          - type: custom:mushroom-title-card
            title: Laptop
            subtitle: "{{ states('sensor.laptop_overall_status_summary') }}"

          # Summary
          - type: horizontal-stack
            cards:
              - type: custom:mushroom-template-card
                entity: sensor.laptop_uptime_s
                icon: "{{ state_attr('sensor.ui_laptop_uptime_s', 'icon') }}"
                icon_color: "{{ state_attr('sensor.ui_laptop_uptime_s', 'color') }}"
                primary: Uptime
                secondary: "{{ state_attr('sensor.ui_laptop_uptime_s', 'label_vertical') }}"
                badge_icon: "{{ state_attr('sensor.ui_laptop_uptime_s', 'badge') }}"
                badge_color: "{{ state_attr('sensor.ui_laptop_uptime_s', 'badge_color') }}"
                layout: vertical
                multiline_secondary: true
                tap_action:
                  action: more-info
                icon_tap_action:
                  action: toggle
                grid_options:
                  columns: 12
                  rows: auto
              - type: custom:mushroom-template-card
                entity: sensor.laptop_updates_pending
                icon: "{{ state_attr('sensor.ui_laptop_updates_pending', 'icon') }}"
                icon_color: "{{ state_attr('sensor.ui_laptop_updates_pending', 'color') }}"
                primary: Updates
                secondary: "{{ state_attr('sensor.ui_laptop_updates_pending', 'label_vertical') }}"
                badge_icon: "{{ state_attr('sensor.ui_laptop_updates_pending', 'badge') }}"
                badge_color: "{{ state_attr('sensor.ui_laptop_updates_pending', 'badge_color') }}"
                layout: vertical
                multiline_secondary: true
                tap_action:
                  action: more-info
                icon_tap_action:
                  action: toggle
                grid_options:
                  columns: 12
                  rows: auto
              - type: custom:mushroom-template-card
                entity: binary_sensor.laptop_status_alarm
                icon: "{{ state_attr('sensor.ui_laptop_status_alarm', 'icon') }}"
                icon_color: "{{ state_attr('sensor.ui_laptop_status_alarm', 'color') }}"
                primary: Status
                secondary: "{{ state_attr('sensor.ui_laptop_status_alarm', 'long_label_vertical') }}"
                badge_icon: "{{ state_attr('sensor.ui_laptop_status_alarm', 'badge') }}"
                badge_color: "{{ state_attr('sensor.ui_laptop_status_alarm', 'badge_color') }}"
                layout: vertical
                multiline_secondary: true
                tap_action:
                  action: more-info
                icon_tap_action:
                  action: toggle
                grid_options:
                  columns: 12
                  rows: auto
          - type: horizontal-stack
            cards:
              - type: custom:mushroom-template-card
                entity: sensor.laptop_cpu_utilization
                icon: "{{ state_attr('sensor.ui_laptop_cpu_utilization_load', 'icon') }}"
                icon_color: "{{ state_attr('sensor.ui_laptop_cpu_utilization_load', 'color') }}"
                primary: CPU
                secondary: "{{ state_attr('sensor.ui_laptop_cpu_utilization_load', 'label') }}"
                badge_icon: "{{ state_attr('sensor.ui_laptop_cpu_utilization_load', 'badge') }}"
                badge_color: "{{ state_attr('sensor.ui_laptop_cpu_utilization_load', 'badge_color') }}"
                multiline_secondary: true
                tap_action:
                  action: more-info
                icon_tap_action:
                  action: toggle
                grid_options:
                  columns: 12
                  rows: auto
              - type: custom:mushroom-template-card
                entity: sensor.laptop_memory_percent
                icon: "{{ state_attr('sensor.ui_laptop_memory_percent', 'icon') }}"
                icon_color: "{{ state_attr('sensor.ui_laptop_memory_percent', 'color') }}"
                primary: Memory
                secondary: "{{ state_attr('sensor.ui_laptop_memory_percent', 'long_desc') }}"
                badge_icon: "{{ state_attr('sensor.ui_laptop_memory_percent', 'badge') }}"
                badge_color: "{{ state_attr('sensor.ui_laptop_memory_percent', 'badge_color') }}"
                multiline_secondary: true
                tap_action:
                  action: more-info
                icon_tap_action:
                  action: toggle
                grid_options:
                  columns: 12
                  rows: auto
          - type: horizontal-stack
            cards:
              - type: vertical-stack
                cards:
                  - type: custom:mushroom-template-card
                    entity: sensor.laptop_fs_windows_c_percent
                    icon: "{{ state_attr('sensor.ui_laptop_fs_windows_c_percent', 'icon') }}"
                    icon_color: "{{ state_attr('sensor.ui_laptop_fs_windows_c_percent', 'color') }}"
                    primary: Samsung 512GB NVMe SSD
                    secondary: "{{ state_attr('sensor.ui_laptop_fs_windows_c_percent', 'long_desc') }}"
                    badge_icon: "{{ state_attr('sensor.ui_laptop_fs_windows_c_percent', 'badge') }}"
                    badge_color: "{{ state_attr('sensor.ui_laptop_fs_windows_c_percent', 'badge_color') }}"
                    multiline_secondary: true
                    tap_action:
                      action: more-info
                    icon_tap_action:
                      action: toggle
                    grid_options:
                      columns: 12
                      rows: auto
              - type: vertical-stack
                cards:
                  - type: custom:mushroom-template-card
                    entity: sensor.laptop_cpu_temp
                    icon: "{{ state_attr('sensor.ui_laptop_cpu_temp', 'icon') }}"
                    icon_color: "{{ state_attr('sensor.ui_laptop_cpu_temp', 'color') }}"
                    primary: CPU Temp
                    secondary: "{{ state_attr('sensor.ui_laptop_cpu_temp', 'label') }}"
                    badge_icon: "{{ state_attr('sensor.ui_laptop_cpu_temp', 'badge') }}"
                    badge_color: "{{ state_attr('sensor.ui_laptop_cpu_temp', 'badge_color') }}"
                    multiline_secondary: true
                    tap_action:
                      action: more-info
                    icon_tap_action:
                      action: more-info
                    grid_options:
                      columns: 12
                      rows: auto
                  - type: custom:mushroom-template-card
                    visibility:
                      - condition: state
                        entity: sensor.laptop_gpu_utilization
                        state_not: unknown
                    entity: sensor.laptop_gpu_utilization
                    icon: "{{ state_attr('sensor.ui_laptop_gpu_utilization', 'icon') }}"
                    icon_color: "{{ state_attr('sensor.ui_laptop_gpu_utilization', 'color') }}"
                    primary: AMD Radeon 780m
                    secondary: "{{ state_attr('sensor.ui_laptop_gpu_utilization', 'label') }}"
                    badge_icon: "{{ state_attr('sensor.ui_laptop_gpu_utilization', 'badge') }}"
                    badge_color: "{{ state_attr('sensor.ui_laptop_gpu_utilization', 'badge_color') }}"
                    multiline_secondary: true
                    tap_action:
                      action: more-info
                    icon_tap_action:
                      action: more-info
                    grid_options:
                      columns: 12
                      rows: auto

          - type: custom:mushroom-title-card
            subtitle: Stopped Services
            visibility:
              - condition: state
                entity: binary_sensor.laptop_display_services
                state: 'on'

          - type: custom:mushroom-chips-card
            alignment: center
            visibility:
              - condition: state
                entity: binary_sensor.laptop_display_services
                state: 'on'
            chips:
              - type: template
                icon: "{{ state_attr('binary_sensor.ui_laptop_services','libre_hardware_monitor').icon }}"
                icon_color: "{{ state_attr('binary_sensor.ui_laptop_services','libre_hardware_monitor').icon_color }}"
                content: "{{ state_attr('binary_sensor.ui_laptop_services','libre_hardware_monitor').content }}"
                tap_action:
                  action: none
              - type: template
                icon: "{{ state_attr('binary_sensor.ui_laptop_services','dnscache').icon }}"
                icon_color: "{{ state_attr('binary_sensor.ui_laptop_services','dnscache').icon_color }}"
                content: "{{ state_attr('binary_sensor.ui_laptop_services','dnscache').content }}"
                tap_action:
                  action: none
              - type: template
                icon: "{{ state_attr('binary_sensor.ui_laptop_services','lanman_server').icon }}"
                icon_color: "{{ state_attr('binary_sensor.ui_laptop_services','lanman_server').icon_color }}"
                content: "{{ state_attr('binary_sensor.ui_laptop_services','lanman_server').content }}"
                tap_action:
                  action: none
              - type: template
                icon: "{{ state_attr('binary_sensor.ui_laptop_services','spooler').icon }}"
                icon_color: "{{ state_attr('binary_sensor.ui_laptop_services','spooler').icon_color }}"
                content: "{{ state_attr('binary_sensor.ui_laptop_services','spooler').content }}"
                tap_action:
                  action: none

      - type: vertical-stack
        cards:

          # Header
          - type: custom:mushroom-title-card
            title: Frigate
            subtitle: "{{ states('sensor.frigate_overall_status_summary') }}"

          # Summary
          - type: horizontal-stack
            cards:
              - type: custom:mushroom-template-card
                entity: sensor.frigate_uptime_s
                icon: "{{ state_attr('sensor.ui_frigate_uptime_s', 'icon') }}"
                icon_color: "{{ state_attr('sensor.ui_frigate_uptime_s', 'color') }}"
                primary: Uptime
                secondary: "{{ state_attr('sensor.ui_frigate_uptime_s', 'label_vertical') }}"
                badge_icon: "{{ state_attr('sensor.ui_frigate_uptime_s', 'badge') }}"
                badge_color: "{{ state_attr('sensor.ui_frigate_uptime_s', 'badge_color') }}"
                layout: vertical
                multiline_secondary: true
                tap_action:
                  action: more-info
                icon_tap_action:
                  action: toggle
                grid_options:
                  columns: 12
                  rows: auto
              - type: custom:mushroom-template-card
                entity: sensor.frigate_updates_pending
                icon: "{{ state_attr('sensor.ui_frigate_updates_pending', 'icon') }}"
                icon_color: "{{ state_attr('sensor.ui_frigate_updates_pending', 'color') }}"
                primary: Updates
                secondary: "{{ state_attr('sensor.ui_frigate_updates_pending', 'label_vertical') }}"
                badge_icon: "{{ state_attr('sensor.ui_frigate_updates_pending', 'badge') }}"
                badge_color: "{{ state_attr('sensor.ui_frigate_updates_pending', 'badge_color') }}"
                layout: vertical
                multiline_secondary: true
                tap_action:
                  action: more-info
                icon_tap_action:
                  action: toggle
                grid_options:
                  columns: 12
                  rows: auto
              - type: custom:mushroom-template-card
                entity: binary_sensor.frigate_status_alarm
                icon: "{{ state_attr('sensor.ui_frigate_status_alarm', 'icon') }}"
                icon_color: "{{ state_attr('sensor.ui_frigate_status_alarm', 'color') }}"
                primary: Status
                secondary: "{{ state_attr('sensor.ui_frigate_status_alarm', 'long_label_vertical') }}"
                badge_icon: "{{ state_attr('sensor.ui_frigate_status_alarm', 'badge') }}"
                badge_color: "{{ state_attr('sensor.ui_frigate_status_alarm', 'badge_color') }}"
                layout: vertical
                multiline_secondary: true
                tap_action:
                  action: more-info
                icon_tap_action:
                  action: toggle
                grid_options:
                  columns: 12
                  rows: auto
          - type: horizontal-stack
            cards:
              - type: custom:mushroom-template-card
                entity: sensor.frigate_cpu_utilization
                icon: "{{ state_attr('sensor.ui_frigate_cpu_utilization_load', 'icon') }}"
                icon_color: "{{ state_attr('sensor.ui_frigate_cpu_utilization_load', 'color') }}"
                primary: CPU
                secondary: "{{ state_attr('sensor.ui_frigate_cpu_utilization_load', 'label') }}"
                badge_icon: "{{ state_attr('sensor.ui_frigate_cpu_utilization_load', 'badge') }}"
                badge_color: "{{ state_attr('sensor.ui_frigate_cpu_utilization_load', 'badge_color') }}"
                multiline_secondary: true
                tap_action:
                  action: more-info
                icon_tap_action:
                  action: toggle
                grid_options:
                  columns: 12
                  rows: auto
              - type: custom:mushroom-template-card
                entity: sensor.frigate_memory_percent
                icon: "{{ state_attr('sensor.ui_frigate_memory_percent', 'icon') }}"
                icon_color: "{{ state_attr('sensor.ui_frigate_memory_percent', 'color') }}"
                primary: Memory
                secondary: "{{ state_attr('sensor.ui_frigate_memory_percent', 'long_desc') }}"
                badge_icon: "{{ state_attr('sensor.ui_frigate_memory_percent', 'badge') }}"
                badge_color: "{{ state_attr('sensor.ui_frigate_memory_percent', 'badge_color') }}"
                multiline_secondary: true
                tap_action:
                  action: more-info
                icon_tap_action:
                  action: toggle
                grid_options:
                  columns: 12
                  rows: auto
          - type: horizontal-stack
            cards:
              - type: vertical-stack
                cards:
                  - type: custom:mushroom-template-card
                    entity: sensor.frigate_fs_root_percent
                    icon: "{{ state_attr('sensor.ui_frigate_fs_root_percent', 'icon') }}"
                    icon_color: "{{ state_attr('sensor.ui_frigate_fs_root_percent', 'color') }}"
                    primary: WD BLACK 2TB SN770 NVMe SSD
                    secondary: "{{ state_attr('sensor.ui_frigate_fs_root_percent', 'long_desc') }}"
                    badge_icon: "{{ state_attr('sensor.ui_frigate_fs_root_percent', 'badge') }}"
                    badge_color: "{{ state_attr('sensor.ui_frigate_fs_root_percent', 'badge_color') }}"
                    multiline_secondary: true
                    tap_action:
                      action: more-info
                    icon_tap_action:
                      action: toggle
                    grid_options:
                      columns: 12
                      rows: auto
                  - type: custom:mushroom-template-card
                    entity: sensor.frigate_fs_data_percent
                    icon: "{{ state_attr('sensor.ui_frigate_fs_data_percent', 'icon') }}"
                    icon_color: "{{ state_attr('sensor.ui_frigate_fs_data_percent', 'color') }}"
                    primary: WD Blue 2TB Mobile HDD
                    secondary: "{{ state_attr('sensor.ui_frigate_fs_data_percent', 'long_desc') }}"
                    badge_icon: "{{ state_attr('sensor.ui_frigate_fs_data_percent', 'badge') }}"
                    badge_color: "{{ state_attr('sensor.ui_frigate_fs_data_percent', 'badge_color') }}"
                    multiline_secondary: true
                    tap_action:
                      action: more-info
                    icon_tap_action:
                      action: toggle
                    grid_options:
                      columns: 12
                      rows: auto
              - type: vertical-stack
                cards:
                  - type: custom:mushroom-template-card
                    entity: sensor.frigate_cpu_temp
                    icon: "{{ state_attr('sensor.ui_frigate_cpu_temp', 'icon') }}"
                    icon_color: "{{ state_attr('sensor.ui_frigate_cpu_temp', 'color') }}"
                    primary: CPU Temp
                    secondary: "{{ state_attr('sensor.ui_frigate_cpu_temp', 'label') }}"
                    badge_icon: "{{ state_attr('sensor.ui_frigate_cpu_temp', 'badge') }}"
                    badge_color: "{{ state_attr('sensor.ui_frigate_cpu_temp', 'badge_color') }}"
                    multiline_secondary: true
                    tap_action:
                      action: more-info
                    icon_tap_action:
                      action: more-info
                    grid_options:
                      columns: 12
                      rows: auto
                  - type: custom:mushroom-template-card
                    visibility:
                      - condition: state
                        entity: sensor.frigate_gpu_utilization
                        state_not: unknown
                    entity: sensor.frigate_gpu_utilization
                    icon: "{{ state_attr('sensor.ui_frigate_gpu_utilization', 'icon') }}"
                    icon_color: "{{ state_attr('sensor.ui_frigate_gpu_utilization', 'color') }}"
                    primary: Intel iGPU
                    secondary: "{{ state_attr('sensor.ui_frigate_gpu_utilization', 'label') }}"
                    badge_icon: "{{ state_attr('sensor.ui_frigate_gpu_utilization', 'badge') }}"
                    badge_color: "{{ state_attr('sensor.ui_frigate_gpu_utilization', 'badge_color') }}"
                    multiline_secondary: true
                    tap_action:
                      action: more-info
                    icon_tap_action:
                      action: more-info
                    grid_options:
                      columns: 12
                      rows: auto
                  - type: custom:mushroom-template-card
                    visibility:
                      - condition: state
                        entity: sensor.frigate_coral_tpu_temp
                        state_not: unknown
                    entity: sensor.frigate_coral_tpu_temp
                    icon: "{{ state_attr('sensor.ui_frigate_coral_tpu_temp', 'icon') }}"
                    icon_color: "{{ state_attr('sensor.ui_frigate_coral_tpu_temp', 'color') }}"
                    primary: Google Coral TPU
                    secondary: "{{ state_attr('sensor.ui_frigate_coral_tpu_temp', 'label') }}"
                    badge_icon: "{{ state_attr('sensor.ui_frigate_coral_tpu_temp', 'badge') }}"
                    badge_color: "{{ state_attr('sensor.ui_frigate_coral_tpu_temp', 'badge_color') }}"
                    multiline_secondary: true
                    tap_action:
                      action: more-info
                    icon_tap_action:
                      action: more-info
                    grid_options:
                      columns: 12
                      rows: auto

          - type: custom:mushroom-title-card
            subtitle: Stopped Services
            visibility:
              - condition: state
                entity: binary_sensor.frigate_display_services
                state: 'on'

          - type: custom:auto-entities
            card:
              type: custom:mushroom-chips-card
              alignment: start
            card_param: chips
            filter:
              include:
                - entity_id: binary_sensor.frigate*_service_*
                  state: 'off'
                  options:
                    type: template
                    entity: this.entity_id
                    icon: |
                      {{- 'mdi:cog' if is_state(entity,'on') else 'mdi:cog-off' -}}
                    icon_color: |
                      {{- 'green' if is_state(entity,'on') else 'red' -}}
                    content: >
                      {%- set s = state_attr(entity,'friendly_name') | default('', true) -%}
                      {{- (s.partition('Service')[2]) if 'Service' in s else '' -}}
                    tap_action: none
              exclude: []
            sort:
              method: name

          - type: custom:mushroom-title-card
            subtitle: Stopped Containers
            visibility:
              - condition: state
                entity: binary_sensor.frigate_display_containers
                state: 'on'

          - type: custom:auto-entities
            card:
              type: custom:mushroom-chips-card
              alignment: start
            card_param: chips
            filter:
              include:
                - entity_id: binary_sensor.frigate*_container_*
                  state: 'off'
                  options:
                    type: template
                    entity: this.entity_id
                    icon: >
                      {{- 'mdi:server' if is_state(entity,'on') else 'mdi:server-off' -}}
                    icon_color: |
                      {{- 'green' if is_state(entity,'on') else 'red' -}}
                    content: >
                      {%- set s = state_attr(entity,'friendly_name') | default('', true) -%}
                      {{- (s.partition('Container')[2]) if 'Container' in s else '' -}}
                    tap_action: none
              exclude: []
            sort:
              method: name

      - type: vertical-stack
        cards:

          # Header
          - type: custom:mushroom-title-card
            title: Desktop
            subtitle: "{{ states('sensor.desktop_overall_status_summary') }}"

          # Summary
          - type: horizontal-stack
            cards:
              - type: custom:mushroom-template-card
                entity: sensor.desktop_uptime_s
                icon: "{{ state_attr('sensor.ui_desktop_uptime_s', 'icon') }}"
                icon_color: "{{ state_attr('sensor.ui_desktop_uptime_s', 'color') }}"
                primary: Uptime
                secondary: "{{ state_attr('sensor.ui_desktop_uptime_s', 'label_vertical') }}"
                badge_icon: "{{ state_attr('sensor.ui_desktop_uptime_s', 'badge') }}"
                badge_color: "{{ state_attr('sensor.ui_desktop_uptime_s', 'badge_color') }}"
                layout: vertical
                multiline_secondary: true
                tap_action:
                  action: more-info
                icon_tap_action:
                  action: toggle
                grid_options:
                  columns: 12
                  rows: auto
              - type: custom:mushroom-template-card
                entity: sensor.desktop_updates_pending
                icon: "{{ state_attr('sensor.ui_desktop_updates_pending', 'icon') }}"
                icon_color: "{{ state_attr('sensor.ui_desktop_updates_pending', 'color') }}"
                primary: Updates
                secondary: "{{ state_attr('sensor.ui_desktop_updates_pending', 'label_vertical') }}"
                badge_icon: "{{ state_attr('sensor.ui_desktop_updates_pending', 'badge') }}"
                badge_color: "{{ state_attr('sensor.ui_desktop_updates_pending', 'badge_color') }}"
                layout: vertical
                multiline_secondary: true
                tap_action:
                  action: more-info
                icon_tap_action:
                  action: toggle
                grid_options:
                  columns: 12
                  rows: auto
              - type: custom:mushroom-template-card
                entity: binary_sensor.desktop_status_alarm
                icon: "{{ state_attr('sensor.ui_desktop_status_alarm', 'icon') }}"
                icon_color: "{{ state_attr('sensor.ui_desktop_status_alarm', 'color') }}"
                primary: Status
                secondary: "{{ state_attr('sensor.ui_desktop_status_alarm', 'long_label_vertical') }}"
                badge_icon: "{{ state_attr('sensor.ui_desktop_status_alarm', 'badge') }}"
                badge_color: "{{ state_attr('sensor.ui_desktop_status_alarm', 'badge_color') }}"
                layout: vertical
                multiline_secondary: true
                tap_action:
                  action: more-info
                icon_tap_action:
                  action: toggle
                grid_options:
                  columns: 12
                  rows: auto
          - type: horizontal-stack
            cards:
              - type: custom:mushroom-template-card
                entity: sensor.desktop_cpu_utilization
                icon: "{{ state_attr('sensor.ui_desktop_cpu_utilization_load', 'icon') }}"
                icon_color: "{{ state_attr('sensor.ui_desktop_cpu_utilization_load', 'color') }}"
                primary: CPU
                secondary: "{{ state_attr('sensor.ui_desktop_cpu_utilization_load', 'label') }}"
                badge_icon: "{{ state_attr('sensor.ui_desktop_cpu_utilization_load', 'badge') }}"
                badge_color: "{{ state_attr('sensor.ui_desktop_cpu_utilization_load', 'badge_color') }}"
                multiline_secondary: true
                tap_action:
                  action: more-info
                icon_tap_action:
                  action: toggle
                grid_options:
                  columns: 12
                  rows: auto
              - type: custom:mushroom-template-card
                entity: sensor.desktop_memory_percent
                icon: "{{ state_attr('sensor.ui_desktop_memory_percent', 'icon') }}"
                icon_color: "{{ state_attr('sensor.ui_desktop_memory_percent', 'color') }}"
                primary: Memory
                secondary: "{{ state_attr('sensor.ui_desktop_memory_percent', 'long_desc') }}"
                badge_icon: "{{ state_attr('sensor.ui_desktop_memory_percent', 'badge') }}"
                badge_color: "{{ state_attr('sensor.ui_desktop_memory_percent', 'badge_color') }}"
                multiline_secondary: true
                tap_action:
                  action: more-info
                icon_tap_action:
                  action: toggle
                grid_options:
                  columns: 12
                  rows: auto
          - type: horizontal-stack
            cards:
              - type: vertical-stack
                cards:
                  - type: custom:mushroom-template-card
                    entity: sensor.desktop_fs_windows_c_percent
                    icon: "{{ state_attr('sensor.ui_desktop_fs_windows_c_percent', 'icon') }}"
                    icon_color: "{{ state_attr('sensor.ui_desktop_fs_windows_c_percent', 'color') }}"
                    primary: SK hynix 256GB SSD
                    secondary: "{{ state_attr('sensor.ui_desktop_fs_windows_c_percent', 'long_desc') }}"
                    badge_icon: "{{ state_attr('sensor.ui_desktop_fs_windows_c_percent', 'badge') }}"
                    badge_color: "{{ state_attr('sensor.ui_desktop_fs_windows_c_percent', 'badge_color') }}"
                    multiline_secondary: true
                    tap_action:
                      action: more-info
                    icon_tap_action:
                      action: toggle
                    grid_options:
                      columns: 12
                      rows: auto
                  - type: custom:mushroom-template-card
                    entity: sensor.desktop_fs_data_d_percent
                    icon: "{{ state_attr('sensor.ui_desktop_fs_data_d_percent', 'icon') }}"
                    icon_color: "{{ state_attr('sensor.ui_desktop_fs_data_d_percent', 'color') }}"
                    primary: WD 1TB HDD
                    secondary: "{{ state_attr('sensor.ui_desktop_fs_data_d_percent', 'long_desc') }}"
                    badge_icon: "{{ state_attr('sensor.ui_desktop_fs_data_d_percent', 'badge') }}"
                    badge_color: "{{ state_attr('sensor.ui_desktop_fs_data_d_percent', 'badge_color') }}"
                    multiline_secondary: true
                    tap_action:
                      action: more-info
                    icon_tap_action:
                      action: toggle
                    grid_options:
                      columns: 12
                      rows: auto
              - type: vertical-stack
                cards:
                  - type: custom:mushroom-template-card
                    entity: sensor.desktop_cpu_temp
                    icon: "{{ state_attr('sensor.ui_desktop_cpu_temp', 'icon') }}"
                    icon_color: "{{ state_attr('sensor.ui_desktop_cpu_temp', 'color') }}"
                    primary: CPU Temp
                    secondary: "{{ state_attr('sensor.ui_desktop_cpu_temp', 'label') }}"
                    badge_icon: "{{ state_attr('sensor.ui_desktop_cpu_temp', 'badge') }}"
                    badge_color: "{{ state_attr('sensor.ui_desktop_cpu_temp', 'badge_color') }}"
                    multiline_secondary: true
                    tap_action:
                      action: more-info
                    icon_tap_action:
                      action: more-info
                    grid_options:
                      columns: 12
                      rows: auto
                  - type: custom:mushroom-template-card
                    visibility:
                      - condition: state
                        entity: sensor.desktop_gpu_utilization
                        state_not: unknown
                    entity: sensor.desktop_gpu_utilization
                    icon: "{{ state_attr('sensor.ui_desktop_gpu_utilization', 'icon') }}"
                    icon_color: "{{ state_attr('sensor.ui_desktop_gpu_utilization', 'color') }}"
                    primary: NVIDIA GTX 1070
                    secondary: "{{ state_attr('sensor.ui_desktop_gpu_utilization', 'label') }}"
                    badge_icon: "{{ state_attr('sensor.ui_desktop_gpu_utilization', 'badge') }}"
                    badge_color: "{{ state_attr('sensor.ui_desktop_gpu_utilization', 'badge_color') }}"
                    multiline_secondary: true
                    tap_action:
                      action: more-info
                    icon_tap_action:
                      action: more-info
                    grid_options:
                      columns: 12
                      rows: auto

          - type: custom:mushroom-title-card
            subtitle: Stopped Services
            visibility:
              - condition: state
                entity: binary_sensor.desktop_display_services
                state: 'on'

          - type: custom:mushroom-chips-card
            alignment: center
            visibility:
              - condition: state
                entity: binary_sensor.desktop_display_services
                state: 'on'
            chips:
              - type: template
                icon: "{{ state_attr('binary_sensor.ui_desktop_services','libre_hardware_monitor').icon }}"
                icon_color: "{{ state_attr('binary_sensor.ui_desktop_services','libre_hardware_monitor').icon_color }}"
                content: "{{ state_attr('binary_sensor.ui_desktop_services','libre_hardware_monitor').content }}"
                tap_action:
                  action: none
              - type: template
                icon: "{{ state_attr('binary_sensor.ui_desktop_services','dnscache').icon }}"
                icon_color: "{{ state_attr('binary_sensor.ui_desktop_services','dnscache').icon_color }}"
                content: "{{ state_attr('binary_sensor.ui_desktop_services','dnscache').content }}"
                tap_action:
                  action: none
              - type: template
                icon: "{{ state_attr('binary_sensor.ui_desktop_services','lanman_server').icon }}"
                icon_color: "{{ state_attr('binary_sensor.ui_desktop_services','lanman_server').icon_color }}"
                content: "{{ state_attr('binary_sensor.ui_desktop_services','lanman_server').content }}"
                tap_action:
                  action: none
              - type: template
                icon: "{{ state_attr('binary_sensor.ui_desktop_services','spooler').icon }}"
                icon_color: "{{ state_attr('binary_sensor.ui_desktop_services','spooler').icon_color }}"
                content: "{{ state_attr('binary_sensor.ui_desktop_services','spooler').content }}"
                tap_action:
                  action: none

      - type: vertical-stack
        cards:

          # Header
          - type: custom:mushroom-title-card
            title: OPNSense
            subtitle: "{{ states('sensor.opnsense_overall_status_summary') }}"

          # Summary
          - type: horizontal-stack
            cards:
              - type: custom:mushroom-template-card
                entity: sensor.opnsense_uptime_s
                icon: "{{ state_attr('sensor.ui_opnsense_uptime_s', 'icon') }}"
                icon_color: "{{ state_attr('sensor.ui_opnsense_uptime_s', 'color') }}"
                primary: Uptime
                secondary: "{{ state_attr('sensor.ui_opnsense_uptime_s', 'label_vertical') }}"
                badge_icon: "{{ state_attr('sensor.ui_opnsense_uptime_s', 'badge') }}"
                badge_color: "{{ state_attr('sensor.ui_opnsense_uptime_s', 'badge_color') }}"
                layout: vertical
                multiline_secondary: true
                tap_action:
                  action: more-info
                icon_tap_action:
                  action: toggle
                grid_options:
                  columns: 12
                  rows: auto
              - type: custom:mushroom-template-card
                entity: sensor.opnsense_updates_pending
                icon: "{{ state_attr('sensor.ui_opnsense_updates_pending', 'icon') }}"
                icon_color: "{{ state_attr('sensor.ui_opnsense_updates_pending', 'color') }}"
                primary: Updates
                secondary: "{{ state_attr('sensor.ui_opnsense_updates_pending', 'label_vertical') }}"
                badge_icon: "{{ state_attr('sensor.ui_opnsense_updates_pending', 'badge') }}"
                badge_color: "{{ state_attr('sensor.ui_opnsense_updates_pending', 'badge_color') }}"
                layout: vertical
                multiline_secondary: true
                tap_action:
                  action: more-info
                icon_tap_action:
                  action: toggle
                grid_options:
                  columns: 12
                  rows: auto
              - type: custom:mushroom-template-card
                entity: binary_sensor.opnsense_status_alarm
                icon: "{{ state_attr('sensor.ui_opnsense_status_alarm', 'icon') }}"
                icon_color: "{{ state_attr('sensor.ui_opnsense_status_alarm', 'color') }}"
                primary: Status
                secondary: "{{ state_attr('sensor.ui_opnsense_status_alarm', 'long_label_vertical') }}"
                badge_icon: "{{ state_attr('sensor.ui_opnsense_status_alarm', 'badge') }}"
                badge_color: "{{ state_attr('sensor.ui_opnsense_status_alarm', 'badge_color') }}"
                layout: vertical
                multiline_secondary: true
                tap_action:
                  action: more-info
                icon_tap_action:
                  action: toggle
                grid_options:
                  columns: 12
                  rows: auto
          - type: horizontal-stack
            cards:
              - type: custom:mushroom-template-card
                entity: sensor.opnsense_cpu_utilization
                icon: "{{ state_attr('sensor.ui_opnsense_cpu_utilization_load', 'icon') }}"
                icon_color: "{{ state_attr('sensor.ui_opnsense_cpu_utilization_load', 'color') }}"
                primary: CPU
                secondary: "{{ state_attr('sensor.ui_opnsense_cpu_utilization_load', 'label') }}"
                badge_icon: "{{ state_attr('sensor.ui_opnsense_cpu_utilization_load', 'badge') }}"
                badge_color: "{{ state_attr('sensor.ui_opnsense_cpu_utilization_load', 'badge_color') }}"
                multiline_secondary: true
                tap_action:
                  action: more-info
                icon_tap_action:
                  action: toggle
                grid_options:
                  columns: 12
                  rows: auto
              - type: custom:mushroom-template-card
                entity: sensor.opnsense_memory_percent
                icon: "{{ state_attr('sensor.ui_opnsense_memory_percent', 'icon') }}"
                icon_color: "{{ state_attr('sensor.ui_opnsense_memory_percent', 'color') }}"
                primary: Memory
                secondary: "{{ state_attr('sensor.ui_opnsense_memory_percent', 'long_desc') }}"
                badge_icon: "{{ state_attr('sensor.ui_opnsense_memory_percent', 'badge') }}"
                badge_color: "{{ state_attr('sensor.ui_opnsense_memory_percent', 'badge_color') }}"
                multiline_secondary: true
                tap_action:
                  action: more-info
                icon_tap_action:
                  action: toggle
                grid_options:
                  columns: 12
                  rows: auto
          - type: horizontal-stack
            cards:
              - type: vertical-stack
                cards:
                  - type: custom:mushroom-template-card
                    entity: sensor.opnsense_fs_root_percent
                    icon: "{{ state_attr('sensor.ui_opnsense_fs_root_percent', 'icon') }}"
                    icon_color: "{{ state_attr('sensor.ui_opnsense_fs_root_percent', 'color') }}"
                    primary: WD Blue SN580 500GB SSD
                    secondary: "{{ state_attr('sensor.ui_opnsense_fs_root_percent', 'long_desc') }}"
                    badge_icon: "{{ state_attr('sensor.ui_opnsense_fs_root_percent', 'badge') }}"
                    badge_color: "{{ state_attr('sensor.ui_opnsense_fs_root_percent', 'badge_color') }}"
                    multiline_secondary: true
                    tap_action:
                      action: more-info
                    icon_tap_action:
                      action: toggle
                    grid_options:
                      columns: 12
                      rows: auto
                  - type: custom:mushroom-template-card
                    entity: sensor.opnsense_fs_logs_percent
                    icon: "{{ state_attr('sensor.ui_opnsense_fs_logs_percent', 'icon') }}"
                    icon_color: "{{ state_attr('sensor.ui_opnsense_fs_logs_percent', 'color') }}"
                    primary: Logs
                    secondary: "{{ state_attr('sensor.ui_opnsense_fs_logs_percent', 'long_desc') }}"
                    badge_icon: "{{ state_attr('sensor.ui_opnsense_fs_logs_percent', 'badge') }}"
                    badge_color: "{{ state_attr('sensor.ui_opnsense_fs_logs_percent', 'badge_color') }}"
                    multiline_secondary: true
                    tap_action:
                      action: more-info
                    icon_tap_action:
                      action: toggle
                    grid_options:
                      columns: 12
                      rows: auto
              - type: vertical-stack
                cards:
                  - type: custom:mushroom-template-card
                    entity: sensor.opnsense_cpu_temp
                    icon: "{{ state_attr('sensor.ui_opnsense_cpu_temp', 'icon') }}"
                    icon_color: "{{ state_attr('sensor.ui_opnsense_cpu_temp', 'color') }}"
                    primary: CPU Temp
                    secondary: "{{ state_attr('sensor.ui_opnsense_cpu_temp', 'label') }}"
                    badge_icon: "{{ state_attr('sensor.ui_opnsense_cpu_temp', 'badge') }}"
                    badge_color: "{{ state_attr('sensor.ui_opnsense_cpu_temp', 'badge_color') }}"
                    multiline_secondary: true
                    tap_action:
                      action: more-info
                    icon_tap_action:
                      action: more-info
                    grid_options:
                      columns: 12
                      rows: auto

          - type: custom:mushroom-title-card
            subtitle: Stopped Services
            visibility:
              - condition: state
                entity: binary_sensor.opnsense_display_services
                state: 'on'

          - type: custom:auto-entities
            card:
              type: custom:mushroom-chips-card
              alignment: start
            card_param: chips
            filter:
              include:
                - entity_id: binary_sensor.opnsense*_service_*
                  state: 'off'
                  options:
                    type: template
                    entity: this.entity_id
                    icon: |
                      {{- 'mdi:cog' if is_state(entity,'on') else 'mdi:cog-off' -}}
                    icon_color: |
                      {{- 'green' if is_state(entity,'on') else 'red' -}}
                    content: >
                      {%- set s = state_attr(entity,'friendly_name') | default('', true) -%}
                      {{- (s.partition('Service')[2]) if 'Service' in s else '' -}}
                    tap_action: none
              exclude: []
            sort:
              method: name

      - type: vertical-stack
        cards:

          # Header
          - type: custom:mushroom-title-card
            title: Raspberry Pi 4
            subtitle: "{{ states('sensor.raspi4_overall_status_summary') }}"

          # Summary
          - type: horizontal-stack
            cards:
              - type: custom:mushroom-template-card
                entity: sensor.raspi4_uptime_s
                icon: "{{ state_attr('sensor.ui_raspi4_uptime_s', 'icon') }}"
                icon_color: "{{ state_attr('sensor.ui_raspi4_uptime_s', 'color') }}"
                primary: Uptime
                secondary: "{{ state_attr('sensor.ui_raspi4_uptime_s', 'label_vertical') }}"
                badge_icon: "{{ state_attr('sensor.ui_raspi4_uptime_s', 'badge') }}"
                badge_color: "{{ state_attr('sensor.ui_raspi4_uptime_s', 'badge_color') }}"
                layout: vertical
                multiline_secondary: true
                tap_action:
                  action: more-info
                icon_tap_action:
                  action: toggle
                grid_options:
                  columns: 12
                  rows: auto
              - type: custom:mushroom-template-card
                entity: sensor.raspi4_updates_pending
                icon: "{{ state_attr('sensor.ui_raspi4_updates_pending', 'icon') }}"
                icon_color: "{{ state_attr('sensor.ui_raspi4_updates_pending', 'color') }}"
                primary: Updates
                secondary: "{{ state_attr('sensor.ui_raspi4_updates_pending', 'label_vertical') }}"
                badge_icon: "{{ state_attr('sensor.ui_raspi4_updates_pending', 'badge') }}"
                badge_color: "{{ state_attr('sensor.ui_raspi4_updates_pending', 'badge_color') }}"
                layout: vertical
                multiline_secondary: true
                tap_action:
                  action: more-info
                icon_tap_action:
                  action: toggle
                grid_options:
                  columns: 12
                  rows: auto
              - type: custom:mushroom-template-card
                entity: binary_sensor.raspi4_status_alarm
                icon: "{{ state_attr('sensor.ui_raspi4_status_alarm', 'icon') }}"
                icon_color: "{{ state_attr('sensor.ui_raspi4_status_alarm', 'color') }}"
                primary: Status
                secondary: "{{ state_attr('sensor.ui_raspi4_status_alarm', 'long_label_vertical') }}"
                badge_icon: "{{ state_attr('sensor.ui_raspi4_status_alarm', 'badge') }}"
                badge_color: "{{ state_attr('sensor.ui_raspi4_status_alarm', 'badge_color') }}"
                layout: vertical
                multiline_secondary: true
                tap_action:
                  action: more-info
                icon_tap_action:
                  action: toggle
                grid_options:
                  columns: 12
                  rows: auto
          - type: horizontal-stack
            cards:
              - type: custom:mushroom-template-card
                entity: sensor.raspi4_cpu_utilization
                icon: "{{ state_attr('sensor.ui_raspi4_cpu_utilization_load', 'icon') }}"
                icon_color: "{{ state_attr('sensor.ui_raspi4_cpu_utilization_load', 'color') }}"
                primary: CPU
                secondary: "{{ state_attr('sensor.ui_raspi4_cpu_utilization_load', 'label') }}"
                badge_icon: "{{ state_attr('sensor.ui_raspi4_cpu_utilization_load', 'badge') }}"
                badge_color: "{{ state_attr('sensor.ui_raspi4_cpu_utilization_load', 'badge_color') }}"
                multiline_secondary: true
                tap_action:
                  action: more-info
                icon_tap_action:
                  action: toggle
                grid_options:
                  columns: 12
                  rows: auto
              - type: custom:mushroom-template-card
                entity: sensor.raspi4_memory_percent
                icon: "{{ state_attr('sensor.ui_raspi4_memory_percent', 'icon') }}"
                icon_color: "{{ state_attr('sensor.ui_raspi4_memory_percent', 'color') }}"
                primary: Memory
                secondary: "{{ state_attr('sensor.ui_raspi4_memory_percent', 'long_desc') }}"
                badge_icon: "{{ state_attr('sensor.ui_raspi4_memory_percent', 'badge') }}"
                badge_color: "{{ state_attr('sensor.ui_raspi4_memory_percent', 'badge_color') }}"
                multiline_secondary: true
                tap_action:
                  action: more-info
                icon_tap_action:
                  action: toggle
                grid_options:
                  columns: 12
                  rows: auto
          - type: horizontal-stack
            cards:
              - type: vertical-stack
                cards:
                  - type: custom:mushroom-template-card
                    entity: sensor.raspi4_fs_root_percent
                    icon: "{{ state_attr('sensor.ui_raspi4_fs_root_percent', 'icon') }}"
                    icon_color: "{{ state_attr('sensor.ui_raspi4_fs_root_percent', 'color') }}"
                    primary: MicroSD Card
                    secondary: "{{ state_attr('sensor.ui_raspi4_fs_root_percent', 'long_desc') }}"
                    badge_icon: "{{ state_attr('sensor.ui_raspi4_fs_root_percent', 'badge') }}"
                    badge_color: "{{ state_attr('sensor.ui_raspi4_fs_root_percent', 'badge_color') }}"
                    multiline_secondary: true
                    tap_action:
                      action: more-info
                    icon_tap_action:
                      action: toggle
                    grid_options:
                      columns: 12
                      rows: auto
              - type: vertical-stack
                cards:
                  - type: custom:mushroom-template-card
                    entity: sensor.raspi4_cpu_temp
                    icon: "{{ state_attr('sensor.ui_raspi4_cpu_temp', 'icon') }}"
                    icon_color: "{{ state_attr('sensor.ui_raspi4_cpu_temp', 'color') }}"
                    primary: CPU Temp
                    secondary: "{{ state_attr('sensor.ui_raspi4_cpu_temp', 'label') }}"
                    badge_icon: "{{ state_attr('sensor.ui_raspi4_cpu_temp', 'badge') }}"
                    badge_color: "{{ state_attr('sensor.ui_raspi4_cpu_temp', 'badge_color') }}"
                    multiline_secondary: true
                    tap_action:
                      action: more-info
                    icon_tap_action:
                      action: more-info
                    grid_options:
                      columns: 12
                      rows: auto

          - type: custom:mushroom-title-card
            subtitle: Stopped Services
            visibility:
              - condition: state
                entity: binary_sensor.raspi4_display_services
                state: 'on'

          - type: custom:auto-entities
            card:
              type: custom:mushroom-chips-card
              alignment: start
            card_param: chips
            filter:
              include:
                - entity_id: binary_sensor.raspi4*_service_*
                  state: 'off'
                  options:
                    type: template
                    entity: this.entity_id
                    icon: |
                      {{- 'mdi:cog' if is_state(entity,'on') else 'mdi:cog-off' -}}
                    icon_color: |
                      {{- 'green' if is_state(entity,'on') else 'red' -}}
                    content: >
                      {%- set s = state_attr(entity,'friendly_name') | default('', true) -%}
                      {{- (s.partition('Service')[2]) if 'Service' in s else '' -}}
                    tap_action: none
              exclude: []
            sort:
              method: name

      - type: vertical-stack
        cards:

          # Header
          - type: custom:mushroom-title-card
            title: RV HA Yellow
            subtitle: "{{ states('sensor.rvhass_overall_status_summary') }}"

          # Summary
          - type: horizontal-stack
            cards:
              - type: custom:mushroom-template-card
                entity: sensor.rvhass_uptime_s
                icon: "{{ state_attr('sensor.ui_rvhass_uptime_s', 'icon') }}"
                icon_color: "{{ state_attr('sensor.ui_rvhass_uptime_s', 'color') }}"
                primary: Uptime
                secondary: "{{ state_attr('sensor.ui_rvhass_uptime_s', 'label_vertical') }}"
                badge_icon: "{{ state_attr('sensor.ui_rvhass_uptime_s', 'badge') }}"
                badge_color: "{{ state_attr('sensor.ui_rvhass_uptime_s', 'badge_color') }}"
                layout: vertical
                multiline_secondary: true
                tap_action:
                  action: more-info
                icon_tap_action:
                  action: toggle
                grid_options:
                  columns: 12
                  rows: auto
              - type: custom:mushroom-template-card
                entity: sensor.rvhass_updates_pending
                icon: "{{ state_attr('sensor.ui_rvhass_updates_pending', 'icon') }}"
                icon_color: "{{ state_attr('sensor.ui_rvhass_updates_pending', 'color') }}"
                primary: Updates
                secondary: "{{ state_attr('sensor.ui_rvhass_updates_pending', 'label_vertical') }}"
                badge_icon: "{{ state_attr('sensor.ui_rvhass_updates_pending', 'badge') }}"
                badge_color: "{{ state_attr('sensor.ui_rvhass_updates_pending', 'badge_color') }}"
                layout: vertical
                multiline_secondary: true
                tap_action:
                  action: more-info
                icon_tap_action:
                  action: toggle
                grid_options:
                  columns: 12
                  rows: auto
              - type: custom:mushroom-template-card
                entity: binary_sensor.rvhass_status_alarm
                icon: "{{ state_attr('sensor.ui_rvhass_status_alarm', 'icon') }}"
                icon_color: "{{ state_attr('sensor.ui_rvhass_status_alarm', 'color') }}"
                primary: Status
                secondary: "{{ state_attr('sensor.ui_rvhass_status_alarm', 'long_label_vertical') }}"
                badge_icon: "{{ state_attr('sensor.ui_rvhass_status_alarm', 'badge') }}"
                badge_color: "{{ state_attr('sensor.ui_rvhass_status_alarm', 'badge_color') }}"
                layout: vertical
                multiline_secondary: true
                tap_action:
                  action: more-info
                icon_tap_action:
                  action: toggle
                grid_options:
                  columns: 12
                  rows: auto
          - type: horizontal-stack
            cards:
              - type: custom:mushroom-template-card
                entity: sensor.rvhass_cpu_utilization
                icon: "{{ state_attr('sensor.ui_rvhass_cpu_utilization_load', 'icon') }}"
                icon_color: "{{ state_attr('sensor.ui_rvhass_cpu_utilization_load', 'color') }}"
                primary: CPU
                secondary: "{{ state_attr('sensor.ui_rvhass_cpu_utilization_load', 'label') }}"
                badge_icon: "{{ state_attr('sensor.ui_rvhass_cpu_utilization_load', 'badge') }}"
                badge_color: "{{ state_attr('sensor.ui_rvhass_cpu_utilization_load', 'badge_color') }}"
                multiline_secondary: true
                tap_action:
                  action: more-info
                icon_tap_action:
                  action: toggle
                grid_options:
                  columns: 12
                  rows: auto
              - type: custom:mushroom-template-card
                entity: sensor.rvhass_memory_percent
                icon: "{{ state_attr('sensor.ui_rvhass_memory_percent', 'icon') }}"
                icon_color: "{{ state_attr('sensor.ui_rvhass_memory_percent', 'color') }}"
                primary: Memory
                secondary: "{{ state_attr('sensor.ui_rvhass_memory_percent', 'long_desc') }}"
                badge_icon: "{{ state_attr('sensor.ui_rvhass_memory_percent', 'badge') }}"
                badge_color: "{{ state_attr('sensor.ui_rvhass_memory_percent', 'badge_color') }}"
                multiline_secondary: true
                tap_action:
                  action: more-info
                icon_tap_action:
                  action: toggle
                grid_options:
                  columns: 12
                  rows: auto
          - type: horizontal-stack
            cards:
              - type: vertical-stack
                cards:
                  - type: custom:mushroom-template-card
                    entity: sensor.rvhass_fs_data_percent
                    icon: "{{ state_attr('sensor.ui_rvhass_fs_data_percent', 'icon') }}"
                    icon_color: "{{ state_attr('sensor.ui_rvhass_fs_data_percent', 'color') }}"
                    primary: eMMC Storage
                    secondary: "{{ state_attr('sensor.ui_rvhass_fs_data_percent', 'long_desc') }}"
                    badge_icon: "{{ state_attr('sensor.ui_rvhass_fs_data_percent', 'badge') }}"
                    badge_color: "{{ state_attr('sensor.ui_rvhass_fs_data_percent', 'badge_color') }}"
                    multiline_secondary: true
                    tap_action:
                      action: more-info
                    icon_tap_action:
                      action: toggle
                    grid_options:
                      columns: 12
                      rows: auto
              - type: vertical-stack
                cards:
                  - type: custom:mushroom-template-card
                    entity: sensor.rvhass_cpu_temp
                    icon: "{{ state_attr('sensor.ui_rvhass_cpu_temp', 'icon') }}"
                    icon_color: "{{ state_attr('sensor.ui_rvhass_cpu_temp', 'color') }}"
                    primary: CPU Temp
                    secondary: "{{ state_attr('sensor.ui_rvhass_cpu_temp', 'label') }}"
                    badge_icon: "{{ state_attr('sensor.ui_rvhass_cpu_temp', 'badge') }}"
                    badge_color: "{{ state_attr('sensor.ui_rvhass_cpu_temp', 'badge_color') }}"
                    multiline_secondary: true
                    tap_action:
                      action: more-info
                    icon_tap_action:
                      action: more-info
                    grid_options:
                      columns: 12
                      rows: auto

          - type: custom:mushroom-title-card
            subtitle: Stopped Services
            visibility:
              - condition: state
                entity: binary_sensor.rvhass_display_services
                state: 'on'

          - type: custom:auto-entities
            card:
              type: custom:mushroom-chips-card
              alignment: start
            card_param: chips
            filter:
              include:
                - entity_id: binary_sensor.rvhass*_service_*
                  state: 'off'
                  options:
                    type: template
                    entity: this.entity_id
                    icon: |
                      {{- 'mdi:cog' if is_state(entity,'on') else 'mdi:cog-off' -}}
                    icon_color: |
                      {{- 'green' if is_state(entity,'on') else 'red' -}}
                    content: >
                      {%- set s = state_attr(entity,'friendly_name') | default('', true) -%}
                      {{- (s.partition('Service')[2]) if 'Service' in s else '' -}}
                    tap_action: none
              exclude: []
            sort:
              method: name

      - type: vertical-stack
        cards:

          # Header
          - type: custom:mushroom-title-card
            title: Vultr VPS
            subtitle: "{{ states('sensor.vultr_overall_status_summary') }}"

          # Summary
          - type: horizontal-stack
            cards:
              - type: custom:mushroom-template-card
                entity: sensor.vultr_uptime_s
                icon: "{{ state_attr('sensor.ui_vultr_uptime_s', 'icon') }}"
                icon_color: "{{ state_attr('sensor.ui_vultr_uptime_s', 'color') }}"
                primary: Uptime
                secondary: "{{ state_attr('sensor.ui_vultr_uptime_s', 'label_vertical') }}"
                badge_icon: "{{ state_attr('sensor.ui_vultr_uptime_s', 'badge') }}"
                badge_color: "{{ state_attr('sensor.ui_vultr_uptime_s', 'badge_color') }}"
                layout: vertical
                multiline_secondary: true
                tap_action:
                  action: more-info
                icon_tap_action:
                  action: toggle
                grid_options:
                  columns: 12
                  rows: auto
              - type: custom:mushroom-template-card
                entity: sensor.vultr_updates_pending
                icon: "{{ state_attr('sensor.ui_vultr_updates_pending', 'icon') }}"
                icon_color: "{{ state_attr('sensor.ui_vultr_updates_pending', 'color') }}"
                primary: Updates
                secondary: "{{ state_attr('sensor.ui_vultr_updates_pending', 'label_vertical') }}"
                badge_icon: "{{ state_attr('sensor.ui_vultr_updates_pending', 'badge') }}"
                badge_color: "{{ state_attr('sensor.ui_vultr_updates_pending', 'badge_color') }}"
                layout: vertical
                multiline_secondary: true
                tap_action:
                  action: more-info
                icon_tap_action:
                  action: toggle
                grid_options:
                  columns: 12
                  rows: auto
              - type: custom:mushroom-template-card
                entity: binary_sensor.vultr_status_alarm
                icon: "{{ state_attr('sensor.ui_vultr_status_alarm', 'icon') }}"
                icon_color: "{{ state_attr('sensor.ui_vultr_status_alarm', 'color') }}"
                primary: Status
                secondary: "{{ state_attr('sensor.ui_vultr_status_alarm', 'long_label_vertical') }}"
                badge_icon: "{{ state_attr('sensor.ui_vultr_status_alarm', 'badge') }}"
                badge_color: "{{ state_attr('sensor.ui_vultr_status_alarm', 'badge_color') }}"
                layout: vertical
                multiline_secondary: true
                tap_action:
                  action: more-info
                icon_tap_action:
                  action: toggle
                grid_options:
                  columns: 12
                  rows: auto
          - type: horizontal-stack
            cards:
              - type: custom:mushroom-template-card
                entity: sensor.vultr_cpu_utilization
                icon: "{{ state_attr('sensor.ui_vultr_cpu_utilization_load', 'icon') }}"
                icon_color: "{{ state_attr('sensor.ui_vultr_cpu_utilization_load', 'color') }}"
                primary: CPU
                secondary: "{{ state_attr('sensor.ui_vultr_cpu_utilization_load', 'label') }}"
                badge_icon: "{{ state_attr('sensor.ui_vultr_cpu_utilization_load', 'badge') }}"
                badge_color: "{{ state_attr('sensor.ui_vultr_cpu_utilization_load', 'badge_color') }}"
                multiline_secondary: true
                tap_action:
                  action: more-info
                icon_tap_action:
                  action: toggle
                grid_options:
                  columns: 12
                  rows: auto
              - type: custom:mushroom-template-card
                entity: sensor.vultr_memory_percent
                icon: "{{ state_attr('sensor.ui_vultr_memory_percent', 'icon') }}"
                icon_color: "{{ state_attr('sensor.ui_vultr_memory_percent', 'color') }}"
                primary: Memory
                secondary: "{{ state_attr('sensor.ui_vultr_memory_percent', 'long_desc') }}"
                badge_icon: "{{ state_attr('sensor.ui_vultr_memory_percent', 'badge') }}"
                badge_color: "{{ state_attr('sensor.ui_vultr_memory_percent', 'badge_color') }}"
                multiline_secondary: true
                tap_action:
                  action: more-info
                icon_tap_action:
                  action: toggle
                grid_options:
                  columns: 12
                  rows: auto
          - type: horizontal-stack
            cards:
              - type: vertical-stack
                cards:
                  - type: custom:mushroom-template-card
                    entity: sensor.vultr_fs_root_percent
                    icon: "{{ state_attr('sensor.ui_vultr_fs_root_percent', 'icon') }}"
                    icon_color: "{{ state_attr('sensor.ui_vultr_fs_root_percent', 'color') }}"
                    primary: Disk
                    secondary: "{{ state_attr('sensor.ui_vultr_fs_root_percent', 'long_desc') }}"
                    badge_icon: "{{ state_attr('sensor.ui_vultr_fs_root_percent', 'badge') }}"
                    badge_color: "{{ state_attr('sensor.ui_vultr_fs_root_percent', 'badge_color') }}"
                    multiline_secondary: true
                    tap_action:
                      action: more-info
                    icon_tap_action:
                      action: toggle
                    grid_options:
                      columns: 12
                      rows: auto

          - type: custom:mushroom-title-card
            subtitle: Stopped Services
            visibility:
              - condition: state
                entity: binary_sensor.vultr_display_services
                state: 'on'

          - type: custom:auto-entities
            card:
              type: custom:mushroom-chips-card
              alignment: start
            card_param: chips
            filter:
              include:
                - entity_id: binary_sensor.vultr*_service_*
                  state: 'off'
                  options:
                    type: template
                    entity: this.entity_id
                    icon: |
                      {{- 'mdi:cog' if is_state(entity,'on') else 'mdi:cog-off' -}}
                    icon_color: |
                      {{- 'green' if is_state(entity,'on') else 'red' -}}
                    content: >
                      {%- set s = state_attr(entity,'friendly_name') | default('', true) -%}
                      {{- (s.partition('Service')[2]) if 'Service' in s else '' -}}
                    tap_action: none
              exclude: []
            sort:
              method: name

