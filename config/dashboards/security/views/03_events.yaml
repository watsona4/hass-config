type: panel
title: Events
cards:
  - type: custom:advanced-camera-card
    cameras:
      - camera_entity: camera.garage_camera
      - camera_entity: camera.doorbell_camera
      - camera_entity: camera.backyard_camera
      - camera_entity: camera.driveway_camera
      - camera_entity: camera.rv_camera
      - camera_entity: camera.side_yard_camera
    view:
      default: clips
    menu:
      buttons:
        live:
          enabled: false
